---
phase: 03-slack-interactive-approval
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apphosting.yaml
  - .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md
autonomous: false
requirements:
  - ENV-S01
user_setup:
  - service: slack
    why: "Slack App must be created in Mike's workspace before the Phase 3 code can post or receive messages. ENV-S01 explicitly calls this out — code references the secrets, but Mike fills in the values."
    env_vars:
      - name: SLACK_BOT_TOKEN
        source: "Slack App → OAuth & Permissions → Bot User OAuth Token (xoxb-...)"
      - name: SLACK_SIGNING_SECRET
        source: "Slack App → Basic Information → Signing Secret"
      - name: SLACK_PAYLOAD_SECRET
        source: "Self-generated — `openssl rand -base64 32` — distinct from Slack signing secret"
      - name: SLACK_RELEASE_APPROVAL_CHANNEL
        source: "Slack channel name; default '#release-approvals'"
    dashboard_config:
      - task: "Create Slack App in workspace"
        location: "https://api.slack.com/apps → Create New App → From scratch"
      - task: "Add chat:write bot scope under OAuth & Permissions"
        location: "Slack App → OAuth & Permissions → Scopes → Bot Token Scopes → Add `chat:write`"
      - task: "Install app to workspace"
        location: "Slack App → Install App → Install to Workspace"
      - task: "Enable Interactivity & Shortcuts; set Request URL to https://admin.triarch.dev/api/slack/interact"
        location: "Slack App → Interactivity & Shortcuts → toggle ON, set Request URL"
      - task: "Invite the bot to #release-approvals (or whatever SLACK_RELEASE_APPROVAL_CHANNEL is set to)"
        location: "Slack channel → /invite @<bot-name>"
      - task: "Find Mike's Slack user_id (Profile → ⋯ → Copy member ID) and edit src/lib/slack-identity.ts SLACK_USER_MAP to add it → mike@triarchsecurity.com"
        location: "Slack profile menu"
must_haves:
  truths:
    - "apphosting.yaml exposes SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, SLACK_PAYLOAD_SECRET, SLACK_RELEASE_APPROVAL_CHANNEL via the App Hosting secrets/env mechanism"
    - "An HUMAN-UAT document captures the exact sequence Mike runs to make Phase 3 functional in production"
    - "The phase plans collectively make ENV-S01 satisfiable: code expects the secrets at the names declared in apphosting.yaml; Mike sets the values"
  artifacts:
    - path: apphosting.yaml
      provides: "Three new secret references + one env var for the channel"
      contains: "SLACK_BOT_TOKEN"
    - path: .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md
      provides: "Step-by-step Slack App setup + secret seeding + identity mapping checklist"
      contains: "Slack App"
  key_links:
    - from: apphosting.yaml
      to: src/lib/slack.ts
      via: "SLACK_BOT_TOKEN consumed at runtime by postSlackMessage"
      pattern: "SLACK_BOT_TOKEN"
    - from: apphosting.yaml
      to: src/lib/slack-crypto.ts
      via: "SLACK_SIGNING_SECRET + SLACK_PAYLOAD_SECRET consumed by verifySlackSignature / signPayload / verifyPayload"
      pattern: "SLACK_SIGNING_SECRET|SLACK_PAYLOAD_SECRET"
---

<objective>
Add the Slack secrets + channel env var to the App Hosting deploy config and document the human steps required to make the secrets live.

Purpose: ENV-S01 has two halves. (1) CODE side — the deploy config must declare the secrets so the runtime can consume them; this is fully implementable now. (2) HUMAN-UAT side — Mike must create the Slack App, generate the payload secret, store all three values via `firebase apphosting:secrets:set`, populate the SLACK_USER_MAP entry, and invite the bot to the channel. The HUMAN-UAT document is the persistent checklist Mike runs through (and re-runs in the v1.15 staging environment, etc.).

Output: Edited `apphosting.yaml` with three new secret references + one new env var; new `.planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` capturing the setup runbook.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-slack-interactive-approval/03-CONTEXT.md
@apphosting.yaml
@CLAUDE.md
@/Users/mikegeehan/claude/CLAUDE.md

<interfaces>
<!-- Existing apphosting.yaml secret pattern (DO NOT CHANGE the existing entries) -->

From apphosting.yaml:
```yaml
env:
  - variable: NEXTAUTH_URL
    value: https://admin.triarch.dev
    availability: [BUILD, RUNTIME]

  # Secrets — set via: firebase apphosting:secrets:set <KEY>
  - variable: DATABASE_URL
    secret: DATABASE_URL

  - variable: NEXTAUTH_SECRET
    secret: NEXTAUTH_SECRET
  ...
```

Pattern for new secret: `variable:` (env var name read by Node) + `secret:` (Secret Manager secret name). The two are usually identical for clarity.

Pattern for plain env var: `variable:` + `value:` + optional `availability:`.

<!-- Code side already-shipped expectations -->
- `src/lib/slack.ts` reads `process.env.SLACK_BOT_TOKEN` and `process.env.SLACK_RELEASE_APPROVAL_CHANNEL`
- `src/lib/slack-crypto.ts` reads `process.env.SLACK_SIGNING_SECRET` and `process.env.SLACK_PAYLOAD_SECRET`
- All four are accessed at call time, not module top — safe for build-time absence
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Slack secret + env var entries to apphosting.yaml</name>
  <files>apphosting.yaml</files>
  <read_first>
    - apphosting.yaml (entire file — the existing pattern is the contract)
    - .planning/phases/03-slack-interactive-approval/03-CONTEXT.md (Area 2: secrets storage decision)
  </read_first>
  <action>
    Append four new entries to the `env:` array in `apphosting.yaml`, AFTER the existing `DEPLOY_WEBHOOK_SECRET` entry. Match the existing indentation (2 spaces for list items, list items use `-` followed by aligned key/value).

    Add a comment block above the new entries explaining their purpose and the documented setup runbook.

    Exact additions:

    ```yaml

      # Slack App secrets (Phase 3) — see .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md
      # Set via: firebase apphosting:secrets:set <KEY>
      # SLACK_PAYLOAD_SECRET is self-generated: openssl rand -base64 32
      - variable: SLACK_BOT_TOKEN
        secret: SLACK_BOT_TOKEN

      - variable: SLACK_SIGNING_SECRET
        secret: SLACK_SIGNING_SECRET

      - variable: SLACK_PAYLOAD_SECRET
        secret: SLACK_PAYLOAD_SECRET

      - variable: SLACK_RELEASE_APPROVAL_CHANNEL
        value: "#release-approvals"
        availability:
          - RUNTIME
    ```

    Per CONTEXT.md Area 2: three secrets total — SLACK_BOT_TOKEN (Slack-issued), SLACK_SIGNING_SECRET (Slack-issued), SLACK_PAYLOAD_SECRET (self-generated). The fourth entry (channel name) is plain env, not a secret.

    Do NOT modify any existing entries. Do NOT add `availability: [BUILD]` to the secrets — they are RUNTIME only (slack functions are called only at request time, never at build).

    Validate the resulting YAML by reading it back with `node -e "console.log(require('js-yaml').load(require('fs').readFileSync('apphosting.yaml','utf8')))"` if js-yaml is available, or by `npx --yes yaml-lint apphosting.yaml`. Fall back to a manual visual check if neither is available — the indentation MUST match the existing entries exactly (App Hosting will reject malformed YAML and fail the deploy).
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin && grep -c "SLACK_BOT_TOKEN\|SLACK_SIGNING_SECRET\|SLACK_PAYLOAD_SECRET\|SLACK_RELEASE_APPROVAL_CHANNEL" apphosting.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "variable: SLACK_BOT_TOKEN" apphosting.yaml` returns 1
    - `grep -c "secret: SLACK_BOT_TOKEN" apphosting.yaml` returns 1
    - `grep -c "variable: SLACK_SIGNING_SECRET" apphosting.yaml` returns 1
    - `grep -c "secret: SLACK_SIGNING_SECRET" apphosting.yaml` returns 1
    - `grep -c "variable: SLACK_PAYLOAD_SECRET" apphosting.yaml` returns 1
    - `grep -c "secret: SLACK_PAYLOAD_SECRET" apphosting.yaml` returns 1
    - `grep -c "variable: SLACK_RELEASE_APPROVAL_CHANNEL" apphosting.yaml` returns 1
    - `grep -c "value: \"#release-approvals\"" apphosting.yaml` returns 1
    - `grep -c "availability:" apphosting.yaml` returns at least 4 (existing 3 + new channel-runtime entry)
    - YAML parses cleanly: `node -e "require('fs').readFileSync('apphosting.yaml','utf8'); console.log('ok')"` exits 0 (file readable; YAML parse if a parser available)
    - `npx next build` exits 0 (apphosting.yaml is not consumed at build, but a sanity build confirms nothing else broke)
  </acceptance_criteria>
  <done>Four new entries appended to apphosting.yaml with correct indentation; secrets reference SecretManager keys; channel is a runtime-only env var; build still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create 03-HUMAN-UAT.md runbook</name>
  <files>.planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/03-slack-interactive-approval/03-CONTEXT.md
    - .planning/ROADMAP.md (Phase 3 success criteria)
    - apphosting.yaml (just modified)
  </read_first>
  <action>
    Create `.planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` with the full runbook. Structure:

    ```markdown
    # Phase 3 HUMAN-UAT: Slack Interactive Approval

    Phase 3 ships secure code that depends on real-world Slack configuration. This runbook is the human-side setup. Run through it once for production, once for any new env (staging, preview).

    ## Prerequisites

    - Phase 3 plans 01–04 deployed (code is on https://admin.triarch.dev)
    - Firebase CLI logged in to the triarch-dev-admin project
    - Workspace admin access to the Slack workspace where #release-approvals lives

    ## Step 1 — Create the Slack App

    1. Visit https://api.slack.com/apps → "Create New App" → "From scratch"
    2. App Name: `Triarch Release Gate` (or similar)
    3. Workspace: select your workspace
    4. After creation, you land on the app's Basic Information page. Note the **Signing Secret** under "App Credentials" — copy it now.

    ## Step 2 — Add bot scope and install

    1. Sidebar → OAuth & Permissions
    2. Scroll to "Scopes" → "Bot Token Scopes" → Add `chat:write`
    3. Top of page → "Install to Workspace" → approve
    4. After install, copy the **Bot User OAuth Token** (starts with `xoxb-`)

    ## Step 3 — Generate the payload secret

    Run locally:
    ```bash
    openssl rand -base64 32
    ```
    Copy the output. This is `SLACK_PAYLOAD_SECRET` — distinct from the Slack signing secret.

    ## Step 4 — Push secrets to App Hosting

    From the admin repo root:
    ```bash
    firebase apphosting:secrets:set SLACK_BOT_TOKEN
    # paste the xoxb- token from Step 2
    firebase apphosting:secrets:set SLACK_SIGNING_SECRET
    # paste the signing secret from Step 1
    firebase apphosting:secrets:set SLACK_PAYLOAD_SECRET
    # paste the openssl output from Step 3
    ```

    Then redeploy (next push to main triggers App Hosting).

    ## Step 5 — Wire interactivity

    Back in https://api.slack.com/apps → your app → "Interactivity & Shortcuts":
    1. Toggle "Interactivity" ON
    2. Request URL: `https://admin.triarch.dev/api/slack/interact`
    3. Save

    Slack will ping the URL once with a verification request — the route should accept it (verifySlackSignature passes; payload type !== block_actions returns 400, but Slack is satisfied with the 4xx as long as the signature verifies).

    If you see Slack complain about the URL, tail the Cloud Run logs and look for `[slack-interact]` warnings.

    ## Step 6 — Invite the bot to the channel

    In Slack:
    ```
    /invite @Triarch Release Gate
    ```
    in `#release-approvals` (or whatever channel SLACK_RELEASE_APPROVAL_CHANNEL is set to in apphosting.yaml).

    ## Step 7 — Populate SLACK_USER_MAP

    The signed-in staff identity that clicks Approve/Reject in Slack must map to a real staff email (so the audit row's approver_email is correct).

    1. In Slack, click your profile photo → "..." menu → "Copy member ID"
    2. Open `src/lib/slack-identity.ts`
    3. Replace the placeholder comment with:
       ```ts
       export const SLACK_USER_MAP: Record<string, string> = {
         'U01ABCDEF': 'mike@triarchsecurity.com', // Mike — replace with your actual member ID
       };
       ```
    4. Commit + push (version bump per CLAUDE.md)

    ## Step 8 — End-to-end smoke test

    1. Sign in to https://admin.triarch.dev as a customer-admin email of a project (e.g. mike@mikegeehan.com on darksouls-rpg, or seed a test project)
    2. Navigate to /projects/darksouls-rpg/releases
    3. On a release in `dev` status, click "Approve for Production" → confirm
    4. Within a few seconds, a message lands in `#release-approvals` with two buttons
    5. As mike@triarchsecurity.com (mapped Slack user), click "Approve & Promote"
    6. Message updates to ":white_check_mark: Promoted to production by @mike (mike@triarchsecurity.com)"
    7. Verify in DB: `release_logs.status = 'approved'`, second `release_approvals` row exists with approver_email='mike@triarchsecurity.com'

    Re-click the same button → ephemeral "Already promoted by … on …" message; no new audit row.

    ## Verification checklist

    - [ ] Slack App created with chat:write scope
    - [ ] Three secrets pushed via `firebase apphosting:secrets:set`
    - [ ] Interactivity Request URL set to https://admin.triarch.dev/api/slack/interact
    - [ ] Bot invited to #release-approvals
    - [ ] SLACK_USER_MAP populated and pushed
    - [ ] End-to-end smoke test passed (steps 8.1–8.7)
    - [ ] Re-click idempotency confirmed (step 8 final paragraph)

    ## Rotation

    To rotate any secret (Slack-issued or self-generated):
    ```bash
    firebase apphosting:secrets:set <KEY>
    # paste new value
    ```
    Then trigger a redeploy (push a no-op version bump). No grace window — old secret invalidates immediately. (CONTEXT.md Area 2: dual-secret rotation is a deferred idea.)

    ## Troubleshooting

    - Approve clicked, no Slack message: check Cloud Run logs for `[slack]` warnings — likely SLACK_BOT_TOKEN unset or bot not in channel
    - Slack button click → "Verification failed" toast: check Cloud Run logs for sig.reason — bad_signature means SLACK_SIGNING_SECRET mismatch; stale means clock skew
    - Slack button click → "not mapped to a staff account": SLACK_USER_MAP missing the clicker's user_id; refer back to Step 7
    - DB shows status updated but Slack message didn't update: response_url failures — Slack will sometimes timeout the in-channel update; the DB action still committed
    ```

    Tone: Mike-facing checklist, no team / stakeholder language, no enterprise patterns. Practical and copy-paste-able.
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin && test -f .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md && wc -l .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` exists
    - `grep -c "Step 1 — Create the Slack App" .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` returns 1
    - `grep -c "openssl rand -base64 32" .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` returns at least 1
    - `grep -c "firebase apphosting:secrets:set" .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` returns at least 3
    - `grep -c "https://admin.triarch.dev/api/slack/interact" .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` returns at least 1
    - `grep -c "SLACK_USER_MAP" .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` returns at least 1
    - `grep -c "Verification checklist" .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` returns 1
    - `wc -l < .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` returns at least 60
  </acceptance_criteria>
  <done>Runbook captures every Slack-side and apphosting-side step Mike runs to make Phase 3 functional in production; matches CONTEXT.md decisions on rotation policy and identity mapping.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mike runs the HUMAN-UAT and confirms ENV-S01 is satisfied</name>
  <files>.planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md</files>
  <what-built>
    All Phase 3 code (plans 03-01 through 03-04) plus the apphosting.yaml secret references and the HUMAN-UAT runbook (this plan, tasks 1 + 2). Code is shipped; Slack-side configuration is the gate.
  </what-built>
  <how-to-verify>
    Open `.planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` and execute steps 1 through 8 in order. The runbook is self-contained.

    The "Verification checklist" at the bottom of the runbook is the gate:
    - [ ] Slack App created with chat:write scope
    - [ ] Three secrets pushed via firebase apphosting:secrets:set
    - [ ] Interactivity Request URL set to https://admin.triarch.dev/api/slack/interact
    - [ ] Bot invited to #release-approvals
    - [ ] SLACK_USER_MAP populated and pushed
    - [ ] End-to-end smoke test passed (steps 8.1–8.7)
    - [ ] Re-click idempotency confirmed

    Once every box is ticked, ENV-S01 is satisfied and Phase 3 is shippable.
  </how-to-verify>
  <action>
    Mike opens `.planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md` and follows steps 1–8. This task is a human checkpoint — the work is described in `<how-to-verify>`. Do not auto-resolve.
  </action>
  <verify>
    Manual — Mike confirms every box on the verification checklist is ticked. Resume signal "approved" closes this task.
  </verify>
  <acceptance_criteria>
    - Mike replies "approved" after working through 03-HUMAN-UAT.md
    - All seven checkboxes in the runbook's "Verification checklist" section are ticked
  </acceptance_criteria>
  <done>ENV-S01 satisfied: Slack App live, secrets pushed, SLACK_USER_MAP populated, end-to-end smoke test green, idempotency confirmed.</done>
  <resume-signal>Type "approved" when the verification checklist is fully ticked, or describe what blocks you (Slack permissions, secret push errors, etc.) and we'll triage.</resume-signal>
</task>

</tasks>

<verification>
- apphosting.yaml has four new entries with correct YAML structure (3 secrets + 1 plain env var)
- 03-HUMAN-UAT.md exists with all 8 steps + verification checklist + rotation + troubleshooting
- This plan is `autonomous: false` because the final task is a human verification checkpoint
- The human checkpoint is the formal gate for ENV-S01 — code is implementable now, runtime depends on the runbook
</verification>

<success_criteria>
- ENV-S01 satisfiable: code references the secrets at the names declared in apphosting.yaml; the runbook walks Mike through populating them
- After Mike runs the runbook, the full Phase 3 flow works: customer approve → Slack message → button click → DB update + message replacement
- Rotation policy is documented; troubleshooting hints exist for the three most likely failure modes
- The human checkpoint is the gate, not a blocker — Mike can run plans 01-04 first, ship the code, then run the runbook
</success_criteria>

<output>
After completion, create `.planning/phases/03-slack-interactive-approval/03-05-SUMMARY.md`
</output>
