---
phase: 04-github-app-promotion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apphosting.yaml
  - .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md
autonomous: false
requirements:
  - ENV-G01
  - GATE-11a
user_setup:
  - service: github
    why: "GitHub App must be created in MyAlterLego org and installed before Phase 4 code can dispatch workflows. ENV-G01 + GATE-11a explicitly call this out — code references the secrets and consumes the App pattern, but Mike creates the App, generates the private key, installs it, and pushes the secret values."
    env_vars:
      - name: GITHUB_APP_ID
        source: "GitHub App settings → App ID (numeric, top of the App's General page)"
      - name: GITHUB_APP_PRIVATE_KEY
        source: "GitHub App settings → Private keys → Generate a private key (downloads .pem). Push entire PEM contents (including BEGIN/END lines) to the secret."
      - name: GITHUB_APP_INSTALLATION_ID
        source: "After installing the App on the MyAlterLego org, visit https://github.com/organizations/MyAlterLego/settings/installations — installation ID is in the URL: /installations/{ID}/"
    dashboard_config:
      - task: "Create GitHub App in MyAlterLego org"
        location: "https://github.com/organizations/MyAlterLego/settings/apps -> New GitHub App"
      - task: "Set permissions: Actions=Read and write, Contents=Read-only, Metadata=Read-only"
        location: "App settings -> Permissions and events -> Repository permissions"
      - task: "Generate and download private key (.pem)"
        location: "App settings -> Private keys -> Generate a private key"
      - task: "Install App on MyAlterLego org with access to admin-managed repos"
        location: "App settings -> Install App -> choose org -> select repos (or All repositories)"
      - task: "Push 3 secrets via firebase apphosting:secrets:set"
        location: "Local terminal in admin repo root"
must_haves:
  truths:
    - "apphosting.yaml exposes GITHUB_APP_ID, GITHUB_APP_PRIVATE_KEY, GITHUB_APP_INSTALLATION_ID via the App Hosting secret/env mechanism, RUNTIME availability"
    - "An HUMAN-UAT document captures the exact GitHub App creation, installation, and secret-push sequence Mike runs to make Phase 4 functional in production"
    - "Code (Plan 04-02 + 04-04) references the three env var names exactly as declared in apphosting.yaml. Mike sets the values."
    - "The runbook includes the exact GitHub App permissions required (actions:write, contents:read, metadata:read) per GATE-11a"
  artifacts:
    - path: apphosting.yaml
      provides: "Three new GitHub App secret references appended to the env array"
      contains: "GITHUB_APP_ID"
    - path: .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md
      provides: "Step-by-step GitHub App setup, private key generation, installation, secret seeding, db push, smoke test"
      contains: "GitHub App"
  key_links:
    - from: apphosting.yaml
      to: src/lib/github-app.ts
      via: "GITHUB_APP_ID + GITHUB_APP_PRIVATE_KEY + GITHUB_APP_INSTALLATION_ID consumed at request time by readEnv inside getInstallationToken"
      pattern: "GITHUB_APP_ID|GITHUB_APP_PRIVATE_KEY|GITHUB_APP_INSTALLATION_ID"
    - from: .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md
      to: GitHub App
      via: "Mike follows the runbook to create + install the App and push the secrets"
      pattern: "https://github.com/organizations/MyAlterLego/settings/apps"
---

<objective>
Add the three GitHub App secret references to `apphosting.yaml` and ship a `04-HUMAN-UAT.md` runbook that walks Mike through GitHub App creation in the `MyAlterLego` org, private key generation, installation, secret seeding via `firebase apphosting:secrets:set`, schema migration push (Plan 04-01's 0009 migration), and an end-to-end smoke test.

Purpose: ENV-G01 + GATE-11a have two halves. (1) CODE side - `apphosting.yaml` declares the three env vars so the runtime can consume them; this is fully implementable now. (2) HUMAN-UAT side - Mike creates the GitHub App with the exact permissions, generates the private key, installs it on MyAlterLego, pushes the secret values, runs the schema migration from Plan 04-01 (`db:push`), and verifies the end-to-end flow once Plan 04-04 is wired. The HUMAN-UAT document is the persistent checklist Mike runs through.

Output: Edited `apphosting.yaml` with three new secret references; new `.planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` runbook; this plan is `autonomous: false` because Task 3 is the formal HUMAN-UAT checkpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-github-app-promotion/04-CONTEXT.md
@.planning/phases/03-slack-interactive-approval/03-05-PLAN.md
@apphosting.yaml
@CLAUDE.md
@/Users/mikegeehan/claude/CLAUDE.md

<interfaces>
<!-- Existing apphosting.yaml secret pattern from Phase 3 - DO NOT modify the existing entries -->

From apphosting.yaml (current state, after Phase 3):
```yaml
env:
  - variable: NEXTAUTH_URL
    value: https://admin.triarch.dev
    availability: [BUILD, RUNTIME]
  # ... existing secrets ...
  - variable: SLACK_BOT_TOKEN
    secret: SLACK_BOT_TOKEN
  - variable: SLACK_SIGNING_SECRET
    secret: SLACK_SIGNING_SECRET
  - variable: SLACK_PAYLOAD_SECRET
    secret: SLACK_PAYLOAD_SECRET
  - variable: SLACK_RELEASE_APPROVAL_CHANNEL
    value: "#release-approvals"
    availability:
      - RUNTIME
```

Pattern: `variable:` (env var name read by Node) + `secret:` (Secret Manager secret name). RUNTIME default is implicit when only `secret:` is specified — confirmed by Phase 3's working SLACK_BOT_TOKEN/SLACK_SIGNING_SECRET/SLACK_PAYLOAD_SECRET entries that do not set `availability:`.

<!-- Phase 4 code expectations (already established by Plan 04-02) -->

- `src/lib/github-app.ts` reads `process.env.GITHUB_APP_ID`, `process.env.GITHUB_APP_PRIVATE_KEY`, `process.env.GITHUB_APP_INSTALLATION_ID` at call time (not import time)
- All three accessed inside `readEnv()` - safe for build-time absence
- Plan 04-04 imports `dispatchWorkflow` from `@/lib/github-app` and calls it inside the Slack interact route's promote path

<!-- Phase 3's HUMAN-UAT structure (the model to mirror) -->

From .planning/phases/03-slack-interactive-approval/03-05-PLAN.md:
- 8-step runbook: create App, scope, install, generate secret, push secrets, wire interactivity, invite bot, smoke test
- Verification checklist at the end
- Rotation policy + Troubleshooting sections
- Mike-facing tone, no team or stakeholder language

<!-- GitHub App permissions required per GATE-11a -->

| Permission | Level |
|------------|-------|
| Actions    | Read and write (required for workflow_dispatch) |
| Contents   | Read-only (required for repo metadata) |
| Metadata   | Read-only (mandatory for all GitHub Apps) |

No Pull requests, no Issues, no Webhooks subscription, no Deployments. Keep the App's blast radius minimal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GitHub App secret entries to apphosting.yaml</name>
  <files>apphosting.yaml</files>
  <read_first>
    - apphosting.yaml (entire file - the existing pattern is the contract)
    - .planning/phases/04-github-app-promotion/04-CONTEXT.md (Area 1 - secrets stored in App Hosting)
  </read_first>
  <action>
    Append three new entries to the `env:` array in `apphosting.yaml`, AFTER the existing `SLACK_RELEASE_APPROVAL_CHANNEL` entry. Match the existing indentation exactly (2 spaces for the list item, list items use `-` followed by aligned key/value).

    Add a comment block above the new entries explaining their purpose and pointing to the runbook.

    Exact additions (verbatim, including the leading blank line for separation):

    ```yaml

      # GitHub App secrets (Phase 4) - see .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md
      # Set via: firebase apphosting:secrets:set <KEY>
      # GITHUB_APP_PRIVATE_KEY: paste entire PEM contents including BEGIN/END lines
      - variable: GITHUB_APP_ID
        secret: GITHUB_APP_ID

      - variable: GITHUB_APP_PRIVATE_KEY
        secret: GITHUB_APP_PRIVATE_KEY

      - variable: GITHUB_APP_INSTALLATION_ID
        secret: GITHUB_APP_INSTALLATION_ID
    ```

    Per CONTEXT.md Area 1: three secrets total, ALL stored in App Hosting. The existing `GITHUB_TOKEN` PAT stays for legacy paths and is unaffected. The build-time `GITHUB_PACKAGES_TOKEN` / `NODE_AUTH_TOKEN` entry is a different secret for npm package fetches and is also unaffected.

    Per Phase 3 precedent: do NOT add `availability: [BUILD]` - these are RUNTIME only (github-app.ts is called only at request time, never at build).

    Do NOT modify any existing entry. The indentation MUST match the existing entries exactly (App Hosting will reject malformed YAML and fail the deploy).
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin && grep -c "variable: GITHUB_APP_ID" apphosting.yaml && grep -c "secret: GITHUB_APP_ID" apphosting.yaml && grep -c "variable: GITHUB_APP_PRIVATE_KEY" apphosting.yaml && grep -c "secret: GITHUB_APP_PRIVATE_KEY" apphosting.yaml && grep -c "variable: GITHUB_APP_INSTALLATION_ID" apphosting.yaml && grep -c "secret: GITHUB_APP_INSTALLATION_ID" apphosting.yaml && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "variable: GITHUB_APP_ID" apphosting.yaml` returns 1
    - `grep -c "secret: GITHUB_APP_ID" apphosting.yaml` returns 1
    - `grep -c "variable: GITHUB_APP_PRIVATE_KEY" apphosting.yaml` returns 1
    - `grep -c "secret: GITHUB_APP_PRIVATE_KEY" apphosting.yaml` returns 1
    - `grep -c "variable: GITHUB_APP_INSTALLATION_ID" apphosting.yaml` returns 1
    - `grep -c "secret: GITHUB_APP_INSTALLATION_ID" apphosting.yaml` returns 1
    - `grep -c "variable: SLACK_BOT_TOKEN" apphosting.yaml` returns 1 (Phase 3 entry untouched)
    - `grep -c "variable: NODE_AUTH_TOKEN" apphosting.yaml` returns 1 (build-time PAT entry untouched)
    - `node -e "require('fs').readFileSync('apphosting.yaml','utf8')"` exits 0 (file readable)
    - `npx next build` exits 0 (production build still passes - apphosting.yaml is not consumed at build, but a sanity build confirms nothing else broke)
    - `npm test` exits 0 (existing Vitest suite still green)
  </acceptance_criteria>
  <done>Three new entries appended to apphosting.yaml with correct indentation; secrets reference Secret Manager keys; build still passes; existing tests still green.</done>
</task>

<task type="auto">
  <name>Task 2: Create 04-HUMAN-UAT.md runbook for GitHub App setup</name>
  <files>.planning/phases/04-github-app-promotion/04-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/04-github-app-promotion/04-CONTEXT.md (decisions on GitHub App scope, error handling, audit trail)
    - .planning/phases/03-slack-interactive-approval/03-05-PLAN.md (HUMAN-UAT structure to mirror)
    - .planning/ROADMAP.md (Phase 4 success criteria)
    - apphosting.yaml (just modified - confirm names match)
  </read_first>
  <action>
    Create `.planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` with the full runbook. Use the Write tool. Structure:

    ```markdown
    # Phase 4 HUMAN-UAT: GitHub App Promotion

    Phase 4 ships secure code that depends on a real GitHub App installation in the MyAlterLego org. This runbook is the human-side setup. Run through it once for production; re-run for any new staging environment that needs to dispatch workflows.

    ## Prerequisites

    - Phase 4 plans 04-01 (schema migration), 04-02 (github-app.ts + tests), 04-03 (this plan's apphosting.yaml edits) merged to main
    - Plan 04-04 NOT yet required - the smoke test in step 8 needs 04-04 wired, but the App can be created and secrets pushed in parallel
    - Firebase CLI logged in to the triarch-dev-admin App Hosting backend
    - Org admin permissions on github.com/MyAlterLego

    ## Step 1 - Create the GitHub App

    1. Visit https://github.com/organizations/MyAlterLego/settings/apps
    2. Click "New GitHub App"
    3. Fill in:
       - **GitHub App name**: `triarch-dev-promotion` (or similar - must be globally unique)
       - **Homepage URL**: https://admin.triarch.dev
       - **Webhook**: UNCHECK "Active" (no webhook needed for dispatch-only flow)
       - **Webhook URL / secret**: leave blank
    4. Click "Create GitHub App"
    5. After creation, note the **App ID** at the top of the General page - this is `GITHUB_APP_ID`

    ## Step 2 - Set repository permissions

    Still on the App's General settings page (or sidebar -> Permissions and events):

    | Permission | Level | Why |
    |------------|-------|-----|
    | Actions    | Read and write | Required for workflow_dispatch (GATE-11a) |
    | Contents   | Read-only      | Read repo metadata, default branch |
    | Metadata   | Read-only      | Mandatory for all GitHub Apps |

    Leave ALL other permissions at "No access". Specifically: no Pull requests, no Issues, no Deployments, no Webhooks subscription. Keep the blast radius minimal.

    Save changes.

    ## Step 3 - Generate the private key

    1. Sidebar -> Private keys
    2. Click "Generate a private key"
    3. Browser downloads a `.pem` file (e.g. `triarch-dev-promotion.2026-05-04.private-key.pem`)
    4. Open the PEM file in a text editor. The contents look like:
       ```
       -----BEGIN RSA PRIVATE KEY-----
       MIIEowIBAAKCAQEA...
       ...many lines...
       -----END RSA PRIVATE KEY-----
       ```
    5. Keep this file open - you paste the entire contents (including BEGIN/END lines) into Step 5.

    Security note: this is the App's master credential. Treat it like SLACK_PAYLOAD_SECRET. Never commit it. After Step 5 succeeds, delete the local `.pem` file.

    ## Step 4 - Install the App on MyAlterLego

    1. Sidebar -> Install App
    2. Find "MyAlterLego" -> click "Install"
    3. Choose "All repositories" (recommended - admin manages all org repos) OR "Only select repositories" and pick the repos that have a `deploy-prod.yml` workflow (e.g. darksouls-rpg, triarchsecurity-portal, etc.)
    4. Click "Install"
    5. After install, the URL contains the installation ID:
       ```
       https://github.com/organizations/MyAlterLego/settings/installations/<NUMBER>
       ```
       That `<NUMBER>` is `GITHUB_APP_INSTALLATION_ID`.

    Alternative: visit https://github.com/organizations/MyAlterLego/settings/installations and click the gear next to the App's name - the URL on that page contains the installation ID.

    ## Step 5 - Push 3 secrets to App Hosting

    From the admin repo root:
    ```bash
    firebase apphosting:secrets:set GITHUB_APP_ID
    # paste the App ID from Step 1 (numeric string)

    firebase apphosting:secrets:set GITHUB_APP_PRIVATE_KEY
    # paste ENTIRE PEM contents from Step 3, including the
    # -----BEGIN RSA PRIVATE KEY----- and -----END RSA PRIVATE KEY----- lines
    # Press Enter, then Ctrl-D (or whatever the firebase CLI prompts for) to commit

    firebase apphosting:secrets:set GITHUB_APP_INSTALLATION_ID
    # paste the installation ID from Step 4 (numeric string)
    ```

    Each command grants the App Hosting backend service-account access to the secret. If the CLI asks "Grant access to backend triarch-dev-admin?" answer yes.

    Then trigger a redeploy: `git push` any branch that lands on main (a no-op version bump is fine - see CLAUDE.md).

    Note on private key formatting: github-app.ts normalizes literal `\n` sequences to real newlines automatically (see Plan 04-02 readEnv). If the firebase CLI escapes newlines, the code handles it. If it preserves them as actual newlines, the code also handles it. Either form works.

    ## Step 6 - Apply the schema migration

    Plan 04-01 emitted `src/db/migrations/0009_promotion_dispatch_audit.sql`. Apply it to the production CockroachDB:

    ```bash
    # Option A: drizzle-kit push (recommended if DATABASE_URL is in your local shell)
    DATABASE_URL=<the production URL from Firebase secrets> npm run db:push

    # Option B: apply the SQL manually via cockroach sql
    cockroach sql --url <the production URL> < src/db/migrations/0009_promotion_dispatch_audit.sql
    ```

    Verify in the DB:
    ```sql
    SELECT column_name FROM information_schema.columns
    WHERE table_name = 'release_logs'
      AND column_name IN ('promotion_dispatched_at', 'promotion_dispatched_by');
    ```
    Should return both rows.

    ## Step 7 - Confirm code is deployed

    1. Wait for App Hosting to finish the redeploy from Step 5
    2. Tail the Cloud Run logs:
       ```bash
       gcloud run services logs read triarch-dev-build --project angular-concord-489522-c4 --limit 50
       ```
    3. Look for clean startup - no `[github-app] missing required env vars` errors at request time

    If you see "missing required env vars" - one of the secrets did not bind. Double-check `firebase apphosting:secrets:set` was run for each, and that the redeploy picked them up. The error message lists the specific missing var(s).

    ## Step 8 - End-to-end smoke test (requires Plan 04-04 deployed)

    Skip this step until Plan 04-04 has shipped (Wave 2). The smoke test exercises the full chain: customer approves -> Slack message posts -> Mike clicks Promote -> dispatchWorkflow fires -> deploy-prod.yml runs.

    1. Sign in to https://admin.triarch.dev as a customer-admin email of a project that has a `deploy-prod.yml` in its repo (e.g. darksouls-rpg)
    2. Navigate to /projects/darksouls-rpg/releases
    3. On a release in `dev` status, click "Approve for Production"
    4. Within a few seconds, a message lands in #release-approvals
    5. As mike@triarchsecurity.com (mapped Slack user), click "Approve and Promote"
    6. Slack message updates to ":white_check_mark: Promoted to production by @mike (mike@triarchsecurity.com)"
    7. Within ~10 seconds, a threaded reply appears: ":rocket: Workflow dispatched: deploy-prod.yml run #<N>"
    8. Visit https://github.com/MyAlterLego/darksouls-rpg/actions - confirm a fresh `deploy-prod.yml` run started with `tag` input matching the release version
    9. Verify in the DB:
       ```sql
       SELECT id, version, status, promotion_dispatched_at, promotion_dispatched_by
         FROM release_logs WHERE id = '<the release id>';
       ```
       - status = 'approved'
       - promotion_dispatched_at = recent timestamp (within the last minute)
       - promotion_dispatched_by = 'mike@triarchsecurity.com'

    Failure-mode smoke test (optional - exercises GATE-11/error handling):
    1. Temporarily set GITHUB_APP_INSTALLATION_ID to a wrong number (or revoke access to the target repo)
    2. Click Promote on another release
    3. Slack message updates to "Promoted" (the DB approval succeeds independently of dispatch)
    4. Threaded reply: ":warning: Promotion dispatch failed: <reason>"
    5. Original message gets `chat.update`'d to ":warning: Approved (promotion failed - see logs)" per CONTEXT.md Area 3
    6. Restore the secret/installation, redeploy, retry from the GitHub Actions UI manually

    ## Verification checklist

    - [ ] GitHub App created in MyAlterLego org with permissions: actions:write, contents:read, metadata:read (and nothing else)
    - [ ] Private key (.pem) generated and downloaded
    - [ ] App installed on MyAlterLego org with access to admin-managed repos
    - [ ] GITHUB_APP_ID, GITHUB_APP_PRIVATE_KEY, GITHUB_APP_INSTALLATION_ID pushed via firebase apphosting:secrets:set
    - [ ] Local .pem file deleted after secrets pushed
    - [ ] Schema migration 0009_promotion_dispatch_audit.sql applied to production CRDB
    - [ ] App Hosting redeploy succeeded; no "missing required env vars" errors in logs
    - [ ] (Once Plan 04-04 deployed) End-to-end smoke test passed - Slack click triggers a real deploy-prod.yml run
    - [ ] Database row for the test release has promotion_dispatched_at and promotion_dispatched_by populated

    ## Rotation

    To rotate the private key (e.g. annually, or on suspected compromise):
    1. GitHub App settings -> Private keys -> Generate a private key (keeps the old one valid until you delete it)
    2. `firebase apphosting:secrets:set GITHUB_APP_PRIVATE_KEY` with the new PEM
    3. Trigger redeploy
    4. Confirm Cloud Run logs show successful dispatch on the next promotion
    5. GitHub App settings -> delete the old key

    Cache implication: the in-process installation token cache (50 min TTL) holds the OLD installation token. After redeploy, fresh containers sign with the NEW key and get a fresh installation token automatically. No action needed - the cache is per-instance.

    Rotating GITHUB_APP_INSTALLATION_ID is a transplant - typically only happens if you uninstall and reinstall the App. Same firebase apphosting:secrets:set + redeploy flow.

    Rotating GITHUB_APP_ID would mean creating a new App entirely - rare; treat as a re-onboarding rather than a rotation.

    ## Troubleshooting

    - "missing required env vars" - one or more of the three secrets did not bind to the App Hosting backend; re-run `firebase apphosting:secrets:set` and confirm the CLI granted the backend service-account access
    - "installation token exchange failed: 401" - GITHUB_APP_PRIVATE_KEY is wrong or App Hosting received a corrupted PEM (escaped newlines without the BEGIN/END markers); re-paste the PEM exactly as the .pem file shows
    - "installation token exchange failed: 404" - GITHUB_APP_INSTALLATION_ID is wrong; re-check the URL on the org's installations page
    - "dispatch failed: 404 Workflow not found" - target repo does not have `.github/workflows/deploy-prod.yml` on its main branch; this is a per-project setup, not a Phase 4 concern (Phase 5 covers workflow file rollout)
    - "dispatch failed: 422 No ref found for: main" - the target repo's default branch is not main, OR it has no commits; for Phase 4 we hardcode `ref: 'main'` (per CONTEXT.md Area 2); per-project ref override is deferred
    - Threaded Slack reply never arrives - Slack posting failures are logged but swallowed; check Cloud Run logs for `[slack]` warnings
    - Slack message reaches "Approved" but `chat.update` doesn't downgrade to ":warning: Approved (promotion failed)" - the bot may not have edit access on its own messages; verify the bot was invited to #release-approvals (Phase 3's Step 6)
    ```

    Tone: Mike-facing checklist, copy-paste-able commands, no team or stakeholder language.
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin && test -f .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md && grep -c "Step 1 - Create the GitHub App" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md && grep -c "firebase apphosting:secrets:set" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md && grep -c "actions:write" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` exists
    - `grep -c "Step 1 - Create the GitHub App" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns 1
    - `grep -c "Step 2 - Set repository permissions" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns 1
    - `grep -c "Step 3 - Generate the private key" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns 1
    - `grep -c "Step 4 - Install the App on MyAlterLego" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns 1
    - `grep -c "Step 5 - Push 3 secrets to App Hosting" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns 1
    - `grep -c "Step 6 - Apply the schema migration" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns 1
    - `grep -c "Step 7 - Confirm code is deployed" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns 1
    - `grep -c "Step 8 - End-to-end smoke test" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns 1
    - `grep -c "Verification checklist" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns 1
    - `grep -c "Rotation" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns at least 1
    - `grep -c "Troubleshooting" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns at least 1
    - `grep -c "firebase apphosting:secrets:set" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns at least 3
    - `grep -c "GITHUB_APP_ID" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns at least 3
    - `grep -c "GITHUB_APP_PRIVATE_KEY" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns at least 3
    - `grep -c "GITHUB_APP_INSTALLATION_ID" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns at least 3
    - `grep -c "actions:write" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns at least 1 (GATE-11a permission scope documented)
    - `grep -c "promotion_dispatched_at" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns at least 1 (Plan 04-01 columns referenced in smoke test)
    - `grep -c "0009_promotion_dispatch_audit" .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns at least 1 (migration referenced)
    - `wc -l < .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` returns at least 100
  </acceptance_criteria>
  <done>Runbook captures every GitHub-side, App Hosting-side, and DB-side step Mike runs to make Phase 4 functional in production; matches CONTEXT.md decisions on permissions scope, PEM handling, and rotation policy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mike runs the HUMAN-UAT and confirms ENV-G01 + GATE-11a are satisfied</name>
  <files>.planning/phases/04-github-app-promotion/04-HUMAN-UAT.md</files>
  <what-built>
    apphosting.yaml has three new GitHub App secret references (Task 1); 04-HUMAN-UAT.md captures the full Mike-side runbook (Task 2). Code in Plans 04-01 (schema), 04-02 (github-app.ts), 04-04 (Slack interact wire-up) reference the secrets; values come from the GitHub App Mike creates here.
  </what-built>
  <how-to-verify>
    Open `.planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` and execute steps 1-8 in order. The runbook is self-contained.

    The "Verification checklist" at the bottom of the runbook is the gate:
    - [ ] GitHub App created in MyAlterLego org with permissions: actions:write, contents:read, metadata:read (and nothing else)
    - [ ] Private key (.pem) generated and downloaded
    - [ ] App installed on MyAlterLego org with access to admin-managed repos
    - [ ] GITHUB_APP_ID, GITHUB_APP_PRIVATE_KEY, GITHUB_APP_INSTALLATION_ID pushed via firebase apphosting:secrets:set
    - [ ] Local .pem file deleted after secrets pushed
    - [ ] Schema migration 0009_promotion_dispatch_audit.sql applied to production CRDB
    - [ ] App Hosting redeploy succeeded; no "missing required env vars" errors in logs
    - [ ] (Once Plan 04-04 deployed) End-to-end smoke test passed
    - [ ] Database row for the test release has promotion_dispatched_at and promotion_dispatched_by populated

    Once every box is ticked, ENV-G01 + GATE-11a are satisfied.
  </how-to-verify>
  <action>
    Mike opens `.planning/phases/04-github-app-promotion/04-HUMAN-UAT.md` and follows steps 1-8. This task is a human checkpoint - the work is described in `<how-to-verify>`. Do not auto-resolve. Steps 1-7 can run in parallel with Plan 04-04's code work; step 8 (end-to-end smoke test) requires Plan 04-04 deployed.
  </action>
  <verify>
    Manual - Mike confirms every box on the verification checklist is ticked. Resume signal "approved" closes this task.
  </verify>
  <acceptance_criteria>
    - Mike replies "approved" after working through 04-HUMAN-UAT.md
    - All checkboxes in the runbook's "Verification checklist" section are ticked
    - End-to-end smoke test in Step 8 produced an actual deploy-prod.yml run on a real repo (link captured in the SUMMARY)
  </acceptance_criteria>
  <done>ENV-G01 + GATE-11a satisfied: GitHub App live in MyAlterLego with correct permissions, secrets pushed, migration applied, end-to-end smoke test green with a real workflow run.</done>
  <resume-signal>Type "approved" when the verification checklist is fully ticked, or describe what blocks you (App permissions, PEM paste issues, installation ID lookup, etc.) and we'll triage.</resume-signal>
</task>

</tasks>

<verification>
- apphosting.yaml has three new entries with correct YAML structure (3 secrets, RUNTIME-only)
- 04-HUMAN-UAT.md exists with all 8 steps, verification checklist, rotation, troubleshooting
- This plan is `autonomous: false` because the final task is a human verification checkpoint
- The human checkpoint is the formal gate for ENV-G01 + GATE-11a - code is implementable now, runtime depends on the runbook
</verification>

<success_criteria>
- ENV-G01 satisfiable: code references the secrets at the names declared in apphosting.yaml; the runbook walks Mike through populating them
- GATE-11a satisfiable: the runbook documents the exact permission scopes (actions:write, contents:read, metadata:read) - and only those - so the App is created with minimal blast radius
- After Mike runs the runbook, the full Phase 4 flow works once 04-04 is deployed: customer approve -> Slack click -> dispatchWorkflow -> real deploy-prod.yml run
- Rotation policy is documented; troubleshooting hints exist for the four most likely failure modes
- The human checkpoint is the gate, not a blocker - Mike can run plans 04-01/02/04 first (Wave 1 + Wave 2), ship the code, then run the runbook
</success_criteria>

<output>
After completion, create `.planning/phases/04-github-app-promotion/04-03-SUMMARY.md`
</output>
