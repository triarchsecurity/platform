---
phase: 05-round-trip-+-shared-workflows+pilot
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md
autonomous: false
requirements: [WORKFLOW-01, WORKFLOW-02, PILOT-01]
must_haves:
  truths:
    - "A single master HUMAN-UAT exists at .planning/phases/05-.../05-HUMAN-UAT.md that consolidates ALL deferred human steps for the v1.14 milestone closeout"
    - "Phase 2 deferred items (DB push of migration 0008 — reason column) are listed with the exact command(s) to run"
    - "Phase 3 deferred items (Slack App creation + 3 secrets) are referenced via link to 03-HUMAN-UAT.md and called out in the master checklist"
    - "Phase 4 deferred items (GitHub App creation + 3 secrets + DB push of migration 0009) are referenced via link to 04-HUMAN-UAT.md and called out in the master checklist"
    - "Phase 5 cross-repo work — shared-workflows ci-cd.yml + deploy-prod.yml YAML changes (WORKFLOW-01, WORKFLOW-02) — appears with copy-paste-ready YAML snippets"
    - "Phase 5 pilot work — Truth+Treason shared-workflows ref bump + full UI→Slack→GitHub App→round-trip E2E smoke test (PILOT-01) — appears as a sequenced runbook"
    - "The master checklist has a single human checkpoint at the end where Mike confirms v1.14.0 milestone closeout"
  artifacts:
    - path: ".planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md"
      provides: "Master v1.14.0 milestone closeout checklist"
      min_lines: 200
      contains: "shared-workflows"
  key_links:
    - from: "05-HUMAN-UAT.md Phase 5 cross-repo section"
      to: "MyAlterLego/shared-workflows ci-cd.yml + deploy-prod.yml"
      via: "Copy-paste-ready YAML snippets"
      pattern: "ci-cd\\.yml.*POST|deploy-prod\\.yml.*POST"
    - from: "05-HUMAN-UAT.md Phase 5 pilot section"
      to: "MyAlterLego/triarchsecurity-portal (Truth+Treason)"
      via: "shared-workflows ref bump in T+T's workflow files"
      pattern: "Truth\\+Treason|triarchsecurity-portal"
---

<objective>
Author the v1.14.0 milestone master closeout checklist (05-HUMAN-UAT.md). This is the single document Mike works through to ship v1.14 — covering ALL deferred manual steps from earlier phases plus all Phase 5 cross-repo + pilot work.

Purpose: CONTEXT.md decision Area 4 locked the structure: 4 phases of deferred work + Phase 5's cross-repo YAML + Truth+Treason pilot E2E, all in one master document. WORKFLOW-01, WORKFLOW-02, and PILOT-01 are all cross-repo / human-side requirements that cannot be automated from the admin session — this is where they get their concrete instructions.

Output: One master HUMAN-UAT markdown file with sequenced sections, copy-paste-ready YAML, exact commands, exact verification queries, and a final checkpoint where Mike confirms milestone closeout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-round-trip-+-shared-workflows+pilot/05-CONTEXT.md
@.planning/phases/04-github-app-promotion/04-HUMAN-UAT.md
@.planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md
@src/db/schema.ts
@src/app/api/platform/ingest/release-logs/route.ts

<interfaces>
<!-- Reference content from earlier phases — link, do not duplicate. -->

Phase 2 deferred (per STATE.md):
- "[Phase 02-01]: DB push deferred to human — DATABASE_URL is Firebase secret, not in local shell; same pattern as Phase 01-01"
- Migration: src/db/migrations/0008_release_approvals_reason.sql (adds release_approvals.reason column for REJECT-01)

Phase 3 deferred (per 03-HUMAN-UAT.md):
- Slack App creation in workspace
- 3 secrets: SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, SLACK_PAYLOAD_SECRET pushed via firebase apphosting:secrets:set
- SLACK_USER_MAP populated with at least mike@triarchsecurity.com → Slack user ID

Phase 4 deferred (per 04-HUMAN-UAT.md):
- GitHub App creation in MyAlterLego org with permissions actions:write, contents:read, metadata:read
- 3 secrets: GITHUB_APP_ID, GITHUB_APP_PRIVATE_KEY, GITHUB_APP_INSTALLATION_ID
- DB push of migration 0009 (promotion_dispatched_at + promotion_dispatched_by)

Phase 5 cross-repo (this plan owns the documentation):

WORKFLOW-01 — shared-workflows ci-cd.yml POST step (after dev deploy completes):
```yaml
- name: Notify admin of dev deploy
  if: success() && inputs.notify-admin
  run: |
    curl -fsSL -X POST "${{ inputs.admin-api-url }}/api/platform/ingest/release-logs" \
      -H "Authorization: Bearer ${{ secrets.ADMIN_API_TOKEN }}" \
      -H "Content-Type: application/json" \
      -d "$(jq -n \
        --arg version "${{ inputs.version }}" \
        --arg releaseType patch \
        --arg env dev \
        --arg commitSha "${{ github.sha }}" \
        --arg deployedAt "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
        --arg deployedBy "${{ github.actor }}@users.noreply.github.com" \
        '{version: $version, releaseType: $releaseType, env: $env, commitSha: $commitSha, deployedAt: $deployedAt, releasedBy: $deployedBy}')"
```

WORKFLOW-02 — shared-workflows deploy-prod.yml POST step (after prod deploy completes):
```yaml
- name: Notify admin of prod promotion
  if: success() && inputs.notify-admin
  run: |
    curl -fsSL -X POST "${{ inputs.admin-api-url }}/api/releases/promoted" \
      -H "Authorization: Bearer ${{ secrets.ADMIN_API_TOKEN }}" \
      -H "Content-Type: application/json" \
      -d "$(jq -n \
        --arg version "${{ inputs.tag }}" \
        --arg commit_sha "${{ github.sha }}" \
        --arg deployed_at "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
        --arg deployed_by "${{ github.actor }}@users.noreply.github.com" \
        '{version: $version, commit_sha: $commit_sha, deployed_at: $deployed_at, deployed_by: $deployed_by}')"
```

Notes for the executor:
- The dev ingest endpoint accepts both `commitSha` and `deployedAt` (camelCase) per existing route.ts line 21–24
- The prod ingest endpoint (Plan 05-01) accepts `commit_sha`, `deployed_at`, `deployed_by` (snake_case) — match the wire format
- `inputs.notify-admin` and `inputs.admin-api-url` and `secrets.ADMIN_API_TOKEN` are NEW workflow inputs/secrets that need adding to the workflow_call signature
- Before adding the POST steps, the workflow file needs new `inputs` declarations — the snippet above assumes those are present
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 05-HUMAN-UAT.md master closeout checklist</name>
  <files>.planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-round-trip-+-shared-workflows+pilot/05-CONTEXT.md (Area 3 + Area 4 — what goes in this UAT)
    - .planning/phases/04-github-app-promotion/04-HUMAN-UAT.md (formatting reference — the gold standard for human runbooks; this UAT mirrors that style)
    - .planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md (Slack-specific patterns to reference)
    - .planning/STATE.md (Phase 2 DB push deferral note — quote it for context)
    - src/app/api/platform/ingest/release-logs/route.ts (so the WORKFLOW-01 YAML snippet uses the right field names)
    - .planning/phases/05-round-trip-+-shared-workflows+pilot/05-01-PLAN.md (so the WORKFLOW-02 YAML snippet matches the route's actual snake_case wire format)
  </read_first>
  <action>
    Author `.planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` with the following structure. Aim for ~250–350 lines — this is the single document for v1.14.0 closeout, so it earns its weight.

    ```markdown
    # v1.14.0 Master HUMAN-UAT — Customer Release Gating Milestone Closeout

    Single source of truth for shipping v1.14.0. Consolidates all deferred manual steps from Phases 2, 3, 4, and 5 plus the cross-repo work (shared-workflows + Truth+Treason pilot) and the final E2E smoke test.

    Work through the sections in order. Each section ends with a checklist. The final section is the milestone-closeout signoff.

    ## Prerequisites

    - All Phase 1–5 admin-repo plans merged to main
    - Firebase CLI logged in (`firebase login` against the triarch-dev-admin App Hosting backend)
    - Org admin permissions on github.com/MyAlterLego
    - Production CRDB connection string available (in Firebase secrets — not in local shell)
    - Slack workspace admin (or App-management permissions) on the Triarch workspace

    ---

    ## Section A — Phase 2 deferred: DB push of migration 0008 (reason column)

    Phase 2 plan 02-01 emitted `src/db/migrations/0008_release_approvals_reason.sql` adding the `reason` column to `release_approvals` (REJECT-01). DB push was deferred per the established pattern (DATABASE_URL is a Firebase secret, not in local shell).

    Apply the migration:

    [...exact commands mirroring 04-HUMAN-UAT.md Step 6 — drizzle-kit push OR cockroach sql with the migration file...]

    Verify:

    ```sql
    SELECT column_name FROM information_schema.columns
    WHERE table_name = 'release_approvals' AND column_name = 'reason';
    ```

    Should return one row.

    Section A checklist:
    - [ ] Migration 0008 applied to production CRDB
    - [ ] `release_approvals.reason` column verified present
    - [ ] No app errors in Cloud Run logs after the push

    ---

    ## Section B — Phase 3 deferred: Slack App + 3 secrets

    Phase 3 ships secure code that depends on a real Slack App in the Triarch workspace. The full runbook lives at [.planning/phases/03-slack-interactive-approval/03-HUMAN-UAT.md](../03-slack-interactive-approval/03-HUMAN-UAT.md).

    Quick checklist (run the full runbook for details):
    - [ ] Slack App created with `chat:write` scope
    - [ ] Bot token, signing secret, payload secret pushed via `firebase apphosting:secrets:set`
    - [ ] App invited to `#release-approvals` channel
    - [ ] SLACK_USER_MAP populated with at least mike@triarchsecurity.com → Slack user ID

    [Refer to 03-HUMAN-UAT.md for: exact dashboard navigation, OAuth scope screenshots, secret-push commands, troubleshooting]

    ---

    ## Section C — Phase 4 deferred: GitHub App + 3 secrets + migration 0009

    Phase 4 ships secure code that depends on a GitHub App installed in MyAlterLego org. The full runbook lives at [.planning/phases/04-github-app-promotion/04-HUMAN-UAT.md](../04-github-app-promotion/04-HUMAN-UAT.md).

    Quick checklist (run the full runbook for details):
    - [ ] GitHub App created with permissions: actions:write, contents:read, metadata:read
    - [ ] Private key generated, pasted to firebase secret, local .pem deleted
    - [ ] App installed on MyAlterLego org with access to admin-managed repos
    - [ ] GITHUB_APP_ID, GITHUB_APP_PRIVATE_KEY, GITHUB_APP_INSTALLATION_ID pushed via `firebase apphosting:secrets:set`
    - [ ] Migration 0009 (promotion_dispatched_at + promotion_dispatched_by) applied to production CRDB

    [Refer to 04-HUMAN-UAT.md for: exact dashboard navigation, permission-screen verification, end-to-end smoke test instructions]

    ---

    ## Section D — Phase 5 cross-repo: shared-workflows updates (WORKFLOW-01 + WORKFLOW-02)

    The admin repo has the new `/api/releases/promoted` endpoint (Plan 05-01) and the existing `/api/platform/ingest/release-logs` endpoint accepts `env` + `commitSha` + `deployedAt`. The `MyAlterLego/shared-workflows` repo needs two new POST steps so every CI/CD run round-trips back to admin.

    These edits are NOT made from the admin session. Open the shared-workflows repo separately (likely a `/gsd:autonomous` session targeting that repo, or hand-edit + push).

    ### D.1 — Add new workflow inputs

    In each shared workflow that needs to notify admin (`ci-cd.yml`, `deploy-prod.yml`), add three new inputs to the `workflow_call` signature:

    ```yaml
    on:
      workflow_call:
        inputs:
          # ... existing inputs ...
          notify-admin:
            description: 'POST deploy completion to admin.triarch.dev'
            type: boolean
            default: true
          admin-api-url:
            description: 'Admin API base URL'
            type: string
            default: 'https://admin.triarch.dev'
        secrets:
          # ... existing secrets ...
          ADMIN_API_TOKEN:
            description: 'Per-project admin API key (project_members.api_key)'
            required: true
    ```

    ### D.2 — WORKFLOW-01: ci-cd.yml dev-deploy notify step

    Append to the deploy job (after the existing deploy step completes successfully):

    ```yaml
    [paste the WORKFLOW-01 YAML snippet from the interfaces block — full curl command with jq payload construction]
    ```

    Notes:
    - Uses `inputs.version` — should already be defined upstream (the version being deployed)
    - The `releasedBy` field is set to the GitHub Actions actor's noreply email — adequate for the audit trail; the customer page shows this string verbatim

    ### D.3 — WORKFLOW-02: deploy-prod.yml prod-deploy notify step

    Append to the deploy job:

    ```yaml
    [paste the WORKFLOW-02 YAML snippet from the interfaces block — note the snake_case wire format matching Plan 05-01]
    ```

    Notes:
    - Uses `inputs.tag` — that's the input deploy-prod.yml already accepts (Phase 4 GATE-10 dispatches with `tag` set to release version)
    - The endpoint is idempotent — replays of this step return 200 with the existing prod row, no duplicate inserts (per Plan 05-01)

    ### D.4 — Tag a new shared-workflows release

    After both files merge to main:

    ```bash
    cd /path/to/shared-workflows
    git tag v0.4.0  # or whatever the next version is — check existing tags
    git push origin v0.4.0
    ```

    Section D checklist:
    - [ ] shared-workflows repo's ci-cd.yml has the `notify-admin` POST step
    - [ ] shared-workflows repo's deploy-prod.yml has the `notify-admin` POST step
    - [ ] `notify-admin`, `admin-api-url`, and `ADMIN_API_TOKEN` secret added to both workflows' signatures
    - [ ] New shared-workflows tag pushed (note the version: __________)

    ---

    ## Section E — Phase 5 pilot: Truth+Treason consumes new shared-workflows (PILOT-01)

    Truth+Treason (`MyAlterLego/triarchsecurity-portal`) is the pilot project for v1.14.

    ### E.1 — Bump shared-workflows ref in T+T

    In the triarchsecurity-portal repo:

    ```yaml
    # .github/workflows/ci-cd.yml — bump the ref on the `uses:` line
    jobs:
      build:
        uses: MyAlterLego/shared-workflows/.github/workflows/ci-cd.yml@v0.4.0  # ← new tag from D.4
        secrets:
          ADMIN_API_TOKEN: ${{ secrets.ADMIN_API_TOKEN }}
          # ... existing secrets ...
    ```

    Same edit in `deploy-prod.yml`.

    ### E.2 — Set ADMIN_API_TOKEN secret in T+T's repo

    Look up T+T's project apiKey from the admin DB:

    ```sql
    SELECT key, api_key FROM projects WHERE key = 'triarchsecurity-portal';
    ```

    Set as a GitHub Actions secret on the T+T repo:
    - GitHub → triarchsecurity-portal → Settings → Secrets and variables → Actions → New repository secret
    - Name: `ADMIN_API_TOKEN`
    - Value: (the api_key from the SQL above)

    ### E.3 — Smoke test the dev path

    1. Push a small commit to T+T's main branch (or whatever branch deploys to dev — typically a no-op version bump per workspace CLAUDE.md rules)
    2. Watch the GitHub Actions run for ci-cd.yml
    3. Verify admin DB has a fresh release_logs row:

    ```sql
    SELECT id, version, env, status, commit_sha, deployed_at, released_by
      FROM release_logs
      WHERE project = 'triarchsecurity-portal'
      ORDER BY created_at DESC LIMIT 3;
    ```

    Should show env='dev', status='dev', commit_sha + deployed_at populated.

    Section E checklist:
    - [ ] T+T's ci-cd.yml + deploy-prod.yml refs bumped to the new shared-workflows tag
    - [ ] `ADMIN_API_TOKEN` secret set on T+T's GitHub repo
    - [ ] Dev deploy creates a release_logs row in admin (env='dev', status='dev')

    ---

    ## Section F — Full E2E smoke test (PILOT-01 success criterion)

    The chain: customer admin clicks Approve on /projects/{slug}/releases → Slack message in #release-approvals → staff clicks Promote → GitHub App dispatches deploy-prod.yml → workflow completes → POSTs to /api/releases/promoted → admin DB has paired prod row → Timeline reflects all 5 events.

    Run through the test as Mike (or the staff Slack user mapped in SLACK_USER_MAP):

    1. Sign in to https://admin.triarch.dev as a customer admin email seeded in T+T's project_members (e.g. a real Truth+Treason customer admin, OR seed a test email and use it)
    2. Navigate to /projects/triarchsecurity-portal/releases
    3. Find the release from Section E.3 — status should be 'dev'
    4. Expand the row — Timeline shows "Deployed to dev" with the actor and timestamp
    5. Click "Approve for Production" — confirmation pops with 5-second countdown — click "Confirm approval"
    6. Within seconds, a Slack message appears in `#release-approvals` with version, project, approver, feedback excerpt (if any), and Approve & Promote / Reject buttons
    7. Sign out as the customer; have mike@triarchsecurity.com (mapped staff in SLACK_USER_MAP) click "Approve and Promote" in Slack
    8. Slack message updates to ":white_check_mark: Promoted to production by @mike (mike@triarchsecurity.com)"
    9. Within ~10 seconds, a threaded reply: ":rocket: Workflow dispatched: deploy-prod.yml run #<N>"
    10. Visit https://github.com/MyAlterLego/triarchsecurity-portal/actions — confirm a fresh deploy-prod.yml run started with `tag` input matching the release version
    11. Wait for deploy-prod.yml to complete (typically 2–5 minutes for FAH)
    12. After completion, verify in the admin DB:

    ```sql
    SELECT id, project, version, env, status, deployed_at, released_by, promotion_dispatched_at, promotion_dispatched_by
      FROM release_logs
      WHERE project = 'triarchsecurity-portal' AND version = '<test version>'
      ORDER BY env, created_at DESC;
    ```

    Should return TWO rows:
    - dev row: env='dev', status='promoted', promotion_dispatched_at + promotion_dispatched_by populated
    - prod row: env='prod', status='promoted', deployed_at = the deploy-prod.yml completion timestamp, released_by = the GitHub Actions actor

    13. Sign back into admin.triarch.dev as the customer admin, navigate to /projects/triarchsecurity-portal/releases, expand the same release row. Timeline should now show ALL 5 events:
        - GitCommit Deployed to dev
        - MessageSquare Feedback (any feedback posted before approval)
        - ShieldCheck Approved for production
        - Rocket Promotion dispatched
        - Server Deployed to production

    14. Idempotency sanity (optional but recommended): manually re-trigger the deploy-prod.yml run in GitHub Actions UI. Verify admin DB still has exactly TWO rows for that version (no third row inserted).

    Section F checklist:
    - [ ] Customer click → Slack message round-trip ≤5 seconds
    - [ ] Slack click → GitHub Actions run dispatched ≤10 seconds
    - [ ] deploy-prod.yml completes successfully
    - [ ] Both release_logs rows present (dev with status='promoted', prod with status='promoted')
    - [ ] Customer page Timeline shows all 5 lifecycle events with correct actors + timestamps
    - [ ] Idempotent re-run does not create a third row

    ---

    ## Section G — Milestone closeout signoff

    All sections above complete? v1.14.0 ships.

    Final checklist:
    - [ ] Section A complete (Phase 2 DB push)
    - [ ] Section B complete (Phase 3 Slack App)
    - [ ] Section C complete (Phase 4 GitHub App + DB push)
    - [ ] Section D complete (shared-workflows POST steps + new tag)
    - [ ] Section E complete (T+T ref bump + ADMIN_API_TOKEN + dev round-trip)
    - [ ] Section F complete (full E2E smoke test passes)

    When all six are checked: bump `package.json` to `1.14.0`, commit, push to main, monitor the FAH deploy, then update STATE.md to mark milestone v1.14.0 complete.

    ## Troubleshooting

    [...consolidated troubleshooting list referencing the per-phase HUMAN-UAT troubleshooting sections, plus Phase 5 specific failure modes:
    - "ADMIN_API_TOKEN 401 from CI" — re-check the secret matches projects.api_key for that project
    - "/api/releases/promoted 404 — no dev row found" — version mismatch between ci-cd.yml's POST and deploy-prod.yml's POST; verify both use the same version string source
    - "Timeline missing promoted event" — check release_logs.promotion_dispatched_at is populated; if NULL, the Slack→GitHub-App handler in /api/slack/interact didn't audit (Phase 4 plan 04-04 issue)
    - "Timeline missing deployed-prod event" — check pairedProd row exists in release_logs (env='prod'); if missing, deploy-prod.yml's notify step didn't fire or hit a 404/500
    - "Slack signing-secret mismatch" — see 03-HUMAN-UAT.md troubleshooting
    - "GitHub App installation token 401" — see 04-HUMAN-UAT.md troubleshooting
    ...]
    ```

    Write the actual full document text — bracketed placeholders are guides for the executor to expand. Use exact commands (no "..."), exact SQL, exact YAML.
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin && test -f .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md && wc -l .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md && grep -c "^## Section" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` exists with ≥200 lines
    - `grep -c "^## Section [A-G]" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns 7 (Sections A–G all present)
    - `grep -c "shared-workflows" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns ≥3 (Section D references shared-workflows multiple times)
    - `grep -c "ci-cd\\.yml" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns ≥2 (WORKFLOW-01 covered)
    - `grep -c "deploy-prod\\.yml" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns ≥2 (WORKFLOW-02 covered)
    - `grep -c "/api/releases/promoted" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns ≥1
    - `grep -c "/api/platform/ingest/release-logs" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns ≥1
    - `grep -c "Truth+Treason\\|triarchsecurity-portal" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns ≥2 (PILOT-01 covered)
    - `grep -c "03-HUMAN-UAT\\|04-HUMAN-UAT" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns ≥2 (links to phase-specific UATs, not duplicating their content)
    - `grep -c "0008\\|0009" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns ≥2 (both deferred migrations called out)
    - `grep -c "ADMIN_API_TOKEN" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns ≥3 (new secret introduced + referenced in D + E + Troubleshooting)
    - `grep -c "Timeline" .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md` returns ≥2 (Section F E2E references the timeline subsection from Plan 05-02)
  </acceptance_criteria>
  <done>05-HUMAN-UAT.md is a complete, sequenced runbook for v1.14.0 closeout — Mike can work through it without consulting any other doc except the per-phase UATs that are explicitly linked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: v1.14.0 milestone closeout — work through 05-HUMAN-UAT.md</name>
  <files>(none — checkpoint task)</files>
  <what-built>
    Plan 05-01 shipped POST /api/releases/promoted (admin endpoint, idempotent + atomic). Plan 05-02 shipped the Timeline subsection inside /projects/{slug}/releases. Plan 05-03 shipped the onboarding runbook. Plan 05-04 task 1 just shipped the master closeout checklist.

    Now Mike works through 05-HUMAN-UAT.md from Section A through Section G. This includes:
    - Section A: DB push of Phase 2 migration 0008 (release_approvals.reason)
    - Section B: Phase 3 Slack App + 3 secrets (refer to 03-HUMAN-UAT.md)
    - Section C: Phase 4 GitHub App + 3 secrets + migration 0009 (refer to 04-HUMAN-UAT.md)
    - Section D: shared-workflows ci-cd.yml + deploy-prod.yml POST steps (WORKFLOW-01 + WORKFLOW-02) — cross-repo work
    - Section E: Truth+Treason ref bump + ADMIN_API_TOKEN secret + dev round-trip verification (PILOT-01 prep)
    - Section F: Full UI → Slack → GitHub App → round-trip E2E smoke test on Truth+Treason (PILOT-01)
    - Section G: Final signoff + version bump to 1.14.0
  </what-built>
  <action>
    Human checkpoint — work through 05-HUMAN-UAT.md from Section A through Section G as documented in <how-to-verify> below. This task is not autonomous: Claude does not perform Sections A–G. Mike (or the operator) executes them.
  </action>
  <verify>
    <automated>cat .planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md | grep -c "^- \[x\]"</automated>
    Manual: confirm all section checklists (A through F) in 05-HUMAN-UAT.md are checked and admin's package.json is at 1.14.0 on main.
  </verify>
  <how-to-verify>
    1. Open `.planning/phases/05-round-trip-+-shared-workflows+pilot/05-HUMAN-UAT.md`
    2. Work through Sections A → G in order. Check each box as you complete it.
    3. Section A — apply migration 0008 to production CRDB; verify the reason column exists.
    4. Sections B + C — confirm the Slack App and GitHub App are still healthy from Phases 3 + 4 (they should be — these were completed earlier in the milestone).
    5. Section D — switch to the `MyAlterLego/shared-workflows` repo (in a separate session — likely `/gsd:autonomous` targeting that repo, or hand-edit). Add the two POST steps + the new inputs/secret. Tag a new release.
    6. Section E — switch to `MyAlterLego/triarchsecurity-portal` (Truth+Treason). Bump the shared-workflows ref. Set the ADMIN_API_TOKEN GitHub secret using T+T's project apiKey from the admin DB. Push a no-op commit to trigger ci-cd.yml. Verify a release_logs row appears in admin.
    7. Section F — full E2E smoke test. Sign in to admin.triarch.dev as a T+T customer admin, click Approve on the dev release. Confirm Slack message arrives. Click Promote in Slack as mike@triarchsecurity.com. Confirm deploy-prod.yml dispatches and completes. Confirm the paired prod row appears in admin DB. Confirm /projects/triarchsecurity-portal/releases Timeline shows all 5 lifecycle events.
    8. Section G — bump admin's `package.json` to `1.14.0`, commit, push, watch the FAH deploy, then update `.planning/STATE.md` to mark milestone complete.

    If any section fails: troubleshoot using the Troubleshooting section in 05-HUMAN-UAT.md. If the failure is a code issue (not a config issue), file a gap and run `/gsd:plan-phase --gaps`.
  </how-to-verify>
  <resume-signal>
    Type "approved" once all six section checklists in 05-HUMAN-UAT.md are checked AND admin's package.json is bumped to 1.14.0 and pushed to main. If any section is incomplete or any step failed, describe what blocked you so we can plan a gap-closure phase.
  </resume-signal>
  <acceptance_criteria>
    - All six section checklists in 05-HUMAN-UAT.md (A–F) are checked
    - Section G signoff complete: admin/package.json on main is at version 1.14.0
    - STATE.md milestone status updated to complete
    - Mike has explicitly typed "approved" or equivalent in response to this checkpoint
  </acceptance_criteria>
  <done>v1.14.0 milestone shipped end-to-end. Truth+Treason exercises the full release-gating workflow. The loop is closed.</done>
</task>

</tasks>

<verification>
- 05-HUMAN-UAT.md exists at the phase path with all required sections (A–G)
- All grep checks in Task 1 acceptance criteria pass
- Mike has worked through the document and signed off on the human checkpoint
</verification>

<success_criteria>
- WORKFLOW-01 covered: shared-workflows ci-cd.yml POST step documented with copy-paste-ready YAML in Section D
- WORKFLOW-02 covered: shared-workflows deploy-prod.yml POST step documented with copy-paste-ready YAML in Section D
- PILOT-01 covered: Truth+Treason E2E smoke test sequenced in Sections E + F
- All deferred work from Phases 2, 3, and 4 consolidated into Sections A, B, C
- Final milestone signoff (Section G) is the single bar v1.14.0 must clear to ship
</success_criteria>

<output>
After completion, create `.planning/phases/05-round-trip-+-shared-workflows+pilot/05-04-SUMMARY.md` capturing:
- Final line count of 05-HUMAN-UAT.md
- shared-workflows tag used (recorded after Section D completes)
- Truth+Treason version that exercised the full E2E (recorded after Section F completes)
- Any post-pilot gaps surfaced during the smoke test (file as backlog items if not blocking)
</output>
