---
phase: 01.1-membership-enforcement-audit
plan: "05"
subsystem: api
tags: [auth, access-control, membership, project-detail, 404-no-leak]

# Dependency graph
requires:
  - phase: 01.1-01
    provides: "requireSignedIn, requireStaff, requireMembership helpers in api-auth.ts"
  - phase: 01.1-02
    provides: "getCurrentUserContext, UserContext — project membership context lookup"
provides:
  - "All 4 project-detail endpoints enforce membership: fetch-then-membership-check, 404 to non-members"
  - "reports/[id] DELETE restricted to staff-only (per CLASSIFICATION.md special case)"
affects:
  - "01.1-06 — page-level audit and UAT; detail API gates are now in place"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "fetch-then-membership: fetch row by id first, then call getCurrentUserContext, then check ctx.memberships.some(m => m.project_key === row.project) — returns 404 (not 403) to non-members"
    - "no-leak 404: non-member gets same 404 as if the id was bogus — no signal that the row exists"
    - "reuse existing select: bug-reports and feature-requests PATCH already fetched current row for transition logging; membership check added inline, no second DB call"

key-files:
  created: []
  modified:
    - "src/app/api/platform/release-logs/[id]/route.ts"
    - "src/app/api/platform/bug-reports/[id]/route.ts"
    - "src/app/api/platform/feature-requests/[id]/route.ts"
    - "src/app/api/platform/reports/[id]/route.ts"

key-decisions:
  - "reports/[id] DELETE uses requireStaff only — non-staff project members cannot delete reports (CLASSIFICATION.md special case; GET and PUT use project-detail membership pattern)"
  - "feature-requests/[id] has no GET handler (PATCH only per actual file) — plan mentioned GET/PUT but classification table row 5 clarifies PATCH only; no GET was added"
  - "bug-reports PATCH reuses the single existing current fetch for both membership check and workflowTransitions logging — no duplicate DB query"

patterns-established:
  - "Project-detail pattern: requireSignedIn → fetch row → membership check → 404 to non-members → proceed"
  - "Per-method auth in same route file: GET/PUT use requireSignedIn+membership; DELETE uses requireStaff"

requirements-completed:
  - MEMBER-AUDIT-07

# Metrics
duration: 6min
completed: 2026-05-03
---

# Phase 01.1 Plan 05: Project-Detail Endpoint Membership Enforcement Summary

**Fetch-then-membership-check pattern applied to 4 project-detail endpoints returning 404 (not 403) to non-members, with reports DELETE gated to staff-only.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-05-03T23:03:55Z
- **Completed:** 2026-05-03T23:09:47Z
- **Tasks:** 3 (2 code tasks + 1 build gate)
- **Files modified:** 4

## Accomplishments

- All 4 project-detail endpoints (`release-logs/[id]`, `bug-reports/[id]`, `feature-requests/[id]`, `reports/[id]`) now enforce membership via fetch-then-check pattern
- Non-staff non-members receive 404 regardless of whether the row exists — no information leak
- `bug-reports/[id]` PATCH reuses the existing `current` fetch for membership gate (no extra DB round-trip), and the workflowTransitions insert is unchanged
- `reports/[id]` DELETE is `requireStaff` only — per CLASSIFICATION.md special case, non-staff members should read/comment on reports but not delete them
- Build clean: `npx tsc --noEmit` and `npx next build` both exit 0 (47 routes, 0 errors)

## Task Commits

1. **Task 1: Migrate release-logs, bug-reports, feature-requests detail endpoints** - `34f1535` (feat)
2. **Task 2: Migrate reports/[id] — project-detail with staff-only DELETE** - `b148a47` (feat)
3. **Task 3: Build green and final requireAdmin sweep** - (no file changes, gate task — verified inline)

## Files Created/Modified

- `src/app/api/platform/release-logs/[id]/route.ts` — GET + PUT: requireSignedIn, fetch-then-membership, 404 to non-members; PUT fetches current before mutation
- `src/app/api/platform/bug-reports/[id]/route.ts` — GET: requireSignedIn + membership gate; PATCH: reuses existing current fetch for membership check + workflowTransitions unchanged
- `src/app/api/platform/feature-requests/[id]/route.ts` — PATCH only (no GET): requireSignedIn, reuses existing current fetch for membership gate + workflowTransitions unchanged
- `src/app/api/platform/reports/[id]/route.ts` — GET + PUT: requireSignedIn + membership (fetch-then-check); DELETE: requireStaff only

## reports/[id] Classification

`reports/[id]` is classified **project-detail** (row 28 in CLASSIFICATION.md). GET and PUT use the full fetch-then-membership pattern with 404 to non-members. DELETE is **staff-only** per the special case in CLASSIFICATION.md: "non-staff customers should not be able to DELETE reports; Plan 05 should apply requireStaff on DELETE."

## feature-requests/[id] Note

CLASSIFICATION.md row 5 lists only PATCH for `feature-requests/[id]/route.ts` (no GET handler in the file). The plan objective mentioned "GET and PUT" but the actual file only exports PATCH. No GET was added — the file was migrated as-is with the membership gate added to the existing PATCH handler.

## Final requireAdmin Sweep

After this plan, none of this plan's 4 files contain `requireAdmin`:

```
grep -rln "requireAdmin\b" src/app/api/platform/
# Result: src/app/api/platform/service-offerings/[id]/route.ts (Plan 03 scope)
```

The `requireAdmin` deprecated alias remains in `src/lib/api-auth.ts` (Plan 01's deliverable) for any remaining in-flight callsites. The `service-offerings/[id]/route.ts` file is Plan 03's responsibility.

## Decisions Made

- **reports/[id] DELETE = requireStaff**: Per CLASSIFICATION.md special case. Non-staff project members can read and edit reports but must not be able to delete them. Only GET and PUT use the project-detail membership pattern.
- **No duplicate select in bug-reports PATCH**: The existing `[current]` fetch (originally for workflowTransitions transition logging) is reused for the membership gate. No second `db.select()` added.
- **feature-requests/[id] PATCH only**: No GET handler was added. The file only exports PATCH per the actual codebase; membership gate was added to the existing handler.

## Deviations from Plan

None — plan executed exactly as written. All structural transforms applied as specified. The feature-requests/[id] note (PATCH-only, no GET) is a pre-existing file shape, not a deviation.

## Known Stubs

None. This plan is pure access-control logic with no UI rendering or data sources.

## Issues Encountered

None.

## Next Phase Readiness

- All project-detail API endpoints are membership-gated
- Together with Plans 03 (staff-only) and 04 (project-list), the full 32-endpoint audit surface is covered
- Plan 06 picks up page-level audit verification and UAT update

---
*Phase: 01.1-membership-enforcement-audit*
*Completed: 2026-05-03*
