---
phase: 01.1-membership-enforcement-audit
plan: 03
type: execute
wave: 2
depends_on:
  - 01.1-01
  - 01.1-02
files_modified:
  - src/app/api/platform/projects/[id]/route.ts
  - src/app/api/platform/projects/[id]/destroy/route.ts
  - src/app/api/platform/projects/scaffold-repo/route.ts
  - src/app/api/platform/projects/provision-db/route.ts
  - src/app/api/platform/projects/provision-dns/route.ts
  - src/app/api/platform/projects/sync-state/route.ts
  - src/app/api/platform/projects/tools/route.ts
  - src/app/api/platform/projects/tools/scaffold/route.ts
  - src/app/api/platform/navigation/sections/route.ts
  - src/app/api/platform/navigation/sections/[id]/route.ts
  - src/app/api/platform/navigation/pages/route.ts
  - src/app/api/platform/navigation/pages/[id]/route.ts
  - src/app/api/platform/navigation/subpages/route.ts
  - src/app/api/platform/navigation/subpages/[id]/route.ts
  - src/app/api/platform/navigation/reorder/route.ts
  - src/app/api/platform/navigation/admin/route.ts
  - src/app/api/platform/settings/route.ts
  - src/app/api/platform/access-logs/route.ts
  - src/app/api/platform/release-logs/backfill/route.ts
  - src/app/api/platform/webhooks/backfill/route.ts
  - src/app/api/platform/report-section-types/route.ts
  - src/app/api/platform/service-offerings/route.ts
  - src/app/api/platform/service-offerings/[id]/route.ts
autonomous: true
requirements:
  - MEMBER-AUDIT-05

must_haves:
  truths:
    - "Every endpoint classified `staff-only` in CLASSIFICATION.md uses `requireStaff()` instead of `requireAdmin()`"
    - "Non-staff signed-in users hitting any of these endpoints get HTTP 403"
    - "Triarch staff (mike@triarchsecurity.com) experience is unchanged — all of these endpoints still work for staff"
    - "No staff-only endpoint still imports `requireAdmin` from @/lib/api-auth"
    - "Build is green: npx tsc --noEmit and npx next build both succeed"
  artifacts:
    - path: "src/app/api/platform/projects/[id]/route.ts"
      provides: "Project edit/delete restricted to staff"
      contains: "requireStaff"
    - path: "src/app/api/platform/projects/[id]/destroy/route.ts"
      provides: "Project destruction restricted to staff"
      contains: "requireStaff"
    - path: "src/app/api/platform/navigation/admin/route.ts"
      provides: "Navigation admin restricted to staff"
      contains: "requireStaff"
  key_links:
    - from: "each updated route file"
      to: "src/lib/api-auth.ts"
      via: "import { requireStaff } from '@/lib/api-auth'"
      pattern: "import \\{ requireStaff \\} from '@/lib/api-auth'"
---

<objective>
For every endpoint classified `staff-only` in `01.1-CLASSIFICATION.md`, swap the `requireAdmin()` call for `requireStaff()`. These are the destructive / provisioning / platform-admin endpoints that must reject non-staff with 403.

Purpose: Closes the live access leak verified 2026-05-03 — `mike@mikegeehan.com` (a non-staff customer admin) could call `/destroy`, `/scaffold-repo`, `/provision-db`, `/provision-dns`, navigation editing, settings, etc. After this plan, all of those return 403 to anyone without the wildcard staff row.

Output: Each staff-only file imports `requireStaff` from `@/lib/api-auth` and calls it in place of `requireAdmin()` in every HTTP handler.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-membership-enforcement-audit/01.1-CONTEXT.md
@.planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md
@AGENTS.md

@src/lib/api-auth.ts
@src/lib/auth-context.ts

<interfaces>
<!-- The helper this plan consumes (built in Plan 01.1-01): -->

```typescript
// src/lib/api-auth.ts
export async function requireStaff(): Promise<{
  error: NextResponse | null;
  session: Session | null;
  ctx: UserContext | null;
}>;
```

Returns 401 if not signed in, 403 if signed in but not staff (or DB error). Otherwise `error: null` and `ctx.isStaff === true`.

<!-- The legacy helper this plan REPLACES at each callsite: -->

```typescript
// Same file — kept as deprecated alias for unmigrated callers
export const requireAdmin = requireSignedIn;  // returns { error, session }
```

<!-- The 23 staff-only files (per CLASSIFICATION.md) — each gets the same import-swap + call-swap pattern. -->
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate every staff-only file to requireStaff</name>
  <files>
    src/app/api/platform/projects/[id]/route.ts,
    src/app/api/platform/projects/[id]/destroy/route.ts,
    src/app/api/platform/projects/scaffold-repo/route.ts,
    src/app/api/platform/projects/provision-db/route.ts,
    src/app/api/platform/projects/provision-dns/route.ts,
    src/app/api/platform/projects/sync-state/route.ts,
    src/app/api/platform/projects/tools/route.ts,
    src/app/api/platform/projects/tools/scaffold/route.ts,
    src/app/api/platform/navigation/sections/route.ts,
    src/app/api/platform/navigation/sections/[id]/route.ts,
    src/app/api/platform/navigation/pages/route.ts,
    src/app/api/platform/navigation/pages/[id]/route.ts,
    src/app/api/platform/navigation/subpages/route.ts,
    src/app/api/platform/navigation/subpages/[id]/route.ts,
    src/app/api/platform/navigation/reorder/route.ts,
    src/app/api/platform/navigation/admin/route.ts,
    src/app/api/platform/settings/route.ts,
    src/app/api/platform/access-logs/route.ts,
    src/app/api/platform/release-logs/backfill/route.ts,
    src/app/api/platform/webhooks/backfill/route.ts,
    src/app/api/platform/report-section-types/route.ts,
    src/app/api/platform/service-offerings/route.ts,
    src/app/api/platform/service-offerings/[id]/route.ts
  </files>

  <read_first>
    - .planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md (authoritative list of staff-only files; this plan owns every row marked `staff-only`)
    - src/lib/api-auth.ts (post-Plan-01 — confirm `requireStaff` is exported)
    - src/app/api/platform/projects/[id]/destroy/route.ts (read at least one representative staff-only file before starting to confirm the import + call pattern)
    - .planning/phases/01.1-membership-enforcement-audit/01.1-CONTEXT.md (locked decisions)
  </read_first>

  <action>
    For each file in the `<files>` list above, apply this exact two-step transform.

    **Authoritative list:** read `.planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md` and confirm the file list. If CLASSIFICATION.md surfaced any `unclear` rows that resolved to `staff-only` and aren't in this plan's `<files>` list, ALSO update them and note the addition in the SUMMARY. If a file in this plan's `<files>` list was reclassified to a different bucket, SKIP it and note in the SUMMARY.

    **Step A — Update the import line.**

    Find this line:
    ```typescript
    import { requireAdmin } from '@/lib/api-auth';
    ```
    Replace it with:
    ```typescript
    import { requireStaff } from '@/lib/api-auth';
    ```

    If the file imports MORE than just `requireAdmin` from `@/lib/api-auth` (rare), update only the `requireAdmin` token; leave any other named imports intact.

    **Step B — Update every callsite.**

    For every occurrence inside the file of:
    ```typescript
    const { error } = await requireAdmin();
    ```
    Replace with:
    ```typescript
    const { error } = await requireStaff();
    ```

    For every occurrence of:
    ```typescript
    const { error, session } = await requireAdmin();
    ```
    Replace with:
    ```typescript
    const { error, session } = await requireStaff();
    ```

    If a callsite destructures additional properties (e.g., `const { error, session, ctx } = await requireAdmin();`), preserve the destructured names — `requireStaff` returns `{error, session, ctx}` so adding `ctx` is safe; do not add it speculatively, only if the function body already uses it.

    **Step C — Do NOT change anything else.**

    - Do not refactor handler logic.
    - Do not add comments or JSDoc.
    - Do not change error response shapes.
    - Do not touch any file outside the `<files>` list.
    - Do not change `src/lib/api-auth.ts` (Plan 01 owns it).
    - Do not change anything in `src/app/api/platform/release-logs/route.ts` or any other `project-list` / `project-detail` file (Plans 04/05 own those).
    - Per AGENTS.md: this is Next.js 16+. The `getServerSession`/`NextResponse` patterns inside the handlers stay exactly as they are. We are only swapping the auth helper.

    **Step D — Special cases.**

    - `src/app/api/platform/projects/[id]/route.ts` has both PUT and DELETE handlers; update both `requireAdmin()` calls.
    - `src/app/api/platform/access-logs/route.ts` has GET and POST; update both. (Audit-log writes/reads are staff-only.)
    - `src/app/api/platform/release-logs/backfill/route.ts` and `webhooks/backfill/route.ts` are one-shot maintenance endpoints; staff-only.
    - `src/app/api/platform/navigation/admin/route.ts`, `settings/route.ts`, `report-section-types/route.ts`, `service-offerings/route.ts`, `service-offerings/[id]/route.ts` — IF CLASSIFICATION.md marked any of these something other than `staff-only` after the body-read step, follow CLASSIFICATION.md (it's the authoritative source) and remove that file from the migration here, noting in the SUMMARY.
  </action>

  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin &amp;&amp; FILES="src/app/api/platform/projects/[id]/route.ts src/app/api/platform/projects/[id]/destroy/route.ts src/app/api/platform/projects/scaffold-repo/route.ts src/app/api/platform/projects/provision-db/route.ts src/app/api/platform/projects/provision-dns/route.ts src/app/api/platform/projects/sync-state/route.ts src/app/api/platform/projects/tools/route.ts src/app/api/platform/projects/tools/scaffold/route.ts src/app/api/platform/navigation/sections/route.ts src/app/api/platform/navigation/sections/[id]/route.ts src/app/api/platform/navigation/pages/route.ts src/app/api/platform/navigation/pages/[id]/route.ts src/app/api/platform/navigation/subpages/route.ts src/app/api/platform/navigation/subpages/[id]/route.ts src/app/api/platform/navigation/reorder/route.ts src/app/api/platform/navigation/admin/route.ts src/app/api/platform/settings/route.ts src/app/api/platform/access-logs/route.ts src/app/api/platform/release-logs/backfill/route.ts src/app/api/platform/webhooks/backfill/route.ts src/app/api/platform/report-section-types/route.ts src/app/api/platform/service-offerings/route.ts src/app/api/platform/service-offerings/[id]/route.ts"; for f in $FILES; do grep -l "requireStaff" "$f" &gt; /dev/null || { echo "MISSING requireStaff: $f"; exit 1; }; grep -l "requireAdmin" "$f" &gt; /dev/null &amp;&amp; { echo "STILL HAS requireAdmin: $f"; exit 1; }; done; echo "all $(echo $FILES | wc -w) files migrated"</automated>
  </verify>

  <acceptance_criteria>
    - For every file in the `<files>` list: `grep -l 'requireStaff' {file}` returns the file (it is now using requireStaff)
    - For every file in the `<files>` list: `grep -l 'requireAdmin' {file}` returns nothing (no `requireAdmin` import or callsite remains in any staff-only file)
    - Files NOT in this plan's scope (`projects/route.ts`, `release-logs/route.ts`, `bug-reports/route.ts`, `feature-requests/route.ts`, `release-logs/[id]/route.ts`, `bug-reports/[id]/route.ts`, `feature-requests/[id]/route.ts`, `reports/route.ts`, `reports/[id]/route.ts`) are UNCHANGED — they still import `requireAdmin` (Plans 04/05 will migrate them)
    - The legacy `requireAdmin` alias still exists in `src/lib/api-auth.ts` (Plan 01's deliverable; this plan must not touch it)
  </acceptance_criteria>

  <done>
    Every staff-only endpoint rejects non-staff with 403. Triarch staff continue to work normally because `requireStaff` short-circuits to success when `ctx.isStaff === true`. Non-staff signed-in customer admins can no longer call destructive or platform-admin operations.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify the build and grep for any leftover staff-only callsites</name>
  <files>(no file changes — gate task)</files>

  <read_first>
    - .planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md (use the staff-only count to confirm post-migration grep results)
  </read_first>

  <action>
    1. Run `npx tsc --noEmit` and `npx next build`. Both must succeed.

    2. Run a sanity grep:
       ```
       grep -rln 'requireAdmin\b' src/app/api/platform/ | sort
       ```
       The result should contain ONLY the files NOT yet migrated by this plan — i.e., the files Plans 04 and 05 own (`projects/route.ts`, `release-logs/route.ts`, `release-logs/[id]/route.ts`, `bug-reports/route.ts`, `bug-reports/[id]/route.ts`, `feature-requests/route.ts`, `feature-requests/[id]/route.ts`, plus `reports/route.ts` and `reports/[id]/route.ts` if they're list/detail per CLASSIFICATION.md). The count should equal `32 - {staff-only count from CLASSIFICATION.md}`. If that math doesn't add up, an endpoint was missed.

    3. If `npx next build` surfaces any error pointing into one of the migrated files, fix it inside that file using the same import/call swap pattern from Task 1 — do not introduce new logic.
  </action>

  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin &amp;&amp; npx tsc --noEmit &amp;&amp; npx next build</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npx next build` exits 0
    - `grep -rln 'requireAdmin\b' src/app/api/platform/` returns ONLY the project-list and project-detail files owned by Plans 04 and 05 (cross-check against `01.1-CLASSIFICATION.md` Counts section — should match `32 - {staff-only count}`)
  </acceptance_criteria>

  <done>
    Build is green. Staff-only enforcement is in place across all classified endpoints. The remaining `requireAdmin` callsites are exactly the ones Plans 04 and 05 will migrate.
  </done>
</task>

</tasks>

<verification>
- All staff-only endpoints from `01.1-CLASSIFICATION.md` import and call `requireStaff`.
- No staff-only endpoint still imports `requireAdmin`.
- `npx tsc --noEmit` and `npx next build` succeed.
- Live UAT (MEMBER-AUDIT-09) is deferred to Plan 06 — this plan's verification is grep + build, not browser sign-in.
</verification>

<success_criteria>
Phase 1.1 success criterion #3 (all staff-only endpoints reject non-staff with 403) is satisfied at the code level. Live verification with `mike@mikegeehan.com` happens in Plan 06 / post-deploy UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-membership-enforcement-audit/01.1-03-SUMMARY.md` capturing:
- Count of files migrated (should match CLASSIFICATION.md staff-only count)
- Any deviations from CLASSIFICATION.md (e.g., a file moved between buckets after a closer read)
- The remaining `requireAdmin\b` grep output (which files Plans 04/05 still own)
</output>
