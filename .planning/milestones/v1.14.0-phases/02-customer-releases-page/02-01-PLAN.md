---
phase: 02-customer-releases-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/migrations/0008_*.sql
  - src/db/migrations/meta/0008_snapshot.json
  - src/db/migrations/meta/_journal.json
autonomous: true
requirements:
  - REJECT-01

must_haves:
  truths:
    - "release_approvals table has a reason column for rejection rationale"
    - "Drizzle relations() exist linking releaseLogs ↔ releaseFeedback ↔ releaseApprovals"
    - "Generated migration applied cleanly to local DB without dropping data"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "releaseApprovals.reason column + relations declarations"
      contains: "reason: text"
    - path: "src/db/migrations/0008_*.sql"
      provides: "ADD COLUMN reason migration"
      contains: "ADD COLUMN \"reason\""
  key_links:
    - from: "src/db/schema.ts"
      to: "releaseApprovals table"
      via: "reason column declaration"
      pattern: "reason:\\s*text"
    - from: "src/db/schema.ts"
      to: "Drizzle relations()"
      via: "releaseLogsRelations / releaseFeedbackRelations / releaseApprovalsRelations"
      pattern: "releaseLogsRelations\\s*=\\s*relations"
---

<objective>
Add the `reason` column to `release_approvals` (required by REJECT-01 to persist rejection rationale per UI-SPEC's 500-char-limit reject form). Add Drizzle `relations()` declarations for the three release-gating tables — Phase 1 deferred these explicitly: "Phase 2 adds them when customer releases page consumes them" (per 01-01 SUMMARY key-decisions).

Purpose: Phase 1 schema is complete EXCEPT for `release_approvals.reason` (required by REJECT-01 — rejected releases need persisted reason for the audit trail line `rejected by X: {excerpt}` per UI-SPEC) and `relations()` declarations (Phase 1 explicitly deferred). All remaining plans (02, 03, 04, 05) depend on this column existing. This is the only schema change in Phase 2.

Output: Updated schema.ts + new generated Drizzle migration + applied to local DB.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-customer-releases-page/02-CONTEXT.md
@.planning/phases/02-customer-releases-page/02-UI-SPEC.md
@.planning/phases/01-schema-membership-migration/01-01-SUMMARY.md
@src/db/schema.ts

<interfaces>
<!-- Existing schema for the three release-gating tables (from src/db/schema.ts lines 136-183) -->

releaseLogs (line 136): UUID id, project varchar(64), version varchar(32), env varchar(8) NULL, status varchar(24) NULL, commitSha varchar(64) NULL, deployedAt timestamp NULL, plus standard release-log fields.

releaseFeedback (line 166): UUID id, releaseId uuid FK→releaseLogs.id (cascade), authorEmail varchar(256), body text, createdAt timestamp.

releaseApprovals (line 174): UUID id, releaseId uuid FK→releaseLogs.id (cascade), approverEmail varchar(256), decision varchar(16) ('approved'|'rejected'), approvedAt timestamp, ipAddress varchar(45) NULL, userAgent varchar(512) NULL, createdAt timestamp. **NO reason column yet — this plan adds it.**

Drizzle `relations()` block (line 363): currently only menuSections / menuPages / menuSubpages have relations declared. Per 01-01 SUMMARY: "No Drizzle relations() declarations for new tables in Phase 1 — Phase 2 adds them when customer releases page consumes them."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reason column + relations to schema.ts</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - src/db/schema.ts (current state of releaseApprovals + relations block)
    - .planning/phases/02-customer-releases-page/02-CONTEXT.md (CONTEXT specifics line 86: "Add a nullable `reason TEXT` column if it doesn't exist yet")
    - .planning/phases/02-customer-releases-page/02-UI-SPEC.md (Reject Inline Form section: 500-char limit on rejection reason)
    - .planning/phases/01-schema-membership-migration/01-01-SUMMARY.md (key-decisions: relations deferred to Phase 2)
  </read_first>
  <action>
    In `src/db/schema.ts`:

    1. Add a `reason` column to the `releaseApprovals` pgTable definition (currently at line 174). Insert it AFTER `userAgent` and BEFORE `createdAt`:
       ```
       reason: text('reason'),  // rejection reason for REJECT-01; nullable (approve rows have NULL); 500-char limit enforced server-side
       ```
       Use `text(...)` (not `varchar(500)`) — matches `body: text('body')` pattern on `releaseFeedback`. Server-side validation enforces the 500-char ceiling; DB stays unbounded text.

    2. Add three `relations()` blocks AFTER the existing `menuSubpagesRelations` declaration (currently the last relations block, around line 375) but BEFORE the `// ── Plan 5: Report Generator ──` comment block. Insert exactly:
       ```
       export const releaseLogsRelations = relations(releaseLogs, ({ many }) => ({
         feedback: many(releaseFeedback),
         approvals: many(releaseApprovals),
       }));

       export const releaseFeedbackRelations = relations(releaseFeedback, ({ one }) => ({
         release: one(releaseLogs, {
           fields: [releaseFeedback.releaseId],
           references: [releaseLogs.id],
         }),
       }));

       export const releaseApprovalsRelations = relations(releaseApprovals, ({ one }) => ({
         release: one(releaseLogs, {
           fields: [releaseApprovals.releaseId],
           references: [releaseLogs.id],
         }),
       }));
       ```

    Do NOT modify any other table. Do NOT touch `releaseFeedback` or `releaseLogs` columns. Do NOT add `decision`-specific check constraints — server logic validates.

    Why text not varchar: `releaseFeedback.body` is `text` and uses 2000-char server-side limit; `release_approvals.reason` follows same pattern with 500-char server-side limit. Drizzle migration cleaner with text columns.
  </action>
  <verify>
    <automated>grep -E "reason:\s*text\('reason'\)" src/db/schema.ts && grep -E "releaseLogsRelations\s*=\s*relations" src/db/schema.ts && grep -E "releaseFeedbackRelations\s*=\s*relations" src/db/schema.ts && grep -E "releaseApprovalsRelations\s*=\s*relations" src/db/schema.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `reason: text('reason')` line exists inside `releaseApprovals` definition
    - Three new exported relations consts exist: `releaseLogsRelations`, `releaseFeedbackRelations`, `releaseApprovalsRelations`
    - `tsc --noEmit` passes (no type errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate + apply Drizzle migration for reason column</name>
  <files>src/db/migrations/0008_*.sql, src/db/migrations/meta/0008_snapshot.json, src/db/migrations/meta/_journal.json</files>
  <read_first>
    - src/db/migrations/0007_flashy_madelyne_pryor.sql (most recent migration — the Phase 1 schema migration; reference for naming pattern)
    - src/db/schema.ts (post-Task-1 state — the schema being migrated)
    - drizzle.config.ts if present at repo root
  </read_first>
  <action>
    Run `npm run db:generate` (which runs `drizzle-kit generate`). This will:
    - Detect the new `reason` column on `release_approvals`
    - Auto-generate `src/db/migrations/0008_<random_name>.sql` containing `ALTER TABLE "release_approvals" ADD COLUMN "reason" text;`
    - Update `src/db/migrations/meta/_journal.json` and write `meta/0008_snapshot.json`

    Do NOT hand-write the migration. Drizzle-kit generates it.

    Then apply to local DB:
    - Run `npm run db:push` (or `drizzle-kit push`) to apply the new column. CRDB / Postgres `ADD COLUMN ... text` with no default is non-blocking on existing rows.

    Verify the column exists in the live DB by running:
    ```
    psql "$DATABASE_URL" -c "\d release_approvals"
    ```
    or equivalent — must show a `reason | text |` row.

    The relations() additions DO NOT generate SQL (relations are TypeScript-only metadata for Drizzle's relational query builder). The migration file should ONLY contain the `ADD COLUMN reason text` statement.

    Note: relations() consumers in later plans (02/03/04/05) use Drizzle's relational query builder (e.g. `db.query.releaseLogs.findMany({ with: { feedback: true, approvals: true } })`) — but plain `db.select()` joins also remain available.
  </action>
  <verify>
    <automated>ls src/db/migrations/0008_*.sql && grep -E "ADD COLUMN \"reason\" text" src/db/migrations/0008_*.sql && psql "$DATABASE_URL" -c "\d release_approvals" | grep -E "^\s*reason\s*\|\s*text"</automated>
  </verify>
  <done>
    - File `src/db/migrations/0008_*.sql` exists and contains `ADD COLUMN "reason" text`
    - `meta/0008_snapshot.json` exists with the new column in the snapshot
    - `_journal.json` updated to reference the new migration
    - Local DB column exists (`\d release_approvals` shows `reason` text column)
    - No data loss — existing release_approvals rows still present (`SELECT count(*) FROM release_approvals` matches pre-migration count, all with `reason IS NULL`)
  </done>
</task>

</tasks>

<verification>
Schema delta is the foundation for the rest of Phase 2. Verifying:
- `grep -E "reason:\s*text\('reason'\)" src/db/schema.ts` returns the line
- `grep -E "releaseLogsRelations\s*=\s*relations" src/db/schema.ts` returns the line
- `ls src/db/migrations/0008_*.sql` shows exactly one new migration file
- `psql "$DATABASE_URL" -c "SELECT column_name FROM information_schema.columns WHERE table_name='release_approvals' AND column_name='reason'"` returns one row
- `npm run build` (or `npx next build`) compiles cleanly
</verification>

<success_criteria>
- `release_approvals.reason` column exists in DB (text, nullable)
- `releaseLogsRelations`, `releaseFeedbackRelations`, `releaseApprovalsRelations` exported from schema.ts
- Drizzle migration 0008_*.sql applied
- No existing data lost
- TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/02-customer-releases-page/02-01-SUMMARY.md` documenting:
- The `reason` column added to release_approvals
- The three relations() declarations added (releaseLogs ↔ releaseFeedback ↔ releaseApprovals)
- Migration file name (0008_*.sql)
- Confirmation of clean local DB application
</output>
