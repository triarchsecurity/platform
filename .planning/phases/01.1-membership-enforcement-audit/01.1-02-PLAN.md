---
phase: 01.1-membership-enforcement-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md
autonomous: true
requirements:
  - MEMBER-AUDIT-04

must_haves:
  truths:
    - "Every endpoint currently importing requireAdmin from @/lib/api-auth is listed in the classification table — no omissions"
    - "Each endpoint is classified into one of: staff-only, project-list, project-detail, unclear"
    - "Each row records: file path, HTTP methods present, target table/resource, classification, target helper, and a 1-sentence rationale"
    - "Classification is sourced from reading each endpoint body — not just the URL — so methods like POST that need body.project membership checks are flagged"
    - "Plans 03/04/05 can implement directly from this table without re-reading every endpoint"
  artifacts:
    - path: ".planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md"
      provides: "The authoritative endpoint classification table consumed by Plans 03/04/05"
      contains: "## Classification Table"
  key_links:
    - from: ".planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md"
      to: "Plans 01.1-03, 01.1-04, 01.1-05"
      via: "shared reference document — each subsequent plan reads CLASSIFICATION.md to know which endpoints it owns"
      pattern: "01.1-CLASSIFICATION.md"
---

<objective>
Audit every endpoint currently using `requireAdmin()` and produce a single authoritative classification table at `.planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md`. Plans 03/04/05 will read directly from this table to know which endpoints they own and what helper to call.

Purpose: Avoids three downstream plans each re-reading 32 files to figure out their boundaries. Avoids drift between plans (e.g., Plan 03 thinking `service-offerings` is staff-only while Plan 04 thinks it's project-scoped). One source of truth, written once, consumed three times.

Output: `01.1-CLASSIFICATION.md` with a single Markdown table covering all 32 endpoints (every file under `src/app/api/platform/` that imports `requireAdmin`).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01.1-membership-enforcement-audit/01.1-CONTEXT.md
@AGENTS.md

@src/lib/api-auth.ts
@src/lib/auth-context.ts
@src/app/api/platform/projects/route.ts
@src/app/api/platform/projects/[id]/members/route.ts

<interfaces>
<!-- The four classification labels, fixed by 01.1-CONTEXT.md. Use these exact strings. -->

`staff-only` — operates on platform infrastructure or cross-project state. Customers must NEVER call. Action: switch to `requireStaff()`.

`project-list` — returns a collection of project-scoped rows. Action: keep `requireSignedIn()`, then filter results by `ctx.memberships` for non-staff (staff unfiltered).

`project-detail` — operates on a single project's row, identified by URL param or body. Action: fetch row, then `requireMembership(row.project)` for non-staff. Non-member returns 404 (mirroring the page-level no-leak pattern from Phase 1).

`unclear` — not obviously project-scoped or platform-wide on first read. Action: read the body, decide. Default to `staff-only` if customer access has no plausible benefit.

<!-- Initial classifications from 01.1-CONTEXT.md (refine in CLASSIFICATION.md if reading the body changes the verdict): -->

staff-only candidates:
- /api/platform/projects/[id] (PUT/DELETE)
- /api/platform/projects/[id]/destroy
- /api/platform/projects/scaffold-repo, /provision-db, /provision-dns, /sync-state, /tools, /tools/scaffold
- /api/platform/navigation/* (sections, sections/[id], pages, pages/[id], subpages, subpages/[id], reorder, admin)
- /api/platform/settings
- /api/platform/access-logs
- /api/platform/release-logs/backfill
- /api/platform/webhooks/backfill
- /api/platform/report-section-types (verify in body)
- /api/platform/service-offerings, /service-offerings/[id] (verify in body)

project-list candidates:
- GET /api/platform/release-logs
- GET /api/platform/bug-reports
- GET /api/platform/feature-requests

project-detail candidates:
- GET/PUT /api/platform/release-logs/[id]
- GET/PATCH /api/platform/bug-reports/[id]
- GET/PUT /api/platform/feature-requests/[id]

NOTE on POST handlers in project-list files (release-logs, bug-reports, feature-requests):
The list file may also have POST. POSTs need `body.project` membership check for non-staff. Record the POST behavior in the classification too — Plan 04 will handle it.

NOTE on the projects-list itself (`src/app/api/platform/projects/route.ts`):
Its GET handler already uses `getCurrentUserContext` for the membership filter (Plan 01-04 work). It still imports `requireAdmin` though, and POST is staff-only (creating projects). Treat as `project-list` with a staff-only POST — note in the rationale.

NOTE on `reports/route.ts` and `reports/[id]/route.ts`:
The orchestrator's enumerated 30-file list missed these two files but `grep` confirms they import `requireAdmin`. They ARE in scope. Reading their body shows they read/write the `reports` table with a `project` column — same shape as release-logs. Likely `project-list` for GET on /reports, `project-detail` for GET/PUT on /reports/[id]. Verify by reading the bodies.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enumerate all 32 endpoints and read each one</name>
  <files>(no file changes — gathering step)</files>

  <read_first>
    - src/lib/api-auth.ts (current state — to confirm what `requireAdmin` does today)
    - src/lib/auth-context.ts (UserContext shape)
    - .planning/phases/01.1-membership-enforcement-audit/01.1-CONTEXT.md (locked classification policy + initial guesses)
  </read_first>

  <action>
    1. Run `grep -rln "requireAdmin\b" src/app/api/platform/ | sort` to get the canonical file list. Expected count: 32. If the count differs (file was added/removed since planning), record the delta in the rationale column of CLASSIFICATION.md and proceed.

    2. For each file in the list, open it (use Read tool, not cat) and capture:
       - Which HTTP methods are exported (GET / POST / PUT / PATCH / DELETE)
       - Which DB table(s) it reads/writes (look at imports from `@/db/schema` and the `db.select().from(...)` / `db.insert(...)` / `db.update(...)` calls)
       - Whether the table has a `project` column (signal of project-scoped data)
       - For POST handlers: does the body contain a `project` field? (if yes — flag for Plan 04 POST membership check)

    3. Special cases to verify in code:
       - `/api/platform/report-section-types` — read body. If it operates on a global lookup table with no project column, it's `staff-only`.
       - `/api/platform/service-offerings` and `/service-offerings/[id]` — read body. If the offerings table is global (no `project` FK), `staff-only`. If `project`-scoped, `project-list` / `project-detail`.
       - `/api/platform/projects/route.ts` — GET is already partially membership-aware (Plan 01-04 work) but still imports `requireAdmin`. POST creates new projects. Classify as `project-list` with a note that GET keeps the existing filter (just swap helper imports) and POST upgrades to `requireStaff()`.
       - `/api/platform/reports/route.ts` and `/api/platform/reports/[id]/route.ts` — read body to confirm `reports` table has a `project` column. If yes, classify as `project-list` and `project-detail` respectively.

    Output: a structured in-memory list ready to write into CLASSIFICATION.md in Task 2. (No file changes yet.)
  </action>

  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin &amp;&amp; test "$(grep -rln 'requireAdmin\b' src/app/api/platform/ | wc -l | tr -d ' ')" = "32"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rln 'requireAdmin\b' src/app/api/platform/ | wc -l` returns `32`
    - The executor has read every one of the 32 files (use the Read tool — verify by listing each file in the SUMMARY)
    - HTTP methods, target tables, and `project` column presence have been recorded for each file
  </acceptance_criteria>

  <done>
    All 32 endpoints have been enumerated and read. Tables, methods, and project-column presence are known. Ready to write the classification table.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write CLASSIFICATION.md with the full table</name>
  <files>.planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md</files>

  <read_first>
    - The 32 endpoint files enumerated in Task 1
    - .planning/phases/01.1-membership-enforcement-audit/01.1-CONTEXT.md (decisions block — locked classification policy)
  </read_first>

  <action>
    Create `.planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md` with this structure:

    ```markdown
    # Phase 1.1: Endpoint Classification

    **Generated:** {today's date}
    **Source:** Read of every file under src/app/api/platform/ that imports requireAdmin (32 files)
    **Authority:** Plans 01.1-03 (staff-only), 01.1-04 (project-list), 01.1-05 (project-detail) consume this table.

    ## Classification Policy

    | Class           | Action                                                                                                                                  |
    | --------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
    | staff-only      | Replace `requireAdmin()` import + call with `requireStaff()`. Returns 403 to non-staff.                                                 |
    | project-list    | Keep `requireSignedIn()`. After fetching ctx via `getCurrentUserContext(session)`, staff get unfiltered; non-staff get `WHERE project IN (memberships)`. POST handlers validate body.project membership for non-staff. |
    | project-detail  | Keep `requireSignedIn()`. Fetch the row first, then for non-staff verify membership on `row.project`; non-member → 404 (no leak).      |
    | unclear         | Reread; default staff-only if customer benefit is implausible.                                                                          |

    ## Classification Table

    | # | File | Methods | Table(s) | project col? | Class | Target helper | Owning plan | Notes |
    |---|------|---------|----------|--------------|-------|---------------|-------------|-------|
    | 1 | src/app/api/platform/access-logs/route.ts | GET, POST | accessAuditLogs | yes | staff-only | requireStaff | 01.1-03 | Audit log is staff-only viewing/writing — customers don't see who triaged what across projects. |
    | 2 | src/app/api/platform/bug-reports/route.ts | GET, POST | bugReports | yes | project-list | requireSignedIn + filter | 01.1-04 | GET filters by ctx.memberships for non-staff. POST validates body.project membership for non-staff (workflow: customer files bug for own project; staff files for any). |
    | ... | ... | ... | ... | ... | ... | ... | ... | ... |
    ```

    Fill in ALL 32 rows. Follow these rules:

    1. **Use the exact file path** with `src/app/api/platform/...` prefix. Plans 03/04/05 will grep against these paths.

    2. **Methods column**: list HTTP verbs actually exported from the file (e.g., `GET, POST` or `GET, PATCH` or `PUT, DELETE`).

    3. **Class column**: one of `staff-only`, `project-list`, `project-detail`. Avoid `unclear` if at all possible — the reading-the-body step in Task 1 was meant to resolve unclear cases. If you cannot resolve one, mark it `unclear` and add a `**FOLLOW-UP:**` note in the Notes column explaining what additional info is needed.

    4. **Owning plan**:
       - `staff-only` → `01.1-03`
       - `project-list` → `01.1-04`
       - `project-detail` → `01.1-05`

    5. **Special-case rows that need explicit Notes:**
       - `src/app/api/platform/projects/route.ts` — class `project-list`. Note: "GET already implements the membership filter (Plan 01-04 legacy). Plan 04 swaps the import from `requireAdmin` to `requireSignedIn` and keeps the existing filter logic. POST is staff-only (creating new projects); Plan 04 wraps the POST entry with `requireStaff()`."
       - `src/app/api/platform/projects/[id]/route.ts` — class `staff-only`. Note: "PUT and DELETE both edit project metadata; both call requireStaff."
       - `src/app/api/platform/release-logs/[id]/route.ts` — class `project-detail`. Note: "GET fetches the row, then for non-staff calls requireMembership(row.project); non-member → 404. PUT does the same."
       - `src/app/api/platform/release-logs/route.ts` POST — Note: "POST is the manual-create flow (the ingest endpoint at /api/platform/ingest/release-logs is separate and untouched by this phase). Non-staff POSTs must have body.project in ctx.memberships."
       - `src/app/api/platform/bug-reports/route.ts` POST — Note: "POST validates body.project membership for non-staff."
       - `src/app/api/platform/feature-requests/route.ts` POST — same as above.

    6. **End the file with a 'Counts' section:**

       ```markdown
       ## Counts

       - staff-only: {N1} files → owned by Plan 01.1-03
       - project-list: {N2} files → owned by Plan 01.1-04
       - project-detail: {N3} files → owned by Plan 01.1-05
       - unclear (with follow-up): {N4} files
       - **Total:** {N1 + N2 + N3 + N4} = 32

       ## Coverage Verification

       Every file output by `grep -rln 'requireAdmin\b' src/app/api/platform/` appears in exactly one row of the classification table above. Run this grep before Plans 03/04/05 begin to confirm no new endpoint slipped in since this audit.
       ```

    7. **Do not modify any source files in this plan.** This is a documentation-only deliverable.
  </action>

  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin &amp;&amp; test -f .planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md &amp;&amp; test "$(grep -c 'src/app/api/platform/' .planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md)" -ge 32 &amp;&amp; grep -q '## Classification Table' .planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md &amp;&amp; grep -q '## Counts' .planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md &amp;&amp; grep -q 'staff-only' .planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md &amp;&amp; grep -q 'project-list' .planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md &amp;&amp; grep -q 'project-detail' .planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/01.1-membership-enforcement-audit/01.1-CLASSIFICATION.md` exists
    - It contains a `## Classification Table` heading
    - It contains a `## Counts` heading with summed totals = 32
    - At least 32 rows reference a `src/app/api/platform/...` path (one per endpoint, possibly more if a Notes line spills over)
    - Every classification value used is one of the four: `staff-only`, `project-list`, `project-detail`, `unclear`
    - The owning-plan column maps every `staff-only` row to `01.1-03`, every `project-list` to `01.1-04`, every `project-detail` to `01.1-05`
    - The "Coverage Verification" section explicitly tells future plans to re-run the grep before they start
  </acceptance_criteria>

  <done>
    `01.1-CLASSIFICATION.md` is the authoritative source for which plan owns which endpoint. Plans 03/04/05 can implement directly from it. Rationale for each classification is captured so future re-reads (e.g., during checker review) don't have to re-derive the policy.
  </done>
</task>

</tasks>

<verification>
- 32 endpoint files have been enumerated and classified.
- `01.1-CLASSIFICATION.md` lists every file with its owning plan.
- Plans 03/04/05 can begin in parallel without re-doing this audit.
</verification>

<success_criteria>
Phase 1.1 success criterion #2 (every endpoint previously using requireAdmin is classified into staff-only/project-list/project-detail/unclear; classification recorded) is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-membership-enforcement-audit/01.1-02-SUMMARY.md` capturing:
- The final counts ({staff-only}, {project-list}, {project-detail}, {unclear})
- Any endpoint where the body-read changed the initial guess from 01.1-CONTEXT.md (e.g., service-offerings turned out to be project-scoped after all, or vice versa)
- Any endpoint flagged `unclear` with a follow-up question for Mike
</output>
