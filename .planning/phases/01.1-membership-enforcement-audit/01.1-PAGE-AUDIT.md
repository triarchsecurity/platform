# Phase 1.1: Page-Level Audit (MEMBER-AUDIT-08)

**Generated:** 2026-05-03
**Source:** Read of every page.tsx under src/app/admin/ via `find src/app/admin -name page.tsx | sort`

## Layout Context

`src/app/admin/layout.tsx` calls `getServerSession(authOptions)` and redirects to `/login` if no session exists (`!session?.user`). It does NOT check `isStaff` or any membership — it only confirms "is somebody signed in". Per-page guards are required for non-staff scoping wherever a page reads the DB directly.

## Page Audit Table

| # | Page | 'use client'? | Reads DB directly? | Delegates to API? | Existing guard? | Verdict | Action in this plan |
|---|------|---------------|--------------------|-------------------|-----------------|---------|---------------------|
| 1 | src/app/admin/page.tsx | no (server component) | YES — `db.select()` x7 on projects, releaseLogs, bugReports, featureRequests | no | none | inline-needed | Task 2 modifies this file |
| 2 | src/app/admin/modules/access-audit/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/access-logs?...')` | n/a (client) | delegates | none |
| 3 | src/app/admin/modules/bug-reports/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/bug-reports?...')` + `fetch('/api/platform/bug-reports/${id}', {method:'PATCH'})` | n/a (client) | delegates | none |
| 4 | src/app/admin/modules/feature-requests/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/feature-requests?...')` + PATCH | n/a (client) | delegates | none |
| 5 | src/app/admin/modules/release-logs/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/release-logs?...')` + POST | n/a (client) | delegates | none |
| 6 | src/app/admin/modules/reports/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/reports')` + `fetch('/api/platform/report-section-types')` + PUT/DELETE | n/a (client) | delegates | none |
| 7 | src/app/admin/modules/service-offerings/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/service-offerings')` + PUT/DELETE | n/a (client) | delegates | none |
| 8 | src/app/admin/modules/tracker/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/bug-reports?...')` + `fetch('/api/platform/feature-requests?...')` + PATCH | n/a (client) | delegates | none |
| 9 | src/app/admin/platform/navigation/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/navigation/admin')` + PUT/DELETE/POST on navigation endpoints | n/a (client) | delegates | none |
| 10 | src/app/admin/platform/projects/[id]/members/page.tsx | no (server component) | YES — `db.select()` on projects + projectMembers | YES (the client child MembersClient calls API) | YES — `getCurrentUserContext` + `if (!ctx \|\| !ctx.isStaff) redirect('/admin')` | already-guarded | none |
| 11 | src/app/admin/platform/projects/new/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/projects', {method:'POST'})` + provision-db/provision-dns PUT | n/a (client) | delegates | none |
| 12 | src/app/admin/platform/projects/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/projects')` + PUT/DELETE on projects, provision-db, provision-dns | n/a (client) | delegates | none |
| 13 | src/app/admin/platform/tools/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/projects/tools')` + `fetch('/api/platform/projects/tools/scaffold')` + `fetch('/api/platform/projects/scaffold-repo')` | n/a (client) | delegates | none |
| 14 | src/app/admin/settings/page.tsx | YES (`'use client'`) | no | YES — `fetch('/api/platform/settings?...')` + PUT | n/a (client) | delegates | none |

## Summary

- Total pages audited: 14
- Delegates to API (no change): 12
- Already guarded: 1 (src/app/admin/platform/projects/[id]/members/page.tsx — staff-only redirect via getCurrentUserContext)
- Needs inline filter (modified in this plan): 1 (src/app/admin/page.tsx)
- Follow-up needed: 0

## Notes

**Row 10 — members page:** This server component reads the DB directly AND calls `getCurrentUserContext` with a staff-only redirect. It is already correctly guarded — non-staff users are redirected to `/admin` before any DB read executes. No change needed.

**Rows 11-12 (new project wizard + projects page):** These are client components that delegate to `/api/platform/projects` (POST and GET). The API's POST path is now guarded by `requireStaff()` (Plan 04) and the GET is membership-filtered. No page-level change needed.

**Row 7 (service-offerings), Row 9 (navigation), Row 14 (settings):** These delegate to staff-only API endpoints (Plans 03). A non-staff user navigating to these pages will get a 403 from the API — the UI will show empty state or an error. This is an acceptable UX for Phase 1.1; navigation-level access control (hiding these menu items for non-staff) is a deferred v1.15 improvement.

## Coverage Verification

Every page output by `find src/app/admin -name page.tsx` appears in exactly one row above. The 14 discovered pages are all accounted for with no overlaps and no omissions.
