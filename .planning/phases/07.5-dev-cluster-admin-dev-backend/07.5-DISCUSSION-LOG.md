# Phase 7.5: Dev Cluster + Admin Dev Backend - Discussion Log

> **Audit trail only.** Decisions captured in CONTEXT.md.

**Date:** 2026-05-05
**Phase:** 07.5-dev-cluster-admin-dev-backend
**Mode:** auto (4 gray areas auto-resolved with recommended defaults)
**Areas discussed:** Dev cluster provisioning, Per-project dev DB naming + creation, Per-app dev backend (FAH) provisioning + apphosting overlay, shared-workflows v4 design

---

## Dev cluster provisioning

| Question | Selected |
|----------|----------|
| Cluster type | NEW dedicated CRDB Cloud cluster ✓ |
| Cluster naming | Mike's discretion at provision time |
| Region | Same as prod (us-east1 typical) ✓ |
| Sizing | Smallest tier (Basic 2 vCPU) ✓ |

**HUMAN action:** Mike provisions via Cockroach Cloud console.

## Per-project dev DB naming + creation

| Question | Selected |
|----------|----------|
| Naming convention | `<app>_dev` (six DBs) ✓ |
| Creation method | One-shot SQL `scripts/provision-dev-dbs.sql` ✓ |
| Owner role | Default admin role ✓ |
| Schema seeding | Empty; first `drizzle-kit push` creates schema ✓ |

## Per-app dev backend (FAH) provisioning + apphosting overlay

| Question | Selected |
|----------|----------|
| Firebase project | Same as prod for Triarch-owned; existing customer GCP for customer-owned ✓ |
| Git branch convention | `dev` (canonical) ✓ |
| DATABASE_URL_DEV secret | Per-backend `firebase apphosting:secrets:set` ✓ |
| Domain pattern | FAH default (no custom DNS) ✓ |
| Auth | Same NextAuth + Google OAuth as prod (no bypass) ✓ |
| apphosting overlay | `apphosting.yaml` (dev base) + `apphosting.prod.yaml` (prod overrides) ✓ |

**HUMAN action:** Per-project `firebase apphosting:backends:create` runs.

## shared-workflows v4 design

| Question | Selected |
|----------|----------|
| `deploy-firebase.yml` env input | New `environment: dev|prod`, defaults `prod` ✓ |
| New `db-migrate.yml` | Reusable workflow_call; runs `drizzle-kit push` against env URL ✓ |
| Tag | v4; v3 stays for unmigrated consumers ✓ |
| Migration order | Tag v4 → admin canary → verify → bump CRM and others ✓ |

## Phase 4 UAT closure (out-of-band)

| Scenario | Plan |
|----------|------|
| Clean rebase | Branch on admin repo, dispatch promote-branch.yml@v3 against admin-dev — expect `result=merged` |
| Conflict | Conflicting branch — expect `result=conflict` + Slack thread reply |
| CI failed | Branch with intentional test failure — expect `result=ci_failed` + Slack thread reply |
| Concurrency | Two parallel branches — expect both eventually merge, no work reverted |

Document closure in `07.5-PHASE-4-UAT-CLOSURE.md`. Update `04-HUMAN-UAT.md` status to `resolved`.

---

## Claude's Discretion

- CRDB Cloud cluster name (Mike picks)
- CRDB connection role / username
- Whether to seed prod schema mirror in dev DBs (recommend empty)
- Exact apphosting.yaml env vars beyond DATABASE_URL_DEV (per-app, planner identifies)
- Whether to create `dev` branch in every repo upfront or lazily

## Deferred Ideas

- Per-DB user roles — v3
- Auto-trigger db-migrate on schema.ts diff — v3
- Schema mirroring of prod into dev — out of scope
- Custom DNS on dev backends — out of scope
- Auth bypass on dev backends — out of scope
- /api/platform/health/db — v3
- Seed data per dev DB — manual when needed
- Cross-repo simultaneous v4 bump — incremental migration

## HUMAN actions in this phase (planner emits HUMAN-UAT.md runbook)

1. Provision new CRDB Cloud cluster
2. Apply `scripts/provision-dev-dbs.sql` against the new cluster
3. For each Triarch-owned project: create `dev` branch, create `<app>-dev` FAH backend, set `DATABASE_URL_DEV` secret
4. For each customer-owned project (truth+treason, darksouls, tmi): same as above in customer GCP
5. Tag shared-workflows v4 after admin canary
6. Re-run Phase 4 UAT scenarios against admin-dev; document results
