---
phase: 07.5-dev-cluster-admin-dev-backend
plan: 05
type: execute
wave: 3
depends_on:
  - 07.5-01
  - 07.5-02
  - 07.5-03
  - 07.5-04
files_modified:
  - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md
  - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md
autonomous: true
requirements:
  - ENV-05
user_setup: []

must_haves:
  truths:
    - "07.5-PHASE-4-UAT-CLOSURE.md exists in the phase directory as a fillable runbook with frontmatter (status: pending, target backend: admin-dev, completed: TBD)"
    - "07.5-PHASE-4-UAT-CLOSURE.md contains FOUR scenario sections: Scenario A — Clean Rebase + CI + Merge, Scenario B — Rebase Conflict, Scenario C — CI Failure, Scenario D — Concurrency"
    - "Each scenario section has identical structure: Setup paragraph + Action paragraph + checkbox/results table (Check / Expected / Actual / Pass-Fail columns) + Verify SQL block"
    - "Each scenario's Verify SQL block contains a SELECT against promote_attempts table with the appropriate WHERE clause for that scenario"
    - "Closure Actions section at file end lists the steps Mike performs after all 4 scenarios pass: update 04-HUMAN-UAT.md frontmatter status: deferred → resolved + mark each scenario as result: passed"
    - "07.5-HUMAN-UAT.md exists in the phase directory mirroring the runbook items pulled from 07.5-RUNBOOK.html so /gsd:audit-uat can track them"
    - "07.5-HUMAN-UAT.md contains frontmatter with status: pending and the runbook category breakdown (A. Cluster + DBs, B. FAH backends + secrets, C. shared-workflows v4 tag + canary, D. Phase 4 UAT)"
    - "07.5-HUMAN-UAT.md cross-references the HTML runbook (.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RUNBOOK.html) as the source of truth for live state"
    - "Both files committed to admin repo in a single atomic commit"
    - "Neither file requires Mike to do anything in this plan — they are templates Mike fills in over time as he completes the runbook + UAT"
  artifacts:
    - path: ".planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md"
      provides: "Fillable runbook for the 4 deferred Phase 4 UAT scenarios — exercised against the new admin-dev backend"
      contains: "Scenario A"
      min_lines: 80
    - path: ".planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md"
      provides: "Mirror of HTML runbook items in markdown form for /gsd:audit-uat tracking"
      contains: "status: pending"
      min_lines: 50
  key_links:
    - from: "07.5-PHASE-4-UAT-CLOSURE.md scenario verification SQL"
      to: "promote_attempts table (Phase 4 schema)"
      via: "SELECT branch, result, merge_sha, conflict_files, ci_run_url, created_at FROM promote_attempts WHERE branch = ..."
      pattern: "FROM promote_attempts"
    - from: "07.5-PHASE-4-UAT-CLOSURE.md Closure Actions section"
      to: "04-HUMAN-UAT.md frontmatter status: deferred → resolved"
      via: "Manual edit instructions for Mike to apply after all 4 scenarios pass"
      pattern: "status: resolved"
    - from: "07.5-HUMAN-UAT.md"
      to: "07.5-RUNBOOK.html (existing HTML runbook in same directory)"
      via: "Markdown reference link in the body"
      pattern: "07.5-RUNBOOK.html"
---

<objective>
Create two markdown templates in the phase directory that close ENV-05:

1. **`07.5-PHASE-4-UAT-CLOSURE.md`** — A fillable runbook for the four deferred Phase 4 UAT scenarios (clean rebase, conflict, ci_failed, concurrency). Mike fills it in as he runs each scenario against the new `admin-dev` backend. Drafted verbatim in RESEARCH.md §"07.5-PHASE-4-UAT-CLOSURE.md template structure".

2. **`07.5-HUMAN-UAT.md`** — A markdown mirror of the runbook items currently tracked in `07.5-RUNBOOK.html`. The HTML runbook is the live state source of truth (with persisted checkboxes); this markdown file makes the same items visible to `/gsd:audit-uat` which scans `.planning/phases/<phase>/*-HUMAN-UAT.md` for tracking.

Purpose: Close ENV-05 — provide the verification deliverable for ROADMAP success criterion 5 ("Phase 4 deferred UAT scenarios are exercised against the new admin-dev backend and pass — closes 04-HUMAN-UAT.md"). The CODE deliverables exist in Plans 07.5-01..04. This plan delivers the verification SCAFFOLDING. Mike runs the scenarios and updates both files + 04-HUMAN-UAT.md as he goes.

Output: Two markdown files committed in a single atomic commit. No code changes. No regression risk.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-CONTEXT.md
@.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RESEARCH.md
@.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-VALIDATION.md
@.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RUNBOOK.html
@.planning/phases/04-promote-branch-workflow/04-HUMAN-UAT.md

<interfaces>
<!-- This plan creates two fresh markdown files. No existing schema/types are consumed — only existing runbook content is mirrored. -->

<!-- 07.5-PHASE-4-UAT-CLOSURE.md skeleton (RESEARCH §07.5-PHASE-4-UAT-CLOSURE.md template structure):     -->
<!--   Frontmatter: status, target backend, workflow ref (promote-branch.yml@v3 — no changes), completed -->
<!--   Prerequisites checklist (4 items)                                                                   -->
<!--   Four scenario sections (A, B, C, D) — identical structure                                            -->
<!--   Closure Actions section listing the post-pass steps                                                  -->

<!-- promote_attempts schema (from Phase 4 plan 04-01) — verify queries use these columns:               -->
<!--   id (uuid, primary key)                                                                              -->
<!--   branch (text)                                                                                        -->
<!--   release_id (uuid, fk → release_logs.id)                                                              -->
<!--   workflow_run_url (text, nullable)                                                                    -->
<!--   result (text — 'merged' | 'conflict' | 'ci_failed' — VALID_RESULTS array, no CHECK constraint)      -->
<!--   merge_sha (text, nullable)                                                                           -->
<!--   conflict_files (jsonb, default '[]')                                                                 -->
<!--   ci_run_url (text, nullable)                                                                          -->
<!--   error_message (text, nullable)                                                                       -->
<!--   metadata (jsonb, default '{}')                                                                       -->
<!--   created_at (timestamp default now())                                                                 -->

<!-- 07.5-HUMAN-UAT.md mirror — pull category structure from 07.5-RUNBOOK.html. Categories per VALIDATION:  -->
<!--   A. Cluster + DBs (Runbook A-1, A-2)                                                                  -->
<!--   B. FAH backends + secrets (Runbook B-1..B-6)                                                         -->
<!--   C. shared-workflows v4 tag + canary (Runbook C-1, C-2, C-3)                                          -->
<!--   D. Phase 4 UAT (Runbook D-1..D-5)                                                                    -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 07.5-PHASE-4-UAT-CLOSURE.md fillable runbook with 4 scenarios + verify SQL + closure actions</name>
  <files>.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md</files>
  <read_first>
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RESEARCH.md (§"07.5-PHASE-4-UAT-CLOSURE.md template structure" — paste body)
    - .planning/phases/04-promote-branch-workflow/04-HUMAN-UAT.md (FULL — read end-to-end so the 4 scenarios match the original Phase 4 deferred UAT structure exactly)
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-CONTEXT.md (D-22, D-23, D-24 — UAT execution against admin-dev with promote-branch.yml@v3)
    - src/db/schema.ts (the promote_attempts table definition — confirm column names for the verify SQL queries)
  </read_first>
  <action>
**Working directory: admin repo root.**

Create `.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md`. The body is drafted in RESEARCH.md §"07.5-PHASE-4-UAT-CLOSURE.md template structure". Expand the four scenarios so each has the same structure (RESEARCH only fully filled out Scenario A; mirror its structure for B, C, D).

Full file content:

```markdown
--- 
phase: 07.5-dev-cluster-admin-dev-backend
plan: 05
status: pending
target_backend: admin-dev
workflow_ref: promote-branch.yml@v3
created: 2026-05-05
completed: TBD
--- 

# Phase 4 UAT Closure — Promote Branch Scenarios

> **Status:** pending — Mike fills this in as each scenario runs against the new `admin-dev` backend (CONTEXT D-22..D-24).
>
> **Target backend:** `admin-dev` (FAH default URL — to be captured during RUNBOOK B-1)
>
> **Workflow under test:** `MyAlterLego/shared-workflows/.github/workflows/promote-branch.yml@v3` — NO changes to the workflow itself; Phase 7.5 just provides the dev sandbox.
>
> Closes ROADMAP Phase 7.5 success criterion 5 + ENV-05.

## Prerequisites

- [ ] `admin-dev` backend live and returning 200 on `/api/health` (verify in RUNBOOK B-1)
- [ ] `dev` branch exists in `MyAlterLego/triarch-dev` (the admin repo) and was deployed via the v4 canary
- [ ] `ADMIN_API_TOKEN` Actions secret is set on `MyAlterLego/triarch-dev` (matches `projects.api_key` for the admin project — same secret used by deploy-firebase callbacks)
- [ ] `promote_attempts` table exists in `admin_dev` CRDB (run `drizzle-kit push` via `db-migrate.yml@v4` against admin_dev — RUNBOOK D-0)
- [ ] `shared-workflows@v4` is tagged AND `MyAlterLego/triarch-dev`'s `ci-cd.yml` references `@v4` (RUNBOOK C-2, C-3)

## Scenario A — Clean Rebase + CI + Merge (SC-1)

**Setup:** Create a test branch `test/uat-clean` from `dev` with one or more commits ahead of main, no merge conflicts against main.

**Action:** Dispatch `promote-branch.yml@v3` with `branch=test/uat-clean` via the GitHub Actions UI on `MyAlterLego/triarch-dev`.

| Check | Expected | Actual | Pass/Fail |
|-------|----------|--------|-----------|
| Workflow run result | success | | |
| `result` workflow output | `merged` | | |
| `merge_sha` workflow output | non-empty SHA | | |
| `promote_attempts.result` | `merged` | | |
| `promote_attempts.merge_sha` | matches above | | |
| `promote_attempts.conflict_files` | `[]` | | |
| OttoBot Slack reply | thread on the original Slack approval message; `:white_check_mark: Merged` text | | |

**Verify SQL (run against admin_dev):**

```sql
SELECT branch, result, merge_sha, conflict_files, ci_run_url, error_message, created_at
  FROM promote_attempts
  WHERE branch = 'test/uat-clean'
  ORDER BY created_at DESC LIMIT 1;
```

**Notes:**
-

## Scenario B — Rebase Conflict (SC-2)

**Setup:** Create a test branch `test/uat-conflict` from `dev`. Make a change that conflicts with a recent main commit (e.g. modify the same line of `package.json` "version" that was changed on main since the branch was cut). Push `test/uat-conflict` and let CI run.

**Action:** Dispatch `promote-branch.yml@v3` with `branch=test/uat-conflict`.

| Check | Expected | Actual | Pass/Fail |
|-------|----------|--------|-----------|
| Workflow run result | failure (non-zero exit on rebase step) | | |
| `result` workflow output | `conflict` | | |
| `conflict_files` workflow output | non-empty list (e.g. `["package.json"]`) | | |
| `promote_attempts.result` | `conflict` | | |
| `promote_attempts.conflict_files` | matches output above | | |
| `promote_attempts.merge_sha` | NULL | | |
| OttoBot threaded reply | `:warning: Cannot promote {branch} — conflicts with main: {file list}` | | |

**Verify SQL:**

```sql
SELECT branch, result, conflict_files, merge_sha, error_message, created_at
  FROM promote_attempts
  WHERE branch = 'test/uat-conflict'
  ORDER BY created_at DESC LIMIT 1;
```

**Notes:**
-

## Scenario C — CI Failure (SC-3)

**Setup:** Create a test branch `test/uat-ci-failed` from `dev` with a commit that intentionally fails CI (e.g. add a Vitest test that asserts `expect(true).toBe(false)` or a syntax error in a source file).

**Action:** Dispatch `promote-branch.yml@v3` with `branch=test/uat-ci-failed`.

| Check | Expected | Actual | Pass/Fail |
|-------|----------|--------|-----------|
| Workflow run result | failure (CI job non-zero) | | |
| `result` workflow output | `ci_failed` | | |
| `ci_run_url` workflow output | non-empty URL pointing at the failed CI run | | |
| `promote_attempts.result` | `ci_failed` | | |
| `promote_attempts.ci_run_url` | matches output above | | |
| `promote_attempts.merge_sha` | NULL | | |
| `promote_attempts.conflict_files` | `[]` | | |
| OttoBot threaded reply | `:x: Cannot promote {branch} — CI failed. Run: {url}` | | |

**Verify SQL:**

```sql
SELECT branch, result, ci_run_url, conflict_files, merge_sha, error_message, created_at
  FROM promote_attempts
  WHERE branch = 'test/uat-ci-failed'
  ORDER BY created_at DESC LIMIT 1;
```

**Notes:**
-

## Scenario D — Concurrency (SC-4)

**Setup:** Create TWO test branches from `dev` simultaneously: `test/uat-concurrent-a` and `test/uat-concurrent-b`. Each touches a DIFFERENT file (no cross-conflict). Push both. Let CI complete on both.

**Action:** Approve both branches in the customer page Approve UI back-to-back (within ~5 seconds). Both `promote-branch.yml@v3` runs should kick off. The first to merge updates main; the second auto-rebases on the new main and re-runs CI.

| Check | Expected | Actual | Pass/Fail |
|-------|----------|--------|-----------|
| First workflow result | `merged` | | |
| Second workflow result | `merged` (after auto-rebase + CI) | | |
| Two `promote_attempts` rows | both `result=merged`, distinct `merge_sha` values | | |
| Final main commit | contains BOTH branch's changes | | |
| OttoBot replies | both threads show `:white_check_mark: Merged` | | |
| No work reverted | RC-08 success criterion (no commits lost from either branch) | | |

**Verify SQL:**

```sql
SELECT branch, result, merge_sha, created_at
  FROM promote_attempts
  WHERE branch IN ('test/uat-concurrent-a', 'test/uat-concurrent-b')
  ORDER BY created_at ASC;
-- Expected: 2 rows, both result=merged, distinct merge_sha values, second created_at after first.
```

**Notes:**
-

## Closure Actions (after all 4 scenarios pass)

- [ ] Update `.planning/phases/04-promote-branch-workflow/04-HUMAN-UAT.md` frontmatter: `status: deferred` → `status: resolved`
- [ ] In `04-HUMAN-UAT.md`, mark each scenario as `result: passed` (matching this file's results)
- [ ] Set this file's frontmatter `status: pending` → `status: resolved` and fill in `completed: <date>`
- [ ] Commit both files together with message `docs(07.5): close Phase 4 UAT — all 4 scenarios passed (ENV-05)`
- [ ] Update `.planning/ROADMAP.md` Phase 7.5 status from "Not started" or "In Progress" to "Complete"
- [ ] Update STATE.md with the closure decision (e.g. "Phase 7.5 closes 04-HUMAN-UAT.md against admin-dev backend")

## Cleanup

After closure, delete the four test branches in `MyAlterLego/triarch-dev`:

```bash
cd ~/claude/triarch/development/admin
for branch in test/uat-clean test/uat-conflict test/uat-ci-failed test/uat-concurrent-a test/uat-concurrent-b; do
  git push origin --delete "$branch" 2>/dev/null || true
done
```

The `promote_attempts` rows in `admin_dev` may be retained as historical evidence (no cleanup required for the dev DB).
```

After writing the file, do not commit yet — Task 2 creates the second file and we commit both atomically.
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin && test -f .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md && grep -q "^## Scenario A" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md && grep -q "^## Scenario B" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md && grep -q "^## Scenario C" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md && grep -q "^## Scenario D" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md && grep -q "FROM promote_attempts" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md && grep -q "status: pending" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md && grep -q "target_backend: admin-dev" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md && grep -q "Closure Actions" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md && [ "$(wc -l < .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md | tr -d ' ')" -ge "80" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md` exists
    - File starts with frontmatter containing `status: pending`, `target_backend: admin-dev`, `workflow_ref: promote-branch.yml@v3`
    - File contains FOUR scenario headings: `## Scenario A`, `## Scenario B`, `## Scenario C`, `## Scenario D`
    - Each scenario contains a results table with `Check`, `Expected`, `Actual`, `Pass/Fail` columns
    - Each scenario contains a `Verify SQL` block with `FROM promote_attempts` query
    - Scenario A query filters on `branch = 'test/uat-clean'`
    - Scenario B query filters on `branch = 'test/uat-conflict'`
    - Scenario C query filters on `branch = 'test/uat-ci-failed'`
    - Scenario D query filters on both `test/uat-concurrent-a` and `test/uat-concurrent-b`
    - File contains `Closure Actions` section
    - Closure Actions references updating `04-HUMAN-UAT.md` frontmatter to `status: resolved`
    - File length >= 80 lines
    - No commit yet (Task 2 commits both files together)
  </acceptance_criteria>
  <done>07.5-PHASE-4-UAT-CLOSURE.md fillable runbook exists with 4 scenarios + verify SQL + closure actions; ready for Mike to execute and fill in.</done>
</task>

<task type="auto">
  <name>Task 2: Create 07.5-HUMAN-UAT.md mirroring 07.5-RUNBOOK.html items + atomic commit of both files</name>
  <files>.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RUNBOOK.html (FULL — extract item titles + acceptance criteria from each step block; the HTML is the source of truth, this markdown mirrors it)
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-VALIDATION.md (§"Manual-Only Verifications (HUMAN-UAT)" table — confirms category breakdown A/B/C/D)
    - .planning/phases/04-promote-branch-workflow/04-HUMAN-UAT.md (existing HUMAN-UAT.md format model — frontmatter style, body shape)
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-CONTEXT.md (D-04, D-14, D-22, D-24 — HUMAN action breakdown)
  </read_first>
  <action>
**Working directory: admin repo root.**

Step 1 — Read 07.5-RUNBOOK.html and identify the runbook items by category. Per VALIDATION.md §"Manual-Only Verifications", the items group as:

- **A. Cluster + DBs:** A-1 (provision new CRDB cluster), A-2 (apply provision-dev-dbs.sql)
- **B. FAH backends + secrets:** B-1 admin-dev, B-2 portal-dev, B-3 darksouls-dev, B-4 tmi-dev, B-5 truthtreason-dev, B-6 www-dev (each step creates the backend + sets DATABASE_URL_DEV)
- **C. shared-workflows v4 tag + canary:** C-1 (review feat/v4-environment-input branch), C-2 (push branch + tag v4 after admin canary verifies), C-3 (bump consumer ci-cd.yml refs from @v3 to @v4 — incremental)
- **D. Phase 4 UAT:** D-1 clean, D-2 conflict, D-3 ci_failed, D-4 concurrent, D-5 mark 04-HUMAN-UAT.md resolved

Step 2 — Create the mirror markdown:

```markdown
--- 
phase: 07.5-dev-cluster-admin-dev-backend
plan: 05
status: pending
created: 2026-05-05
runbook_html: .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RUNBOOK.html
--- 

# Phase 7.5 — Human UAT Items

> **Source of truth:** [`07.5-RUNBOOK.html`](./07.5-RUNBOOK.html) — the HTML runbook has persisted checkbox state. This markdown file mirrors the same items so `/gsd:audit-uat` can scan and report against them.
>
> **Status:** pending — Mike marks items complete in the HTML runbook as he executes each one. After all items are complete, this file's frontmatter `status: pending` flips to `status: resolved`.

## A. Cluster + DBs (ENV-01)

- [ ] **A-1: Provision new CockroachDB Cloud cluster** (CONTEXT D-01..D-04)
  - Action: Mike creates a new dedicated CRDB Cloud cluster via the Cockroach Cloud console (recommend name `triarchdev-dev`, region same as prod, smallest Basic tier).
  - Acceptance: cluster URL captured; cluster reachable via `cockroach sql --url <new-cluster-url>`.
- [ ] **A-2: Apply `scripts/provision-dev-dbs.sql` to the new cluster** (CONTEXT D-05..D-08)
  - Action: `cockroach sql --url "<new-cluster-url>" -f scripts/provision-dev-dbs.sql`
  - Acceptance: `SHOW DATABASES;` lists `admin_dev`, `portal_dev`, `darksouls_dev`, `tmi_dev`, `truthtreason_dev`, `www_dev` (six DBs total).

## B. FAH backends + DATABASE_URL_DEV secrets (ENV-02)

> Per CONTEXT D-09: Triarch-owned apps (admin, portal, www) get the `<app>-dev` backend in the SAME Firebase project as prod. Customer-owned apps (truthtreason, darksouls, tmi) get `<app>-dev` backends in the existing customer GCP project. NO new GCP projects.

- [ ] **B-1: Create `admin-dev` FAH backend + set `DATABASE_URL_DEV` secret + update apphosting.yaml NEXTAUTH_URL placeholder**
  - Action: `firebase apphosting:backends:create admin-dev --git-branch=dev` in the admin Firebase project; then `firebase apphosting:secrets:set DATABASE_URL_DEV` (paste the `admin_dev` connection URL from A-1).
  - Then update `apphosting.yaml` `NEXTAUTH_URL` from the placeholder `https://admin-dev.triarch.dev` to the actual FAH default URL (e.g. `https://admin-dev--triarch-dev-website.us-central1.hosted.app` or whatever FAH issues).
  - Acceptance: Backend visible in Firebase Console; `firebase apphosting:secrets:access DATABASE_URL_DEV --project triarch-dev-website` returns the dev connection string; `apphosting.yaml` reflects the real URL.
- [ ] **B-2: Create `portal-dev` FAH backend + set `DATABASE_URL_DEV` secret**
  - Same flow as B-1, applied to the triarchsecurity-portal Firebase project against `portal_dev` DB.
- [ ] **B-3: Create `darksouls-dev` FAH backend + set `DATABASE_URL_DEV` secret**
  - Same flow, customer GCP project, `darksouls_dev` DB.
- [ ] **B-4: Create `tmi-dev` FAH backend + set `DATABASE_URL_DEV` secret**
  - Same flow, customer GCP project, `tmi_dev` DB.
- [ ] **B-5: Create `truthtreason-dev` FAH backend + set `DATABASE_URL_DEV` secret**
  - Same flow, customer GCP project, `truthtreason_dev` DB.
- [ ] **B-6: Create `www-dev` FAH backend + set `DATABASE_URL_DEV` secret** (or skip if `www` is a static site without DB)
  - Per RESEARCH §Pitfall 6: if www does not use Drizzle, skip the DATABASE_URL_DEV step but still create the FAH dev backend if dev preview is desired.

## C. shared-workflows v4 tag + canary (ENV-03)

- [ ] **C-1: Review feat/v4-environment-input branch in shared-workflows**
  - Action: Mike reviews the two commits on `~/claude/MyAlterLego/shared-workflows` `feat/v4-environment-input` branch (deploy-firebase.yml v4 + new db-migrate.yml from Plan 07.5-02).
  - Acceptance: `actionlint .github/workflows/*.yml` clean; commits look correct.
- [ ] **C-2: Push feat branch + tag v4 (after admin canary verifies)** (CONTEXT D-20, RESEARCH §Pitfall 3, §Pitfall 8)
  - Action: After admin canary deploy succeeds (i.e. admin-dev backend serves a successful build from `dev` branch using v4 of shared-workflows), Mike runs:
    ```bash
    cd ~/claude/MyAlterLego/shared-workflows
    git push origin feat/v4-environment-input
    # Open + merge PR
    git checkout main && git pull
    git tag v4 <merged-commit-sha>
    git push origin v4
    ```
  - Acceptance: `git tag -l | grep v4` succeeds; `git tag -l | grep v3` still points at the v3 commit (unchanged — RESEARCH §Pitfall 8).
- [ ] **C-3: Bump consumer `ci-cd.yml` refs from @v3 to @v4 (incremental)** (CONTEXT Specifics, deferred section)
  - Order: admin first (canary, already done in C-2). Then CRM. Then portal. Then customer-owned (truthtreason, darksouls, tmi, www) one at a time.
  - Acceptance: Each consumer's `ci-cd.yml` references `@v4`; first dev deploy on each consumer succeeds.

## D. Phase 4 UAT closure (ENV-05)

> Items D-1..D-4 are documented in detail in [`07.5-PHASE-4-UAT-CLOSURE.md`](./07.5-PHASE-4-UAT-CLOSURE.md). Use that file as the working scratchpad; flip checkboxes here as each scenario passes.

- [ ] **D-0: Pre-req — drizzle-kit push schema to admin_dev**
  - Action: Run `db-migrate.yml@v4` with `environment: dev`, `firebase_project_id: triarch-dev-website`, `consumer_repo: MyAlterLego/triarch-dev`. Or manually: `firebase apphosting:secrets:access DATABASE_URL_DEV --project triarch-dev-website | DATABASE_URL=$(cat) npx drizzle-kit push`.
  - Acceptance: `promote_attempts` table exists in admin_dev; query returns 0 rows initially.
- [ ] **D-1: Scenario A — Clean Rebase + CI + Merge** (see PHASE-4-UAT-CLOSURE.md Scenario A)
- [ ] **D-2: Scenario B — Rebase Conflict** (see PHASE-4-UAT-CLOSURE.md Scenario B)
- [ ] **D-3: Scenario C — CI Failure** (see PHASE-4-UAT-CLOSURE.md Scenario C)
- [ ] **D-4: Scenario D — Concurrency** (see PHASE-4-UAT-CLOSURE.md Scenario D)
- [ ] **D-5: Mark 04-HUMAN-UAT.md resolved** (CONTEXT D-24)
  - Action: Update `.planning/phases/04-promote-branch-workflow/04-HUMAN-UAT.md` frontmatter `status: deferred` → `status: resolved`; mark each scenario `result: passed`.
  - Acceptance: `04-HUMAN-UAT.md` frontmatter shows `status: resolved`.

## Closure (when all checkboxes above are checked)

- [ ] Flip THIS file's frontmatter `status: pending` → `status: resolved`
- [ ] Flip `07.5-PHASE-4-UAT-CLOSURE.md` frontmatter `status: pending` → `status: resolved`
- [ ] Update `.planning/ROADMAP.md` Phase 7.5 row to `Complete`
- [ ] Append closure entry to `.planning/STATE.md` Decisions block
```

Step 3 — Single atomic commit for both files (Task 1 file + this file):

```bash
cd /Users/mikegeehan/claude/triarch/development/admin
git add .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md \
        .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md
git commit -m "docs(07.5): add Phase 4 UAT closure runbook + HUMAN-UAT mirror (ENV-05)"
```
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin && test -f .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "status: pending" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "07.5-RUNBOOK.html" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "## A. Cluster + DBs" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "## B. FAH backends" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "## C. shared-workflows v4" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "## D. Phase 4 UAT" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "A-1" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "A-2" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -cE "^- \[ \] \*\*B-[1-6]" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md | grep -qx 6 && grep -q "C-1" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "C-2" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "C-3" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && grep -q "D-5" .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md && [ "$(wc -l < .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md | tr -d ' ')" -ge "50" ] && git log -1 --pretty=%s | grep -q "07.5.*UAT.*ENV-05"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md` exists
    - Frontmatter contains `status: pending` and a reference to `07.5-RUNBOOK.html`
    - File contains FOUR top-level category headings: `## A. Cluster + DBs`, `## B. FAH backends + DATABASE_URL_DEV secrets`, `## C. shared-workflows v4 tag + canary`, `## D. Phase 4 UAT closure`
    - Section A contains items A-1 (cluster provisioning) and A-2 (apply provision-dev-dbs.sql)
    - Section B contains EXACTLY six items B-1..B-6 (one per project: admin, portal, darksouls, tmi, truthtreason, www)
    - Section C contains items C-1, C-2, C-3 (review feat branch, push + tag v4 after canary, bump consumer refs)
    - Section D contains items D-0 (drizzle push prereq), D-1..D-4 (four scenarios), D-5 (mark 04-HUMAN-UAT.md resolved)
    - File length >= 50 lines
    - Single commit covers BOTH `07.5-PHASE-4-UAT-CLOSURE.md` AND `07.5-HUMAN-UAT.md`
    - Commit message matches `docs(07.5): add Phase 4 UAT closure runbook.*ENV-05`
  </acceptance_criteria>
  <done>07.5-HUMAN-UAT.md mirrors the runbook items in markdown for /gsd:audit-uat tracking; both UAT files committed atomically. Mike marks items complete in 07.5-RUNBOOK.html as he executes them; this markdown stays in sync via manual checkbox flips at closure time.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md` returns 0
- `test -f .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md` returns 0
- `grep -c "^## Scenario" 07.5-PHASE-4-UAT-CLOSURE.md` returns 4
- `grep -cE "^- \[ \] \*\*B-[1-6]" 07.5-HUMAN-UAT.md` returns 6
- `git log -1 --pretty=%s` matches `docs(07.5): add Phase 4 UAT closure.*ENV-05`
- `git show --stat HEAD` shows BOTH files in one commit
</verification>

<success_criteria>
- ENV-05 (verification scaffolding): both 07.5-PHASE-4-UAT-CLOSURE.md (fillable Phase 4 UAT runbook) and 07.5-HUMAN-UAT.md (RUNBOOK.html mirror for /gsd:audit-uat) exist; Mike fills both in over time as he completes the runbook + UAT scenarios; closure flow documented (mark 04-HUMAN-UAT.md resolved + ROADMAP Phase 7.5 Complete + STATE.md decision entry).
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-05-SUMMARY.md` documenting:
- Both files exist (paths)
- File line counts
- Confirmation 4 scenarios + Closure Actions present in PHASE-4-UAT-CLOSURE.md
- Confirmation 4 categories (A/B/C/D) + 6 backend items + 5 UAT scenarios + closure section present in HUMAN-UAT.md
- Single atomic commit SHA
- Reminder: Mike fills these in over time; this plan only delivers the templates
</output>
