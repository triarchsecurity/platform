---
phase: 07.5-dev-cluster-admin-dev-backend
plan: 04
type: execute
wave: 2
depends_on:
  - 07.5-02
files_modified:
  - docs/onboarding-projects.md
autonomous: true
requirements:
  - ENV-04
user_setup: []

must_haves:
  truths:
    - "docs/onboarding-projects.md gains a new Step 11 — Dev Environment (apphosting.yaml overlay + db-migrate.yml) section appended after the existing final step but BEFORE the closing 'Verification Checklist' block"
    - "Step 11 documents the apphosting.yaml + apphosting.prod.yaml convention with the side-by-side comparison table from RESEARCH.md §docs/onboarding-projects.md Step 11 content"
    - "Step 11 includes both YAML examples (dev base + prod overlay) so a new project owner can copy-paste with minimal edits"
    - "Step 11 explicitly warns: apphosting.prod.yaml must be a COMPLETE config, not a partial diff (RESEARCH §Pitfall 1) — omitting NODE_AUTH_TOKEN breaks npm ci on prod builds"
    - "Step 11 documents how to invoke db-migrate.yml from a consumer's ci-cd.yml (workflow_call usage example) AND how to run it manually via the GitHub Actions UI workflow_dispatch"
    - "Step 11 documents WHEN to run db-migrate.yml: after merging any PR that changes src/db/schema.ts to the dev branch, before testing anything that reads/writes new columns"
    - "Step 11 documents WHY db-migrate.yml replaces the manual `firebase apphosting:secrets:access | psql` ritual"
    - "Step 11 documents how to set DATABASE_URL_DEV secret on a new dev backend: `firebase apphosting:secrets:set DATABASE_URL_DEV --project <fb_project_id>`"
    - "Step 11 caveat: db-migrate.yml is for repos using Drizzle ORM only — repos without drizzle.config.ts (e.g. www static sites) should NOT call this workflow (RESEARCH §Pitfall 6)"
  artifacts:
    - path: "docs/onboarding-projects.md"
      provides: "Onboarding runbook now covers dev environment overlay convention + db-migrate.yml usage"
      contains: "Step 11"
  key_links:
    - from: "docs/onboarding-projects.md Step 11"
      to: "apphosting.yaml + apphosting.prod.yaml convention (Plan 07.5-03 admin reference implementation)"
      via: "Side-by-side comparison table + two YAML examples"
      pattern: "apphosting.prod.yaml"
    - from: "docs/onboarding-projects.md Step 11d"
      to: "MyAlterLego/shared-workflows/.github/workflows/db-migrate.yml@v4 (Plan 07.5-02)"
      via: "workflow_call uses: directive in onboarding example"
      pattern: "shared-workflows/.github/workflows/db-migrate.yml@v4"
    - from: "Step 11 pitfall callout"
      to: "RESEARCH §Pitfall 1 (apphosting.prod.yaml is COMPLETE, not partial)"
      via: "Critical-warning block in body text"
      pattern: "COMPLETE config"
    - from: "Step 11e"
      to: "DATABASE_URL_DEV Firebase secret"
      via: "firebase apphosting:secrets:set command"
      pattern: "apphosting:secrets:set DATABASE_URL_DEV"
---

<objective>
Append a new "Step 11 — Dev Environment (apphosting.yaml overlay + db-migrate.yml)" section to `docs/onboarding-projects.md`, generalizing the dev-environment convention from Plans 07.5-01..03 into reusable runbook guidance for any future project. The section is drafted verbatim in RESEARCH.md §"docs/onboarding-projects.md Step 11 content".

Purpose: Close the documentation portion of ENV-04 — "the truth+treason apphosting.yaml + apphosting.prod.yaml overlay pattern is generalized into convention and documented for all projects" (ROADMAP Phase 7.5 success criterion 4).

Output: docs/onboarding-projects.md modified with a new Step 11 section. Single atomic commit.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-CONTEXT.md
@.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RESEARCH.md
@.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-VALIDATION.md
@docs/onboarding-projects.md

<interfaces>
<!-- This plan modifies an existing markdown runbook, not code. -->
<!-- The new Step 11 references — but does not modify — the artifacts produced by other plans: -->
<!--   - apphosting.yaml + apphosting.prod.yaml (Plan 07.5-03)                                   -->
<!--   - shared-workflows/.github/workflows/db-migrate.yml@v4 (Plan 07.5-02)                     -->
<!--   - DATABASE_URL_DEV Firebase secret (set per-backend by HUMAN in RUNBOOK B-1)              -->

<!-- Step 11 body is drafted verbatim in RESEARCH.md §"docs/onboarding-projects.md Step 11 content".  -->
<!-- Sub-steps:                                                                                       -->
<!--   11a — Create apphosting.yaml (dev base)                                                        -->
<!--   11b — Create apphosting.prod.yaml (prod overrides)  with COMPLETE-config warning               -->
<!--   11c — Verify the overlay convention (yamllint)                                                 -->
<!--   11d — Wire db-migrate.yml in ci-cd.yml (optional — run manually first)                         -->
<!--   11e — Set DATABASE_URL_DEV secret on dev backend                                                -->
<!--   When-to-use guidance closing paragraph                                                          -->

<!-- This plan does NOT depend on Plan 07.5-03 to be complete — it's documentation that DESCRIBES the -->
<!-- convention. It can run in parallel (Wave 2) — depends_on: [] in frontmatter.                       -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Step 11 — Dev Environment section to docs/onboarding-projects.md</name>
  <files>docs/onboarding-projects.md</files>
  <read_first>
    - docs/onboarding-projects.md (read end-to-end so the insertion point is correct — Step 11 goes BEFORE the existing "Verification Checklist" block; if the doc has changed since 07-06, identify the actual final step number and append after it as Step N+1, renaming Step 11 if needed)
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RESEARCH.md (§"docs/onboarding-projects.md Step 11 content" — paste verbatim)
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-CONTEXT.md (D-15, D-17, D-19 — overlay + db-migrate convention)
  </read_first>
  <action>
**Working directory: admin repo root.**

Step 1 — Determine the actual final step number in `docs/onboarding-projects.md`. The CONTEXT calls this "Step 11" — that's the intended number assuming the file currently ends at Step 10. Read the file and confirm the highest existing `## Step N` heading.

```bash
grep -nE "^## Step [0-9]+" docs/onboarding-projects.md | tail -5
# Expect last match to be Step 10 (or whatever the current latest step is). Use N+1 for the new step.
```

If the file's highest step is NOT 10, use `Step <N+1>` consistently throughout the new section instead of "Step 11". The CONTEXT and RESEARCH text use "Step 11" for the recommended number; adjust if reality differs.

Step 2 — Locate the insertion point. The new step must appear AFTER the final existing step but BEFORE the existing closing matter. Looking at the current doc tail (read in `read_first`), the order is:

1. ... existing Step 10 / latest step ...
2. `## Verification Checklist` (general checklist)
3. `## Troubleshooting` (table)

Insert the new Step 11 section BETWEEN the final existing step and the `## Verification Checklist` heading.

Step 3 — Append the new section. Body text drafted verbatim in RESEARCH §"docs/onboarding-projects.md Step 11 content":

```markdown
## Step 11 — Dev Environment (apphosting.yaml overlay + db-migrate.yml)

Every Triarch project has two Firebase App Hosting configs:

| File | Used by | FAH backend | DB secret |
|------|---------|------------|----------|
| `apphosting.yaml` | `deploy-firebase.yml@v4` with `environment: dev` | `<app>-dev` | `DATABASE_URL_DEV` |
| `apphosting.prod.yaml` | `deploy-firebase.yml@v4` with `environment: prod` (default) | `<app>` | `DATABASE_URL` |

The convention generalizes the pattern first established in `truthtreason/apphosting.yaml` + `truthtreason/apphosting.prod.yaml` and now used in admin (see `apphosting.yaml` + `apphosting.prod.yaml` at the admin repo root for a working reference).

### 11a. Create `apphosting.yaml` (dev base)

Copy the project's existing `apphosting.yaml`. Make these changes:

1. Switch the `DATABASE_URL` secret reference from `DATABASE_URL` to `DATABASE_URL_DEV`.
2. Adjust `NEXTAUTH_URL` to the dev FAH URL (`<app-name>-dev.<region>.hosted.app` pattern, captured at backend creation time).
3. Adjust any other URL-bearing env values (`DEPLOY_WEBHOOK_URL`, etc.) to point at the dev host.
4. Lower `runConfig` to dev sizing if desired (e.g. `concurrency: 10`, `minInstances: 0`).

Example (admin):

```yaml
runConfig:
  runtime: nodejs22
  concurrency: 10
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 2

env:
  - variable: NEXTAUTH_URL
    value: https://admin-dev.triarch.dev
    availability:
      - BUILD
      - RUNTIME

  - variable: DATABASE_URL
    secret: DATABASE_URL_DEV

  - variable: NODE_AUTH_TOKEN
    secret: GITHUB_PACKAGES_TOKEN
    availability:
      - BUILD
  # ... all other secrets identical to apphosting.prod.yaml ...
```

### 11b. Create `apphosting.prod.yaml` (prod overrides)

Duplicate the FINAL prod config (every field from the original `apphosting.yaml`). Keep `DATABASE_URL` secret name. Keep prod `NEXTAUTH_URL`. Set prod `runConfig` (e.g. `concurrency: 20`, `minInstances: 1` for warm starts if needed).

> **Critical (RESEARCH §Pitfall 1):** `apphosting.prod.yaml` must be a COMPLETE config, not a partial diff. The Firebase CLI uses the `--config` file EXCLUSIVELY when specified — it does NOT merge with `apphosting.yaml`. Omitting `NODE_AUTH_TOKEN` (with `availability: [BUILD]`) from the prod file will break `npm ci` on prod builds with `E401 Unauthorized` against GitHub Packages. Every secret declared in `apphosting.yaml` must also appear in `apphosting.prod.yaml`.

### 11c. Verify the overlay convention

```bash
# Dry-run: check both files parse as valid YAML
yamllint -d "{rules: {line-length: disable, document-start: disable, truthy: disable}}" \
  apphosting.yaml apphosting.prod.yaml

# Quick sanity diff: only DATABASE_URL secret name + NEXTAUTH_URL should differ
diff <(grep "secret:" apphosting.yaml | sort) <(grep "secret:" apphosting.prod.yaml | sort)
# Expect: ONE difference — DATABASE_URL_DEV in dev vs DATABASE_URL in prod
```

### 11d. Wire `db-migrate.yml` in `ci-cd.yml` (optional — run manually first)

To run schema migrations against the dev DB without touching prod, use the new reusable workflow:

```yaml
# In .github/workflows/ci-cd.yml, add a dispatch-only job:
  db-migrate-dev:
    uses: MyAlterLego/shared-workflows/.github/workflows/db-migrate.yml@v4
    with:
      environment: dev
      firebase_project_id: <your-firebase-project-id>
      consumer_repo: ${{ github.repository }}
    secrets:
      FIREBASE_SA_KEY: ${{ secrets.FIREBASE_SA_KEY }}
      GH_PAT: ${{ secrets.GH_PAT }}
```

Or trigger it manually via the GitHub Actions UI: navigate to **Actions → DB Migrate → Run workflow**, select `dev` for `environment`, paste the Firebase project ID and consumer repo, then dispatch.

**When to use:** After merging any PR that changes `src/db/schema.ts` to the `dev` branch, run `db-migrate.yml@v4` with `environment: dev` BEFORE testing anything that reads/writes the new columns or tables. This replaces the manual `firebase apphosting:secrets:access DATABASE_URL --project X | psql -f` ritual that previously appeared in Step 8.

**Caveat (RESEARCH §Pitfall 6):** `db-migrate.yml` is for repos that use Drizzle ORM only. The workflow runs `npx drizzle-kit push` and requires a `drizzle.config.ts` at the consumer repo root. Static-site repos (e.g. `www`) without Drizzle should NOT call this workflow.

### 11e. Set `DATABASE_URL_DEV` secret on the dev backend

After Mike has provisioned the dev cluster + per-app database (per `07.5-RUNBOOK.html` step A-2), set the connection string as a Firebase secret on the dev FAH backend:

```bash
firebase apphosting:secrets:set DATABASE_URL_DEV --project <your-firebase-project-id>
# Paste the dev cluster connection URL for <app>_dev database
# Example: postgresql://<user>:<password>@<dev-cluster-host>:26257/<app>_dev?sslmode=require
```

Set `DATABASE_URL` separately on the prod backend (this likely already exists from project bring-up):

```bash
firebase apphosting:secrets:set DATABASE_URL --project <your-firebase-project-id>
# Paste the PROD cluster connection URL for <app> database (unchanged from Step 8)
```

### Cost note

Standing up a dev cluster + per-app dev backends adds approximately $50–100/month across all Triarch projects (smallest CRDB Cloud tier + minimal FAH minInstances). Document this on customer-managed projects so the marginal cost is understood before adoption.
```

After inserting the section, lint and commit:

```bash
# Optional markdown lint
mdl docs/onboarding-projects.md 2>/dev/null || true

git add docs/onboarding-projects.md
git commit -m "docs(onboarding): add Step 11 — dev environment overlay + db-migrate.yml (ENV-04)"
```
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin && grep -q "^## Step 11" docs/onboarding-projects.md && grep -q "Dev Environment (apphosting.yaml overlay" docs/onboarding-projects.md && grep -q "apphosting.prod.yaml" docs/onboarding-projects.md && grep -q "db-migrate.yml" docs/onboarding-projects.md && grep -q "COMPLETE config" docs/onboarding-projects.md && grep -q "DATABASE_URL_DEV" docs/onboarding-projects.md && grep -q "shared-workflows/.github/workflows/db-migrate.yml@v4" docs/onboarding-projects.md && grep -q "apphosting:secrets:set DATABASE_URL_DEV" docs/onboarding-projects.md && grep -q "drizzle.config.ts" docs/onboarding-projects.md && [ "$(awk '/^## Step 11/{p=1} /^## Verification Checklist/{p=0} p' docs/onboarding-projects.md | wc -l | tr -d ' ')" -gt "30" ] && git log -1 --pretty=%s | grep -q "onboarding.*Step 11.*ENV-04"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/onboarding-projects.md` contains a new heading `## Step 11` (or `## Step <N+1>` if file has more than 10 prior steps)
    - The Step 11 heading text contains `Dev Environment (apphosting.yaml overlay`
    - Section appears BEFORE the existing `## Verification Checklist` block (verified by grep line numbers)
    - Section contains the side-by-side comparison table with both `apphosting.yaml` (dev base) and `apphosting.prod.yaml` (prod overrides) rows
    - Section contains the explicit warning text `COMPLETE config` (RESEARCH §Pitfall 1 prevention)
    - Section contains a `MyAlterLego/shared-workflows/.github/workflows/db-migrate.yml@v4` reference (the v4 ref consumers will use)
    - Section contains the literal string `apphosting:secrets:set DATABASE_URL_DEV` (the secret-set command for dev backend)
    - Section contains the literal string `drizzle.config.ts` (Pitfall 6 caveat)
    - Section contains `DATABASE_URL_DEV` (the dev secret name)
    - Section length is at least 30 lines between `## Step 11` heading and the next `##` heading (substantive content, not a stub)
    - Single commit on current branch with message matching `docs(onboarding): add Step 11.*ENV-04`
  </acceptance_criteria>
  <done>docs/onboarding-projects.md gains a new Step 11 covering the apphosting.yaml + apphosting.prod.yaml convention, db-migrate.yml usage (workflow_call + workflow_dispatch), DATABASE_URL_DEV secret setup, and the Pitfall 1 (complete config) + Pitfall 6 (Drizzle-only) warnings. Single atomic commit.</done>
</task>

</tasks>

<verification>
- `grep -c "^## Step 11" docs/onboarding-projects.md` returns 1
- `awk '/^## Step 11/{p=1; n=NR} /^## Verification Checklist/{print NR; exit} END{print n}' docs/onboarding-projects.md` shows Step 11 line < Verification Checklist line
- `git log -1 --pretty=%s` matches `docs(onboarding): add Step 11.*ENV-04`
- `git show --stat HEAD` shows ONLY `docs/onboarding-projects.md` modified
</verification>

<success_criteria>
- ENV-04 (documentation portion): Step 11 published in docs/onboarding-projects.md covering apphosting.yaml convention + db-migrate.yml usage + critical Pitfall 1 + DATABASE_URL_DEV setup steps. The convention is now reusable across all future projects (truth+treason, customer-owned + Triarch-owned).
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-04-SUMMARY.md` documenting:
- Step number used (Step 11 or Step <N+1> if doc had more than 10 prior steps)
- Approximate line count of new section
- Confirmation it appears between the prior final step and `## Verification Checklist`
- Confirmation Pitfall 1 + Pitfall 6 warnings are present
- Commit SHA + message
</output>
