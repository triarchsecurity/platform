---
phase: 07.5-dev-cluster-admin-dev-backend
plan: 03
type: execute
wave: 2
depends_on:
  - 07.5-02
files_modified:
  - apphosting.yaml
  - apphosting.prod.yaml
autonomous: true
requirements:
  - ENV-02
  - ENV-04
user_setup: []

must_haves:
  truths:
    - "Pre-existing apphosting.yaml is preserved as apphosting.prod.yaml VERBATIM (no fields lost) — moved BEFORE creating the new dev-base apphosting.yaml so the prod config never disappears at any point in the file history (RESEARCH §Pitfall 1)"
    - "apphosting.prod.yaml is a COMPLETE config with ALL fields from the original apphosting.yaml — runConfig + every env entry + every secret (RESEARCH §Pitfall 1: --config flag selects file wholesale, does not merge)"
    - "apphosting.prod.yaml contains DATABASE_URL secret reference (NOT DATABASE_URL_DEV) — prod cluster connection unchanged"
    - "apphosting.prod.yaml NEXTAUTH_URL is https://admin.triarch.dev (prod URL — unchanged from original)"
    - "apphosting.prod.yaml NODE_AUTH_TOKEN secret reference with availability: [BUILD] is preserved (RESEARCH §Pitfall 1: omitting this fails npm ci)"
    - "New apphosting.yaml exists as the dev base config — references DATABASE_URL_DEV instead of DATABASE_URL"
    - "New apphosting.yaml NEXTAUTH_URL is the dev URL placeholder https://admin-dev.triarch.dev (Mike updates to actual FAH hosted.app URL during RUNBOOK B-1; planner records it as a known follow-up — RESEARCH §Pitfall 4)"
    - "New apphosting.yaml DEPLOY_WEBHOOK_URL points at the dev host (https://admin-dev.triarch.dev/api/webhooks/github-deploy)"
    - "New apphosting.yaml retains ALL non-DB-secret fields from the original apphosting.yaml (NODE_AUTH_TOKEN, NEXTAUTH_SECRET, GOOGLE_CLIENT_ID/SECRET, DEPLOY_WEBHOOK_SECRET, all SLACK_* secrets, all GITHUB_APP_* secrets, SLACK_RELEASE_APPROVAL_CHANNEL, ADMIN_EMAIL)"
    - "Both files parse as valid YAML (yamllint clean)"
    - "After overlay change, `npx vitest run` regression remains 126+/126+ GREEN (overlay change is YAML-only; runtime code untouched)"
    - "After overlay change, `npx next build` succeeds — proves the dev-base apphosting.yaml does not break the build path"
    - "Both files committed in admin repo with a single atomic commit"
  artifacts:
    - path: "apphosting.yaml"
      provides: "Dev base App Hosting config (DATABASE_URL_DEV secret, dev NEXTAUTH_URL, used by deploy-firebase.yml@v4 environment=dev)"
      contains: "DATABASE_URL_DEV"
    - path: "apphosting.prod.yaml"
      provides: "Prod overlay App Hosting config (COMPLETE config; DATABASE_URL secret, prod NEXTAUTH_URL, used by deploy-firebase.yml@v4 environment=prod with --config apphosting.prod.yaml)"
      contains: "DATABASE_URL"
  key_links:
    - from: "apphosting.yaml DATABASE_URL variable"
      to: "DATABASE_URL_DEV Firebase secret"
      via: "secret: DATABASE_URL_DEV mapping line"
      pattern: "secret: DATABASE_URL_DEV"
    - from: "apphosting.prod.yaml DATABASE_URL variable"
      to: "DATABASE_URL Firebase secret (existing prod secret)"
      via: "secret: DATABASE_URL mapping line"
      pattern: "secret: DATABASE_URL$"
    - from: "deploy-firebase.yml@v4 environment=prod"
      to: "apphosting.prod.yaml"
      via: "firebase apphosting:rollouts:create --config apphosting.prod.yaml flag (Plan 07.5-02)"
      pattern: "apphosting.prod.yaml"
    - from: "Both files runConfig + env arrays"
      to: "Identical superset structure (verified by diff: only DATABASE_URL secret name + NEXTAUTH_URL + DEPLOY_WEBHOOK_URL differ)"
      via: "RESEARCH §Pitfall 1 prevention — both files are complete configs"
      pattern: "NODE_AUTH_TOKEN"
---

<objective>
Split the admin repo's single `apphosting.yaml` into a dev-base + prod-overlay pair per CONTEXT D-15/D-16, generalizing the truth+treason pattern that's drafted verbatim in RESEARCH.md §"Admin apphosting.yaml (dev base, after overlay split)" and §"Admin apphosting.prod.yaml (prod overrides — COMPLETE, not partial)".

Two file changes:
1. Rename current `apphosting.yaml` → `apphosting.prod.yaml` (preserves the live prod config — `DATABASE_URL` secret, prod `NEXTAUTH_URL`, all existing fields).
2. Create new `apphosting.yaml` as the dev base — switches `DATABASE_URL` secret reference to `DATABASE_URL_DEV`, sets `NEXTAUTH_URL` to the dev URL placeholder, retains every other field unchanged.

**Critical pitfall guard (RESEARCH §Pitfall 1):** `apphosting.prod.yaml` must be a COMPLETE config containing every field from the original `apphosting.yaml` — `runConfig` + every env entry + every secret. The Firebase CLI `--config <file>` flag uses the file wholesale; it does NOT merge with `apphosting.yaml`. Omitting `NODE_AUTH_TOKEN` (with `availability: [BUILD]`) from the prod file will fail `npm ci` on prod builds with `E401 Unauthorized` against GitHub Packages.

Purpose: Close ENV-04 (admin-side overlay) and prepare ENV-02 (admin-dev backend). After this plan ships, deploy-firebase.yml@v4 (Plan 07.5-02) can route prod deploys via `--config apphosting.prod.yaml` while dev deploys use the default `apphosting.yaml`.

Output: Two committed files (`apphosting.yaml`, `apphosting.prod.yaml`) in the admin repo. `npx vitest run` 126+/126+ GREEN regression. `npx next build` succeeds.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-CONTEXT.md
@.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RESEARCH.md
@.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-VALIDATION.md
@apphosting.yaml

<interfaces>
<!-- This plan modifies the admin repo apphosting.yaml structure.                                            -->
<!-- The original apphosting.yaml file (78 lines) currently exists with prod-only configuration:             -->
<!--   - runConfig: nodejs22, concurrency 10, cpu 1, memoryMiB 512, minInstances 0, maxInstances 2          -->
<!--   - NEXTAUTH_URL value: https://admin.triarch.dev (BUILD+RUNTIME)                                       -->
<!--   - ADMIN_EMAIL value: mike@triarchsecurity.com (RUNTIME)                                               -->
<!--   - DEPLOY_WEBHOOK_URL value: https://admin.triarch.dev/api/webhooks/github-deploy (RUNTIME)            -->
<!--   - DATABASE_URL → secret: DATABASE_URL                                                                 -->
<!--   - NEXTAUTH_SECRET, GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET → secrets                                   -->
<!--   - NODE_AUTH_TOKEN → secret: GITHUB_PACKAGES_TOKEN with availability: [BUILD]                          -->
<!--   - DEPLOY_WEBHOOK_SECRET → secret                                                                      -->
<!--   - SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, SLACK_PAYLOAD_SECRET → secrets                               -->
<!--   - SLACK_RELEASE_APPROVAL_CHANNEL value: "#release-approvals" (RUNTIME)                                -->
<!--   - GITHUB_APP_ID, GITHUB_APP_PRIVATE_KEY, GITHUB_APP_INSTALLATION_ID → secrets                         -->

<!-- After this plan: apphosting.prod.yaml = identical content (verbatim move). -->
<!-- After this plan: apphosting.yaml (NEW) = same shape, with three differences:               -->
<!--   1. DATABASE_URL → secret: DATABASE_URL_DEV (CONTEXT D-11)                                -->
<!--   2. NEXTAUTH_URL value: https://admin-dev.triarch.dev (placeholder; Mike updates in B-1)  -->
<!--   3. DEPLOY_WEBHOOK_URL value: https://admin-dev.triarch.dev/api/webhooks/github-deploy    -->

<!-- Test/build expectations (regression — overlay change must not break runtime code): -->
<!--   - npx vitest run: 126+/126+ GREEN (current count is 126; new tests may have landed)         -->
<!--   - npx next build: must succeed                                                                -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move apphosting.yaml → apphosting.prod.yaml VERBATIM (preserve prod config first)</name>
  <files>apphosting.prod.yaml</files>
  <read_first>
    - apphosting.yaml (FULL 78 lines — read end-to-end so the move preserves every line)
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RESEARCH.md (§"Admin apphosting.prod.yaml (prod overrides — COMPLETE, not partial)" + §Pitfall 1 — read carefully; this is the file that the v4 prod path uses exclusively)
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-CONTEXT.md (D-15, D-16 — overlay convention)
  </read_first>
  <action>
**Working directory: admin repo root.**

Step 1 — Use git mv to preserve history of the prod config (also makes the rename visible in git log):

```bash
cd /Users/mikegeehan/claude/triarch/development/admin
git mv apphosting.yaml apphosting.prod.yaml
```

Step 2 — Verify the moved file's content is BYTE-IDENTICAL to the original. The current file (78 lines) already contains the live prod config:
- runConfig: nodejs22, concurrency 10, cpu 1, memoryMiB 512, minInstances 0, maxInstances 2
- NEXTAUTH_URL value: `https://admin.triarch.dev` (BUILD+RUNTIME)
- ADMIN_EMAIL value: `mike@triarchsecurity.com` (RUNTIME)
- DEPLOY_WEBHOOK_URL value: `https://admin.triarch.dev/api/webhooks/github-deploy` (RUNTIME)
- All secret references (DATABASE_URL → secret DATABASE_URL, NEXTAUTH_SECRET, GOOGLE_CLIENT_ID/SECRET, NODE_AUTH_TOKEN → secret GITHUB_PACKAGES_TOKEN with availability: [BUILD], DEPLOY_WEBHOOK_SECRET, SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET, SLACK_PAYLOAD_SECRET, GITHUB_APP_ID, GITHUB_APP_PRIVATE_KEY, GITHUB_APP_INSTALLATION_ID)
- SLACK_RELEASE_APPROVAL_CHANNEL value `"#release-approvals"` (RUNTIME)

Per RESEARCH §Pitfall 1: `apphosting.prod.yaml` must be a complete config — every field present in the original. Since this is a verbatim git mv, all fields are preserved automatically. Do NOT edit the moved file in this task.

Step 3 — Lint the renamed file:

```bash
yamllint -d "{rules: {line-length: disable, document-start: disable, truthy: disable}}" apphosting.prod.yaml
```

Step 4 — Quick self-check (must show DATABASE_URL secret, NOT DATABASE_URL_DEV):

```bash
grep -n "DATABASE_URL" apphosting.prod.yaml
# Expected output:
#   <line>:  - variable: DATABASE_URL
#   <line>:    secret: DATABASE_URL
# Should NOT contain "DATABASE_URL_DEV"
grep -q "DATABASE_URL_DEV" apphosting.prod.yaml && { echo "ERROR: prod file must use DATABASE_URL secret, not DATABASE_URL_DEV"; exit 1; } || echo "OK"
```

Step 5 — Confirm at this commit there is currently NO `apphosting.yaml` (Task 2 creates it). The build is in a transient state between tasks; the commit happens at end of Task 2 (single commit, both files together).

```bash
test ! -f apphosting.yaml && echo "OK: apphosting.yaml temporarily absent — Task 2 creates the dev-base version"
```

Do NOT commit yet — Task 2 will combine both file operations into a single atomic commit.
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin && test -f apphosting.prod.yaml && grep -q "secret: DATABASE_URL$" apphosting.prod.yaml && grep -q "https://admin.triarch.dev" apphosting.prod.yaml && grep -q "secret: GITHUB_PACKAGES_TOKEN" apphosting.prod.yaml && grep -q "availability:" apphosting.prod.yaml && grep -A1 "NODE_AUTH_TOKEN" apphosting.prod.yaml | grep -q "GITHUB_PACKAGES_TOKEN" && grep -q "GITHUB_APP_PRIVATE_KEY" apphosting.prod.yaml && grep -q "SLACK_BOT_TOKEN" apphosting.prod.yaml && grep -q "DEPLOY_WEBHOOK_SECRET" apphosting.prod.yaml && ! grep -q "DATABASE_URL_DEV" apphosting.prod.yaml && yamllint -d "{rules: {line-length: disable, document-start: disable, truthy: disable}}" apphosting.prod.yaml && test ! -f apphosting.yaml && [ "$(git status --porcelain | grep -c apphosting)" -ge "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `apphosting.prod.yaml` exists at admin repo root
    - File `apphosting.yaml` is TEMPORARILY ABSENT (Task 2 will create the new dev-base version)
    - `git status --porcelain` shows the move staged or in working tree (`R apphosting.yaml -> apphosting.prod.yaml` or equivalent)
    - apphosting.prod.yaml contains `secret: DATABASE_URL` line (anchored — NOT `DATABASE_URL_DEV`)
    - apphosting.prod.yaml does NOT contain `DATABASE_URL_DEV` (prod uses prod secret only)
    - apphosting.prod.yaml contains `https://admin.triarch.dev` (prod NEXTAUTH_URL preserved)
    - apphosting.prod.yaml contains `secret: GITHUB_PACKAGES_TOKEN` (NODE_AUTH_TOKEN binding preserved — RESEARCH §Pitfall 1)
    - apphosting.prod.yaml contains `availability:` block with `BUILD` for the NODE_AUTH_TOKEN entry (preserved)
    - apphosting.prod.yaml retains all SLACK_*, GITHUB_APP_*, NEXTAUTH_SECRET, GOOGLE_CLIENT_*, DEPLOY_WEBHOOK_* fields (verified by grep on each)
    - yamllint clean
    - No commit yet (Task 2 combines both ops into single atomic commit)
  </acceptance_criteria>
  <done>Original prod-only apphosting.yaml is now apphosting.prod.yaml — all 78 lines preserved verbatim; ready for Task 2 to create the new dev-base apphosting.yaml.</done>
</task>

<task type="auto">
  <name>Task 2: Create new apphosting.yaml as dev base + run regression suite + atomic commit</name>
  <files>apphosting.yaml</files>
  <read_first>
    - apphosting.prod.yaml (the moved file from Task 1 — copy its structure exactly, change three values)
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RESEARCH.md (§"Admin apphosting.yaml (dev base, after overlay split)" — paste body verbatim; §Pitfall 4 — NEXTAUTH_URL placeholder note)
    - .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-CONTEXT.md (D-11, D-15, D-16 — three values that must change between dev and prod)
  </read_first>
  <action>
**Working directory: admin repo root.**

Step 1 — Create the new `apphosting.yaml` as the dev base. Use RESEARCH.md §"Admin apphosting.yaml (dev base, after overlay split)" verbatim. Per RESEARCH §Pitfall 1: this is a COMPLETE config — every field from apphosting.prod.yaml is present here, with three values changed for the dev environment:

```yaml
# apphosting.yaml — dev base config (deploys to <app>-dev backend from dev branch)
# Production config lives in apphosting.prod.yaml — used by deploy-firebase.yml@v4 with --config apphosting.prod.yaml

runConfig:
  runtime: nodejs22
  concurrency: 10
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 2

env:
  - variable: NEXTAUTH_URL
    value: https://admin-dev.triarch.dev   # dev URL placeholder — Mike updates to actual FAH hosted.app URL after backend creation (RUNBOOK B-1 + RESEARCH §Pitfall 4)
    availability:
      - BUILD
      - RUNTIME

  - variable: ADMIN_EMAIL
    value: mike@triarchsecurity.com
    availability:
      - RUNTIME

  - variable: DEPLOY_WEBHOOK_URL
    value: https://admin-dev.triarch.dev/api/webhooks/github-deploy
    availability:
      - RUNTIME

  # Secrets — set via: firebase apphosting:secrets:set <KEY>
  # Dev cluster DB connection (distinct from prod DATABASE_URL — CONTEXT D-11)
  - variable: DATABASE_URL
    secret: DATABASE_URL_DEV

  - variable: NEXTAUTH_SECRET
    secret: NEXTAUTH_SECRET

  - variable: GOOGLE_CLIENT_ID
    secret: GOOGLE_CLIENT_ID

  - variable: GOOGLE_CLIENT_SECRET
    secret: GOOGLE_CLIENT_SECRET

  # Build-time GitHub Packages auth — required for npm ci (RESEARCH §Pitfall 1)
  - variable: NODE_AUTH_TOKEN
    secret: GITHUB_PACKAGES_TOKEN
    availability:
      - BUILD

  - variable: DEPLOY_WEBHOOK_SECRET
    secret: DEPLOY_WEBHOOK_SECRET

  # Slack App secrets
  - variable: SLACK_BOT_TOKEN
    secret: SLACK_BOT_TOKEN

  - variable: SLACK_SIGNING_SECRET
    secret: SLACK_SIGNING_SECRET

  - variable: SLACK_PAYLOAD_SECRET
    secret: SLACK_PAYLOAD_SECRET

  - variable: SLACK_RELEASE_APPROVAL_CHANNEL
    value: "#release-approvals"
    availability:
      - RUNTIME

  # GitHub App secrets
  - variable: GITHUB_APP_ID
    secret: GITHUB_APP_ID

  - variable: GITHUB_APP_PRIVATE_KEY
    secret: GITHUB_APP_PRIVATE_KEY

  - variable: GITHUB_APP_INSTALLATION_ID
    secret: GITHUB_APP_INSTALLATION_ID
```

**Three values that differ from apphosting.prod.yaml:**
1. `NEXTAUTH_URL` value: `https://admin-dev.triarch.dev` (placeholder — Mike updates to real FAH URL in RUNBOOK B-1; RESEARCH §Pitfall 4)
2. `DEPLOY_WEBHOOK_URL` value: `https://admin-dev.triarch.dev/api/webhooks/github-deploy`
3. `DATABASE_URL` → `secret: DATABASE_URL_DEV` (CONTEXT D-11; the rest of apphosting.prod.yaml uses `secret: DATABASE_URL`)

Every other field MUST match apphosting.prod.yaml verbatim.

Step 2 — Lint both files:

```bash
yamllint -d "{rules: {line-length: disable, document-start: disable, truthy: disable}}" apphosting.yaml apphosting.prod.yaml
```

Step 3 — Run vitest regression. Per VALIDATION.md: "126+/126+ GREEN". The overlay change is YAML-only and does not touch runtime code. If any test fails, the failure is unrelated to this plan; investigate.

```bash
npx vitest run 2>&1 | tail -30
# Expected: a "Tests" line showing all tests passed; final "126 passed" or higher
```

Step 4 — Run next build. The new `apphosting.yaml` is the default config used at FAH dev rollout time, but `npx next build` does NOT actually read apphosting.yaml — it only validates the Next.js application build. The check here is that the YAML changes don't introduce a malformed file that would crash a deploy script's pre-build validation.

```bash
npx next build 2>&1 | tail -20
# Expected: a "Compiled successfully" or equivalent line; no fatal errors
```

If `next build` requires runtime env vars and fails on missing DATABASE_URL etc., that's a pre-existing condition unrelated to this plan; capture the failure mode in the SUMMARY and proceed if the failure is identical to a baseline run on origin/main.

Step 5 — Stage both file changes (the rename from Task 1 + the new file) and commit atomically:

```bash
git add apphosting.yaml apphosting.prod.yaml
git commit -m "feat(apphosting): split into dev-base + prod-overlay (ENV-02, ENV-04)

- apphosting.yaml (NEW): dev base — DATABASE_URL_DEV secret, dev NEXTAUTH_URL placeholder
- apphosting.prod.yaml (MOVED from apphosting.yaml): prod complete config — DATABASE_URL secret, prod NEXTAUTH_URL
- Per RESEARCH §Pitfall 1: prod file is COMPLETE (NODE_AUTH_TOKEN, all secrets) — required because Firebase --config flag selects file wholesale

Per CONTEXT D-15/D-16 — deploy-firebase.yml@v4 (Plan 07.5-02) routes prod via --config apphosting.prod.yaml; dev uses default apphosting.yaml.

Pitfall 4 follow-up: NEXTAUTH_URL is placeholder; Mike updates to actual FAH hosted.app URL during RUNBOOK step B-1."
```

Per RESEARCH §Pitfall 4: `NEXTAUTH_URL = https://admin-dev.triarch.dev` is a placeholder. The actual FAH default URL for the admin-dev backend follows the `<app>-dev.<region>.hosted.app` pattern. Mike captures the real URL during RUNBOOK B-1 and updates `apphosting.yaml` accordingly. This is a known follow-up; do not block this plan on it.
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/development/admin && test -f apphosting.yaml && test -f apphosting.prod.yaml && grep -q "secret: DATABASE_URL_DEV" apphosting.yaml && grep -q "secret: DATABASE_URL$" apphosting.prod.yaml && grep -q "https://admin-dev.triarch.dev" apphosting.yaml && grep -q "https://admin.triarch.dev" apphosting.prod.yaml && grep -q "secret: GITHUB_PACKAGES_TOKEN" apphosting.yaml && grep -q "secret: GITHUB_PACKAGES_TOKEN" apphosting.prod.yaml && grep -q "GITHUB_APP_PRIVATE_KEY" apphosting.yaml && grep -q "GITHUB_APP_PRIVATE_KEY" apphosting.prod.yaml && yamllint -d "{rules: {line-length: disable, document-start: disable, truthy: disable}}" apphosting.yaml apphosting.prod.yaml && npx vitest run 2>&1 | grep -qE "([0-9]+) passed" && npx next build 2>&1 | grep -qE "(Compiled successfully|Generating static pages)" && git log -1 --pretty=%s | grep -q "apphosting.*ENV-02.*ENV-04"</automated>
  </verify>
  <acceptance_criteria>
    - File `apphosting.yaml` exists with `secret: DATABASE_URL_DEV` (the dev-secret reference — CONTEXT D-11)
    - File `apphosting.prod.yaml` exists with `secret: DATABASE_URL` (anchored — NOT `DATABASE_URL_DEV`)
    - apphosting.yaml NEXTAUTH_URL value is `https://admin-dev.triarch.dev` (dev placeholder)
    - apphosting.prod.yaml NEXTAUTH_URL value is `https://admin.triarch.dev` (prod URL preserved)
    - Both files contain the literal `secret: GITHUB_PACKAGES_TOKEN` (NODE_AUTH_TOKEN BUILD secret — RESEARCH §Pitfall 1)
    - Both files contain `GITHUB_APP_PRIVATE_KEY` (full secret block preserved across split)
    - Both files contain `SLACK_BOT_TOKEN` (Slack secrets preserved across split)
    - yamllint clean on both files
    - `npx vitest run` shows X passed where X >= 126 (regression — VALIDATION.md target; no test failures)
    - `npx next build` succeeds (no fatal errors; matches baseline behavior on origin/main)
    - Single commit on current branch with message containing both `ENV-02` and `ENV-04`
    - Commit shows BOTH apphosting.yaml (added) and apphosting.prod.yaml (renamed from apphosting.yaml) in `git show --stat`
  </acceptance_criteria>
  <done>Both apphosting.yaml (dev base, DATABASE_URL_DEV) and apphosting.prod.yaml (prod complete config, DATABASE_URL) exist; vitest regression remains 126+/126+ GREEN; next build succeeds; single atomic commit on admin branch. NEXTAUTH_URL placeholder noted for Mike to update during RUNBOOK B-1.</done>
</task>

</tasks>

<verification>
- `test -f apphosting.yaml && test -f apphosting.prod.yaml` returns 0
- `grep -q "secret: DATABASE_URL_DEV" apphosting.yaml` returns 0
- `grep -q "secret: DATABASE_URL$" apphosting.prod.yaml` returns 0
- `! grep -q "DATABASE_URL_DEV" apphosting.prod.yaml` (prod has NO dev-secret reference)
- `diff <(grep "secret:" apphosting.yaml | sort) <(grep "secret:" apphosting.prod.yaml | sort)` shows ONE difference (DATABASE_URL_DEV in dev vs DATABASE_URL in prod) — every other secret line identical
- `npx vitest run` exits 0
- `npx next build` exits 0
- `git log -1 --pretty=%s` matches `feat(apphosting): split into dev-base + prod-overlay.*ENV-02.*ENV-04`
</verification>

<success_criteria>
- ENV-02 (admin-side preparation): apphosting.yaml dev base exists pointing at DATABASE_URL_DEV — Mike provisions the actual admin-dev FAH backend in RUNBOOK B-1.
- ENV-04 (admin-side overlay convention): both files exist; pitfall guard satisfied (apphosting.prod.yaml is complete, NODE_AUTH_TOKEN preserved).
- No regression: vitest 126+/126+ GREEN; next build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-03-SUMMARY.md` documenting:
- Confirmation apphosting.yaml renamed to apphosting.prod.yaml verbatim (no fields lost)
- New apphosting.yaml file body summary (the three differing fields: DATABASE_URL_DEV secret, dev NEXTAUTH_URL, dev DEPLOY_WEBHOOK_URL)
- Confirmation NODE_AUTH_TOKEN with availability: [BUILD] is present in BOTH files (RESEARCH §Pitfall 1 satisfied)
- vitest test count (X/X passed where X >= 126)
- next build status (succeeded vs known baseline failure)
- Known follow-up: Mike updates NEXTAUTH_URL in apphosting.yaml to actual FAH dev hosted.app URL during RUNBOOK step B-1 (RESEARCH §Pitfall 4)
- Commit SHA + message
</output>
