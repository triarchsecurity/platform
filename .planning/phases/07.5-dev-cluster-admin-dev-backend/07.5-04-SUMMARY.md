---
phase: 07.5-dev-cluster-admin-dev-backend
plan: "04"
subsystem: docs
tags: [onboarding, apphosting, db-migrate, dev-environment, ENV-04]
dependency_graph:
  requires: [07.5-02, 07.5-03]
  provides: [onboarding-step-11]
  affects: [docs/onboarding-projects.md]
tech_stack:
  added: []
  patterns: [apphosting-overlay, db-migrate-reusable-workflow]
key_files:
  created: []
  modified:
    - docs/onboarding-projects.md
decisions:
  - "Step 11 uses the detailed sub-section format (11a–11e + cost note) from PLAN.md verbatim rather than the compact RESEARCH.md version — provides copy-pasteable YAML examples"
  - "Section inserted between Step 10 closing --- and ## Verification Checklist heading"
metrics:
  duration: "~2 minutes"
  completed: "2026-05-05"
  tasks_completed: 1
  files_modified: 1
---

# Phase 07.5 Plan 04: Onboarding Step 11 — Dev Environment Documentation Summary

**One-liner:** Appended Step 11 to onboarding runbook documenting apphosting.yaml overlay convention + db-migrate.yml usage for all future Triarch projects.

## What Was Built

A new `## Step 11 — Dev Environment (apphosting.yaml overlay + db-migrate.yml)` section (112 lines) appended to `docs/onboarding-projects.md`, inserted between Step 10 and the existing `## Verification Checklist` block.

**Step number used:** Step 11 (file had exactly 10 prior steps — confirmed by grep).

**Approximate line count of new section:** 112 lines (between `## Step 11` heading and `## Verification Checklist`).

**Insertion position:** Step 11 at line 582, Verification Checklist at line 694 — Step 11 is correctly between the final existing step and the checklist.

## Section Structure

- **Intro table** — Side-by-side comparison: `apphosting.yaml` (dev base, `DATABASE_URL_DEV`) vs `apphosting.prod.yaml` (prod overrides, `DATABASE_URL`)
- **11a** — Create `apphosting.yaml` dev base with 4-step change list + copy-paste YAML example (admin reference)
- **11b** — Create `apphosting.prod.yaml` prod overrides + **Pitfall 1 critical warning** (COMPLETE config, not partial diff)
- **11c** — Verify overlay convention with yamllint + diff sanity check commands
- **11d** — Wire `db-migrate.yml@v4` in `ci-cd.yml` with `workflow_call` YAML example + `workflow_dispatch` UI instructions; when-to-use guidance; Pitfall 6 caveat (Drizzle-only)
- **11e** — Set `DATABASE_URL_DEV` Firebase secret command + set prod `DATABASE_URL` reminder
- **Cost note** — ~$50–100/month marginal cost callout for customer-managed projects

## Warnings Documented

- **Pitfall 1 (COMPLETE config):** `apphosting.prod.yaml` must be a COMPLETE config, not a partial diff — omitting `NODE_AUTH_TOKEN` breaks `npm ci` on prod builds with `E401 Unauthorized`. Present as a `> **Critical (RESEARCH §Pitfall 1):**` blockquote in §11b.
- **Pitfall 6 (Drizzle-only):** `db-migrate.yml` requires `drizzle.config.ts` at repo root; static-site repos (e.g. `www`) must NOT call this workflow. Present as `**Caveat (RESEARCH §Pitfall 6):**` in §11d.

## Key References in Section

- `MyAlterLego/shared-workflows/.github/workflows/db-migrate.yml@v4` (workflow_call ref)
- `apphosting:secrets:set DATABASE_URL_DEV` (secret setup command)
- `DATABASE_URL_DEV` (dev secret name throughout)
- `drizzle.config.ts` (Pitfall 6 caveat)
- `COMPLETE config` (Pitfall 1 warning keyword)

## Commit

| Commit | Message |
|--------|---------|
| `fad2d58` | `docs(onboarding): add Step 11 — dev environment overlay + db-migrate.yml (ENV-04)` |

Note: commit also includes `apphosting.yaml => apphosting.prod.yaml` rename that was pre-staged by parallel agent 07.5-03. File content is correct; the rename is 07.5-03's scope bundled into this commit due to parallel staging.

## Deviations from Plan

None — plan executed exactly as written. Used the detailed verbatim content from PLAN.md (which is an expanded version of RESEARCH.md §"Step 11 content") with full sub-sections and YAML examples as specified.

## Known Stubs

None — documentation only; no code stubs.

## Self-Check: PASSED

- `docs/onboarding-projects.md` exists and contains `## Step 11`: FOUND
- `grep -c "^## Step 11" docs/onboarding-projects.md` returns 1: CONFIRMED
- Step 11 at line 582 < Verification Checklist at line 694: CONFIRMED
- Section length 112 lines (> 30 minimum): CONFIRMED
- All 9 grep checks passed: CONFIRMED
- Commit `fad2d58` exists with message matching `docs(onboarding): add Step 11.*ENV-04`: CONFIRMED
