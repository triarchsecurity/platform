---
phase: 07.5-dev-cluster-admin-dev-backend
verified: 2026-05-05T14:45:00Z
status: human_needed
score: 5/5 code must-haves verified
human_verification:
  - test: "A-1: Provision new dedicated CockroachDB Cloud cluster"
    expected: "New cluster (separate from prod triarchdev-24092) exists in Cockroach Cloud console; cockroach sql --url connects successfully; SHOW DATABASES returns defaultdb + system"
    why_human: "Requires Cockroach Cloud console access and CLI — cannot verify cluster existence from code"

  - test: "A-2: Apply scripts/provision-dev-dbs.sql to the new cluster"
    expected: "cockroach sql --url '<dev-cluster-url>' -f scripts/provision-dev-dbs.sql exits 0; SHOW DATABASES lists all 6: admin_dev, portal_dev, darksouls_dev, tmi_dev, truthtreason_dev, www_dev"
    why_human: "Requires live CRDB cluster connection — code deliverable verified, application is a human CLI action"

  - test: "B-1: Create admin-dev FAH backend + set DATABASE_URL_DEV secret"
    expected: "firebase apphosting:backends:list shows admin-dev; DATABASE_URL_DEV secret set with admin_dev connection URL; apphosting.yaml NEXTAUTH_URL updated from placeholder to actual hosted.app URL"
    why_human: "Requires Firebase Console / CLI with appropriate GCP project access"

  - test: "B-2: Create portal-dev FAH backend + set DATABASE_URL_DEV secret"
    expected: "portal-dev backend visible in Firebase Console; DATABASE_URL_DEV points to portal_dev DB"
    why_human: "Requires triarchsecurity-portal Firebase project access"

  - test: "B-3: Create www-dev FAH backend + set DATABASE_URL_DEV secret (or skip if no DB)"
    expected: "www-dev backend visible; DATABASE_URL_DEV set to www_dev connection URL (if applicable)"
    why_human: "Requires www Firebase project access; decision on whether www uses Drizzle"

  - test: "B-4: Create darksouls-dev FAH backend + set DATABASE_URL_DEV secret"
    expected: "darksouls-dev backend visible in customer GCP project; DATABASE_URL_DEV set to darksouls_dev connection URL"
    why_human: "Requires darksouls customer GCP project access"

  - test: "B-5: Create tmi-dev FAH backend + set DATABASE_URL_DEV secret"
    expected: "tmi-dev backend visible; DATABASE_URL_DEV set to tmi_dev connection URL"
    why_human: "Requires tmi customer GCP project access"

  - test: "B-6: Create truthtreason-dev FAH backend + set DATABASE_URL_DEV secret"
    expected: "truthtreason-dev backend visible; DATABASE_URL_DEV set to truthtreason_dev connection URL"
    why_human: "Requires truthtreason customer GCP project access"

  - test: "C-1: Confirm plan-phase code deliverables — review feat/v4-environment-input branch"
    expected: "actionlint .github/workflows/*.yml clean on feat/v4-environment-input; environment input present in deploy-firebase.yml; db-migrate.yml present with correct structure"
    why_human: "Human sign-off review of code changes before tagging"

  - test: "C-2: Push feat/v4-environment-input branch + merge PR + tag shared-workflows@v4 (after admin canary verifies)"
    expected: "PR merged to main; git tag -l shows v4; v3 tag unchanged; admin-dev backend served successful build from dev branch via v4 workflow"
    why_human: "Requires live admin-dev canary verification + git tag push to shared-workflows repo"

  - test: "C-3: Bump consumer ci-cd.yml refs from @v3 to @v4 (incremental)"
    expected: "Each consumer's ci-cd.yml references @v4; first dev deploy on each consumer succeeds; prod deploys unchanged"
    why_human: "Requires per-consumer repo access and live CI/CD validation"

  - test: "D-0: Pre-req — push promote_attempts schema to admin_dev via db-migrate.yml@v4"
    expected: "promote_attempts table exists in admin_dev; SELECT COUNT(*) FROM promote_attempts returns 0"
    why_human: "Requires live admin-dev backend + database connectivity"

  - test: "D-1: Scenario A — Clean Rebase + CI + Merge against admin-dev"
    expected: "promote_attempts.result=merged; merge_sha populated; OttoBot thread shows checkmark; workflow exits success"
    why_human: "Requires live admin-dev backend, live CI runs, and Slack OttoBot"

  - test: "D-2: Scenario B — Rebase Conflict against admin-dev"
    expected: "promote_attempts.result=conflict; conflict_files non-empty; OttoBot threads warning with file list"
    why_human: "Requires live promote-branch.yml execution with intentional conflict branch"

  - test: "D-3: Scenario C — CI Failure against admin-dev"
    expected: "promote_attempts.result=ci_failed; ci_run_url populated; main unchanged"
    why_human: "Requires live CI run with intentionally failing test"

  - test: "D-4: Scenario D — Concurrency against admin-dev"
    expected: "Two promote_attempts rows both result=merged; distinct merge_sha; final main contains both branches' commits"
    why_human: "Requires concurrent live workflow dispatches"

  - test: "D-5: Mark 04-HUMAN-UAT.md resolved after all 4 scenarios pass"
    expected: "04-HUMAN-UAT.md frontmatter status: partial → status: resolved; all scenarios marked result: passed"
    why_human: "Depends on D-1..D-4 all passing — requires live execution first"
---

# Phase 7.5: Dev Cluster + Admin Dev Backend — Verification Report

**Phase Goal:** Every Triarch app has a non-prod environment safe for schema migrations and live UAT — backed by a dedicated dev CRDB cluster, an `<app>-dev` App Hosting backend per project, and an env-aware shared-workflows tag (`v4`)
**Verified:** 2026-05-05T14:45:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A dev CRDB cluster exists and holds one DB per Triarch-owned project | ? HUMAN NEEDED | CODE: scripts/provision-dev-dbs.sql verified (6 DBs). HUMAN: cluster provisioning (A-1) + SQL application (A-2) pending |
| 2 | Each Triarch project has an `<app>-dev` App Hosting backend | ? HUMAN NEEDED | CODE: apphosting.yaml (DATABASE_URL_DEV) + apphosting.prod.yaml (complete config) verified in admin repo. HUMAN: B-1..B-6 backend creation pending |
| 3 | shared-workflows v4 introduces environment: dev\|prod input and new db-migrate.yml | ? HUMAN NEEDED | CODE: feat/v4-environment-input branch verified (deploy-firebase.yml + db-migrate.yml). HUMAN: C-2 tag v4 + C-3 consumer bumps pending |
| 4 | apphosting.yaml + apphosting.prod.yaml overlay pattern generalized and documented | ✓ VERIFIED | apphosting.yaml (dev base, DATABASE_URL_DEV), apphosting.prod.yaml (prod complete config, DATABASE_URL), Step 11 in docs/onboarding-projects.md (112 lines) — all code-complete |
| 5 | Phase 4 deferred UAT scenarios exercised against admin-dev and closed | ? HUMAN NEEDED | CODE: 07.5-PHASE-4-UAT-CLOSURE.md + 07.5-HUMAN-UAT.md templates verified. HUMAN: D-0..D-5 execution pending |

**Score:** 5/5 code must-haves VERIFIED. 17/17 human runbook items PENDING.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/provision-dev-dbs.sql` | 6 idempotent CREATE DATABASE IF NOT EXISTS statements | ✓ VERIFIED | 26 lines; all 6 DB names (admin_dev, portal_dev, darksouls_dev, tmi_dev, truthtreason_dev, www_dev); cockroach sql header command; SHOW DATABASES verification block; commit 6cd0545 |
| `~/claude/MyAlterLego/shared-workflows/.github/workflows/deploy-firebase.yml` | v4 with environment input + apphosting overlay support | ✓ VERIFIED | environment input added (default prod, backwards compat); BACKEND="${BACKEND}-dev" derivation; --config apphosting.prod.yaml for prod; commit 0cc5897 on feat/v4-environment-input |
| `~/claude/MyAlterLego/shared-workflows/.github/workflows/db-migrate.yml` | New reusable workflow (workflow_call + workflow_dispatch); drizzle-kit push; tr -d newline trim; ::add-mask:: | ✓ VERIFIED | 147 lines; both triggers present; DATABASE_URL_DEV branch; tr -d '\n' applied; add-mask present; no continue-on-error on push step; commit 8bafdd0 |
| `apphosting.yaml` | Dev base config with DATABASE_URL_DEV, dev URLs | ✓ VERIFIED | DATABASE_URL → secret: DATABASE_URL_DEV; NEXTAUTH_URL placeholder https://admin-dev.triarch.dev; NODE_AUTH_TOKEN with availability: [BUILD] (Pitfall 1 guard present) |
| `apphosting.prod.yaml` | COMPLETE prod config with DATABASE_URL (not DATABASE_URL_DEV) | ✓ VERIFIED | All original prod fields preserved verbatim; DATABASE_URL → secret: DATABASE_URL; NODE_AUTH_TOKEN with availability: [BUILD]; no DATABASE_URL_DEV present |
| `docs/onboarding-projects.md` Step 11 | Dev environment convention documented | ✓ VERIFIED | 112-line section at line 582; BEFORE Verification Checklist; table + YAML examples; COMPLETE config warning; db-migrate.yml@v4 usage; apphosting:secrets:set DATABASE_URL_DEV; drizzle.config.ts caveat |
| `.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-PHASE-4-UAT-CLOSURE.md` | 4 scenario sections + SQL + closure actions | ✓ VERIFIED | 172 lines; Scenarios A/B/C/D each with setup, results table, FROM promote_attempts SQL; Closure Actions section; status: pending frontmatter |
| `.planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-HUMAN-UAT.md` | Mirror of HTML runbook items; status: pending | ✓ VERIFIED | 105 lines; A-1..A-2, B-1..B-6 (6 items), C-1..C-3, D-0..D-5; runbook_html cross-reference; status: pending frontmatter |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| deploy-firebase.yml BACKEND derivation | `<app>-dev` backend (env=dev) | `BACKEND="${BACKEND}-dev"` in run: body | ✓ WIRED | grep confirms at line 120 |
| deploy-firebase.yml prod deploy step | apphosting.prod.yaml | `--config apphosting.prod.yaml` flag | ✓ WIRED | grep confirms at line 135 |
| apphosting.yaml DATABASE_URL variable | DATABASE_URL_DEV Firebase secret | `secret: DATABASE_URL_DEV` mapping | ✓ WIRED | verified in file |
| apphosting.prod.yaml DATABASE_URL variable | DATABASE_URL Firebase secret (existing prod) | `secret: DATABASE_URL` mapping (anchored, not DEV) | ✓ WIRED | verified in file; Pitfall 1 guard satisfied |
| db-migrate.yml read_secret step | `firebase apphosting:secrets:access $SECRET_NAME` | env branch: dev → DATABASE_URL_DEV, prod → DATABASE_URL | ✓ WIRED | DATABASE_URL_DEV confirmed in file |
| db-migrate.yml drizzle push step | `npx drizzle-kit push` | DATABASE_URL env var from masked read_secret output | ✓ WIRED | drizzle-kit push present; no continue-on-error |
| db-migrate.yml secret-trim guard | `tr -d '\n'` on firebase CLI stdout | Pitfall 7 prevention | ✓ WIRED | tr -d confirmed present |
| Step 11 documentation | apphosting.yaml + apphosting.prod.yaml convention | Side-by-side table + YAML examples (admin reference) | ✓ WIRED | apphosting.prod.yaml reference in docs |
| Step 11d | db-migrate.yml@v4 usage example | `uses: MyAlterLego/shared-workflows/.github/workflows/db-migrate.yml@v4` | ✓ WIRED | v4 ref confirmed in onboarding doc |
| 07.5-PHASE-4-UAT-CLOSURE.md closure actions | 04-HUMAN-UAT.md status: partial → resolved | Manual edit instruction in Closure Actions section | ✓ WIRED | references 04-HUMAN-UAT.md |
| 07.5-HUMAN-UAT.md | 07.5-RUNBOOK.html | Markdown reference link + runbook_html frontmatter | ✓ WIRED | 07.5-RUNBOOK.html ref confirmed |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ENV-01 | 07.5-01 | Dev CRDB cluster with 6 per-app DBs | Code Complete (HUMAN action pending) | scripts/provision-dev-dbs.sql with all 6 DBs verified; cluster provisioning + SQL application = RUNBOOK A-1, A-2 pending |
| ENV-02 | 07.5-03 | Per-app `<app>-dev` App Hosting backends | Code Complete (HUMAN action pending) | apphosting.yaml (dev base) + apphosting.prod.yaml (prod complete) verified in admin repo; 6 backend creations = RUNBOOK B-1..B-6 pending |
| ENV-03 | 07.5-02 | shared-workflows v4 environment input + db-migrate.yml | Code Complete (HUMAN tag + canary pending) | feat/v4-environment-input branch with 2 commits verified; actionlint clean; v4 tag push = RUNBOOK C-2 pending |
| ENV-04 | 07.5-03, 07.5-04 | apphosting.yaml convention generalized + documented | Complete | Both files in admin repo verified; Step 11 in onboarding-projects.md verified (112 lines, all required content) |
| ENV-05 | 07.5-05 | Phase 4 deferred UAT scenarios exercised against admin-dev | Code Complete (HUMAN UAT execution pending) | 07.5-PHASE-4-UAT-CLOSURE.md + 07.5-HUMAN-UAT.md templates verified; all 4 scenarios scaffolded; execution = RUNBOOK D-0..D-5 pending |

**Note:** ENV-01..ENV-05 are not yet present in REQUIREMENTS.md traceability table. The requirements were created as part of Phase 7.5 planning and are tracked in the ROADMAP.md success criteria and plan frontmatter. REQUIREMENTS.md traceability should be updated to include ENV-01..ENV-05 mapped to Phase 7.5 with statuses reflecting the above.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `apphosting.yaml` NEXTAUTH_URL | `value: https://admin-dev.triarch.dev` — documented placeholder | Info | Known follow-up documented in Plan 07.5-03 and 07.5-HUMAN-UAT.md B-1: Mike updates to actual FAH hosted.app URL during RUNBOOK B-1. NOT a blocker — the correct URL is unknown until backend creation |
| `07.5-PHASE-4-UAT-CLOSURE.md` `Actual` + `Pass/Fail` columns | All empty (unfilled) | Info | Intentional — these are fillable templates; Mike completes them during live UAT execution. Not a stub |
| `07.5-HUMAN-UAT.md` all 17 checkboxes | All unchecked | Info | Intentional pending state — HUMAN actions not yet executed |

No blockers found. All anti-patterns are intentional pending states.

---

### Build + Test Regression

| Check | Result |
|-------|--------|
| `npx vitest run` | 126/126 passed (20 test files) — GREEN |
| `npx next build` | Compiled successfully in 2.8s; static pages generated |
| `npx tsc --noEmit` | 0 errors |
| No production source files modified | Confirmed — phase only creates scripts/, apphosting.yaml, apphosting.prod.yaml, docs/, .planning/ files |

---

### Human Verification Required

All 17 runbook items in `07.5-HUMAN-UAT.md` require Mike's hands-on action. Grouped by block:

#### Block A: Dev Cluster Provisioning (ENV-01)

**A-1: Provision new CockroachDB Cloud cluster**
- Test: Create new dedicated cluster via Cockroach Cloud console (separate from prod `triarchdev-24092`; recommend name `triarchdev-dev`; region `us-east1`; smallest Basic tier)
- Expected: `cockroach sql --url "<connection_string>" -e "SHOW DATABASES;"` returns `defaultdb` + `system`
- Why human: Cloud console provisioning, not automatable from code

**A-2: Apply scripts/provision-dev-dbs.sql**
- Test: `cockroach sql --url "<new-cluster-url>" -f scripts/provision-dev-dbs.sql`
- Expected: `SHOW DATABASES;` lists all 6: `admin_dev`, `portal_dev`, `darksouls_dev`, `tmi_dev`, `truthtreason_dev`, `www_dev`
- Why human: Requires live CRDB cluster connection

#### Block B: FAH Backends + DATABASE_URL_DEV Secrets (ENV-02)

**B-1: Create admin-dev FAH backend** (also: update apphosting.yaml NEXTAUTH_URL placeholder to real hosted.app URL)
- Test: `firebase apphosting:backends:create admin-dev --git-branch=dev --project=triarch-dev-website`; `firebase apphosting:secrets:set DATABASE_URL_DEV`; update `apphosting.yaml` NEXTAUTH_URL
- Expected: Both `admin` and `admin-dev` backends visible; DATABASE_URL_DEV secret accessible via CLI

**B-2 through B-6:** Same flow per project (portal-dev, www-dev, darksouls-dev, tmi-dev, truthtreason-dev)
- Each requires respective Firebase project access and DATABASE_URL_DEV pointing to the correct per-app DB

#### Block C: shared-workflows v4 Tag + Canary (ENV-03)

**C-1: Review feat/v4-environment-input branch**
- Test: Mike reviews the 2 commits on the branch (deploy-firebase.yml v4 + db-migrate.yml); runs `actionlint .github/workflows/*.yml`
- Expected: actionlint exits 0; code changes look correct

**C-2: Push feat branch + merge PR + tag v4 (after admin canary verifies)**
- Test: After B-1 complete and admin-dev canary deploy succeeds, push branch, merge PR, tag v4 on merged commit, push tag
- Expected: `git tag -l | grep v4` succeeds; v3 tag unchanged; admin-dev served successful build

**C-3: Bump consumer ci-cd.yml refs from @v3 to @v4 (incremental)**
- Test: Per-consumer ci-cd.yml update to `@v4` + `environment` input; verify dev + prod deploys
- Expected: Each consumer CI pipeline green; prod behavior unchanged

#### Block D: Phase 4 UAT Closure (ENV-05)

**D-0: Pre-req — push promote_attempts schema to admin_dev via db-migrate.yml**
- Test: Run `db-migrate.yml@v4` workflow_dispatch (or manual `drizzle-kit push`) against admin_dev
- Expected: `promote_attempts` table exists in admin_dev; row count = 0

**D-1: Scenario A — Clean Rebase + CI + Merge**
- Test: Branch `test/uat-clean` with no conflicts; approve from admin UI
- Expected: `promote_attempts.result=merged`; merge_sha populated; OttoBot checkmark thread

**D-2: Scenario B — Rebase Conflict**
- Test: Branch `test/uat-conflict` with intentional conflict; approve from admin UI
- Expected: `promote_attempts.result=conflict`; conflict_files non-empty; OttoBot warning thread

**D-3: Scenario C — CI Failure**
- Test: Branch `test/uat-ci-failed` with failing test; approve from admin UI
- Expected: `promote_attempts.result=ci_failed`; ci_run_url populated; main unchanged

**D-4: Scenario D — Concurrency**
- Test: Two branches (`test/uat-concurrent-A`, `test/uat-concurrent-B`) touching different files; approve both within ~5 seconds
- Expected: Both promote_attempts rows result=merged; distinct merge_sha; final main contains both changes

**D-5: Mark 04-HUMAN-UAT.md resolved**
- Test: After D-1..D-4 all pass, update `04-HUMAN-UAT.md` frontmatter `status: partial → resolved`; mark each scenario result: passed
- Expected: 04-HUMAN-UAT.md shows `status: resolved`

---

### Gaps Summary

There are **no code gaps**. All 5 CODE deliverables for Phase 7.5 are complete and verified:

1. `scripts/provision-dev-dbs.sql` — 6 idempotent CREATE DATABASE statements, all checks pass
2. `shared-workflows/deploy-firebase.yml` v4 — environment input + apphosting overlay, actionlint clean, on feat/v4-environment-input branch
3. `shared-workflows/db-migrate.yml` — new reusable workflow, all required patterns present
4. `apphosting.yaml` + `apphosting.prod.yaml` — overlay split verified; Pitfall 1 guard (NODE_AUTH_TOKEN in both files) confirmed
5. `docs/onboarding-projects.md` Step 11 — 112 lines, all required content present
6. `07.5-PHASE-4-UAT-CLOSURE.md` + `07.5-HUMAN-UAT.md` — 4 scenarios + 17 runbook items scaffolded

Phase status is `human_needed` — not `gaps_found` — because the blocking items are HUMAN infrastructure actions (cluster provisioning, FAH backend creation, v4 tag, UAT execution), not missing code. The 17 runbook items in `07.5-HUMAN-UAT.md` are the remaining work. SC-4 (ENV-04) is the one criterion fully complete without HUMAN dependencies.

**REQUIREMENTS.md action required:** ENV-01..ENV-05 are not in the traceability table. Add these 5 entries mapping to Phase 7.5 with statuses: ENV-01 Code Complete / ENV-02 Code Complete / ENV-03 Code Complete / ENV-04 Complete / ENV-05 Code Complete.

---

*Verified: 2026-05-05T14:45:00Z*
*Verifier: Claude (gsd-verifier)*
