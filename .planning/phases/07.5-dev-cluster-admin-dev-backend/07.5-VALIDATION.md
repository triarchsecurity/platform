---
phase: 7.5
slug: dev-cluster-admin-dev-backend
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-05
---

# Phase 7.5 — Validation Strategy

> Heavy HUMAN-action phase. Most success criteria close via the runbook at `07.5-RUNBOOK.html`. CODE deliverables (SQL seed, shared-workflows v4 YAML, apphosting overlay, docs) are validated via lint + manual review.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.x (existing admin tests) + actionlint + yamllint (new YAML validators) |
| **Quick run** | `npx vitest run` (no new tests in this phase; regression check only) |
| **YAML lint** | `actionlint .github/workflows/*.yml` (in shared-workflows repo) |
| **SQL syntax check** | `psql --set=ON_ERROR_STOP=1 -f scripts/provision-dev-dbs.sql` against an empty cluster (HUMAN — runs A-2 in runbook) |

---

## Sampling Rate

- **After every CODE task commit:** Run `npx vitest run` (regression check) + `actionlint` on any modified workflow YAML
- **Before phase complete:** All HUMAN-UAT runbook items in `07.5-RUNBOOK.html` checked
- **Max feedback latency:** ~30 seconds (vitest), instant (actionlint)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Verification | Status |
|---------|------|------|-------------|-----------|--------------|--------|
| TBD | SQL seed | 1 | ENV-01 | manual SQL | `psql --set=ON_ERROR_STOP=1 -f scripts/provision-dev-dbs.sql` exits 0; `SHOW DATABASES;` returns 6 rows | ⬜ pending (HUMAN-A2) |
| TBD | shared-workflows v4 deploy-firebase | 2 | ENV-03 | actionlint | `actionlint deploy-firebase.yml` exits 0; backwards-compat: omitting `environment` input still produces v3 behavior | ⬜ pending |
| TBD | shared-workflows v4 db-migrate.yml | 2 | ENV-03 | actionlint | `actionlint db-migrate.yml` exits 0; YAML valid | ⬜ pending |
| TBD | admin apphosting overlay | 3 | ENV-04 | yamllint + regression | `yamllint apphosting.yaml apphosting.prod.yaml` clean; `npx vitest run` 126/126 GREEN; `npx next build` succeeds | ⬜ pending |
| TBD | onboarding docs Step 11 | 3 | ENV-04 | grep | `docs/onboarding-projects.md` contains "Step 11", "apphosting.prod.yaml", "db-migrate.yml" | ⬜ pending |
| TBD | Phase 4 UAT closure runbook | 3 | ENV-05 | file existence | `07.5-PHASE-4-UAT-CLOSURE.md` exists with 4 scenario sections + checkbox structure | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] None — Phase 7.5 produces YAML / SQL / Markdown only; no new test files. Vitest globally jsdom (Phase 5 Wave 0) covers regression checks.

---

## Manual-Only Verifications (HUMAN-UAT)

All tracked in `07.5-RUNBOOK.html` with persisted checkbox state. Summary:

| Behavior | Requirement | Where |
|----------|-------------|-------|
| Provision new CRDB Cloud cluster | ENV-01 | Runbook A-1 |
| Apply provision-dev-dbs.sql to new cluster | ENV-01 | Runbook A-2 |
| Create `<app>-dev` FAH backend × 6 projects + DATABASE_URL_DEV secret | ENV-02 | Runbook B-1..B-6 |
| Tag shared-workflows@v4 after admin canary verifies | ENV-03 | Runbook C-2 |
| Bump consumer ci-cd.yml refs from @v3 to @v4 | ENV-03 | Runbook C-3 |
| Phase 4 UAT — clean rebase | ENV-05 | Runbook D-1 |
| Phase 4 UAT — conflict | ENV-05 | Runbook D-2 |
| Phase 4 UAT — ci_failed | ENV-05 | Runbook D-3 |
| Phase 4 UAT — concurrent | ENV-05 | Runbook D-4 |
| Mark `04-HUMAN-UAT.md` resolved | ENV-05 | Runbook D-5 |

---

## Validation Sign-Off

- [ ] CODE deliverables (SQL seed, shared-workflows v4 YAML, apphosting overlay, docs) committed and lint-clean
- [ ] `npx vitest run` 126+/126+ GREEN (regression — apphosting overlay must not break admin tests)
- [ ] `npx next build` succeeds with new apphosting.yaml structure
- [ ] All 17 runbook checkboxes in `07.5-RUNBOOK.html` marked complete (HUMAN sign-off)
- [ ] `04-HUMAN-UAT.md` updated to `status: resolved`
- [ ] `nyquist_compliant: true` set in this file's frontmatter

**Approval:** pending
