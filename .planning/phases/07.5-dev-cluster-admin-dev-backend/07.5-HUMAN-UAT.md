---
phase: 07.5-dev-cluster-admin-dev-backend
plan: 05
status: pending
created: 2026-05-05
runbook_html: .planning/phases/07.5-dev-cluster-admin-dev-backend/07.5-RUNBOOK.html
---

# Phase 7.5 — Human UAT Items

> **Source of truth:** [`07.5-RUNBOOK.html`](./07.5-RUNBOOK.html) — the HTML runbook has persisted checkbox state (via browser localStorage). This markdown file mirrors the same items so `/gsd:audit-uat` can scan and report against them.
>
> **Status:** pending — Mike marks items complete in the HTML runbook as he executes each one. After all items are complete, this file's frontmatter `status: pending` flips to `status: resolved`.
>
> **Categories:** A (Cluster + DBs) · B (FAH backends + secrets) · C (shared-workflows v4 tag + canary) · D (Phase 4 UAT closure)

## A. Cluster + DBs (ENV-01)

- [ ] **A-1: Provision new CockroachDB Cloud cluster** (CONTEXT D-01..D-04)
  - Action: Mike creates a new dedicated CRDB Cloud cluster via the Cockroach Cloud console. Recommended name `triarchdev-dev`, region `us-east1` (parity with prod), smallest Basic tier. Create SQL user; save password + connection string template.
  - Acceptance: `cockroach sql --url "<connection_string>" -e "SHOW DATABASES;"` returns `defaultdb` + `system`. No errors.
- [ ] **A-2: Apply `scripts/provision-dev-dbs.sql` to the new cluster** (CONTEXT D-05..D-08)
  - Action: `cockroach sql --url "<new-cluster-url>" -f scripts/provision-dev-dbs.sql`
  - Acceptance: `SHOW DATABASES;` lists `admin_dev`, `portal_dev`, `darksouls_dev`, `tmi_dev`, `truthtreason_dev`, `www_dev` (six DBs total, plus `defaultdb` and `system`).

## B. FAH backends + DATABASE_URL_DEV secrets (ENV-02)

> Per CONTEXT D-09: Triarch-owned apps (admin, portal, www) get the `<app>-dev` backend in the SAME Firebase project as prod. Customer-owned apps (truthtreason, darksouls, tmi) get `<app>-dev` backends in the existing customer GCP project. NO new GCP projects.

- [ ] **B-1: Create `admin-dev` FAH backend + set `DATABASE_URL_DEV` secret** (CONTEXT D-09..D-14)
  - Action: `firebase apphosting:backends:create admin-dev --git-branch=dev --project=triarch-dev-website --region=us-central1`; then `firebase apphosting:secrets:set DATABASE_URL_DEV --project=triarch-dev-website --backend=admin-dev` (paste the `admin_dev` connection URL from A-1 with `defaultdb` replaced by `admin_dev`).
  - Also: update `apphosting.yaml` `NEXTAUTH_URL` from the placeholder `https://admin-dev.triarch.dev` to the actual FAH default URL (e.g. `https://admin-dev--triarch-dev-website.us-central1.hosted.app` or whatever FAH issues — shown in Firebase Console after backend creation).
  - Acceptance: `firebase apphosting:backends:list --project=triarch-dev-website` shows both `admin` (prod) and `admin-dev`; `firebase apphosting:secrets:access DATABASE_URL_DEV --project=triarch-dev-website` returns the dev connection string; `apphosting.yaml` reflects the real FAH URL.
- [ ] **B-2: Create `portal-dev` FAH backend + set `DATABASE_URL_DEV` secret**
  - Action: Same flow as B-1 applied to the triarchsecurity-portal Firebase project; `DATABASE_URL_DEV` ends in `/portal_dev`.
  - Acceptance: Backend visible in Firebase Console; secret readable via CLI.
- [ ] **B-3: Create `www-dev` FAH backend + set `DATABASE_URL_DEV` secret** (or skip if www has no DB)
  - Action: Same flow, `DATABASE_URL_DEV` ends in `/www_dev`. Per RESEARCH §Pitfall 6: if www does not use Drizzle, skip the DATABASE_URL_DEV step but still create the FAH dev backend if dev preview is desired.
  - Acceptance: Backend visible; secret readable (if applicable).
- [ ] **B-4: Create `darksouls-dev` FAH backend + set `DATABASE_URL_DEV` secret**
  - Action: Same three sub-steps as B-1 in the customer's existing GCP project; `DATABASE_URL_DEV` ends in `/darksouls_dev`.
  - Acceptance: Backend visible; secret readable via CLI.
- [ ] **B-5: Create `tmi-dev` FAH backend + set `DATABASE_URL_DEV` secret**
  - Action: Same as above in customer's GCP; `DATABASE_URL_DEV` ends in `/tmi_dev`.
  - Acceptance: Backend visible; secret readable.
- [ ] **B-6: Create `truthtreason-dev` FAH backend + set `DATABASE_URL_DEV` secret**
  - Action: Same as above in customer's GCP; `DATABASE_URL_DEV` ends in `/truthtreason_dev`. Note: Truth+Treason has the canonical `apphosting.yaml`/`apphosting.prod.yaml` overlay already (RESEARCH §Truth+Treason Overlay Reference).
  - Acceptance: Backend visible; secret readable.

## C. shared-workflows v4 tag + canary (ENV-03)

- [ ] **C-1: Confirm plan-phase code deliverables landed** (CONTEXT D-18..D-20)
  - Action: Verify the following exist in `~/claude/MyAlterLego/shared-workflows`:
    - `environment: dev|prod` input on `deploy-firebase.yml` (default `prod`)
    - New reusable `db-migrate.yml` workflow
    - Per-app `apphosting.prod.yaml` overlay applied to admin
    - Updated `docs/onboarding-projects.md` Step 11
  - Acceptance: All four deliverables visible in the shared-workflows repo; `actionlint .github/workflows/*.yml` clean.
- [ ] **C-2: Push `dev` branch + tag `shared-workflows@v4` (after admin canary verifies)** (CONTEXT D-20, RESEARCH §Pitfall 3, §Pitfall 8)
  - Action: After admin-dev backend is live (B-1 done) and `apphosting.yaml` NEXTAUTH_URL is updated, push admin's `dev` branch to trigger the canary deploy. Verify admin-dev URL resolves and DB connectivity works. If green, tag v4:
    ```bash
    cd ~/claude/MyAlterLego/shared-workflows
    git checkout main && git pull
    git tag v4 <merged-commit-sha>
    git push origin v4
    ```
  - Acceptance: `git tag -l | grep v4` succeeds; `git tag -l | grep v3` still points at the v3 commit (unchanged — RESEARCH §Pitfall 8); admin-dev served a successful build from `dev` branch.
- [ ] **C-3: Bump consumer `ci-cd.yml` refs from `@v3` to `@v4` (incremental)** (CONTEXT D-21)
  - Action: Update each consumer's `.github/workflows/ci-cd.yml` to reference `@v4` and add `environment: ${{ github.ref_name == 'main' && 'prod' || 'dev' }}` input. Order: admin first (canary, already done in C-2 verify), then CRM, then portal, then customer-owned (truthtreason, darksouls, tmi, www) one at a time. Push each change; verify both prod and dev deploys work.
  - Acceptance: Each consumer's `ci-cd.yml` references `@v4`; first dev deploy on each consumer succeeds; prod deploys unchanged.

## D. Phase 4 UAT closure (ENV-05)

> Items D-1..D-4 are documented in detail in [`07.5-PHASE-4-UAT-CLOSURE.md`](./07.5-PHASE-4-UAT-CLOSURE.md). Use that file as the working scratchpad for setup steps and SQL verification queries; flip checkboxes here as each scenario passes.

- [ ] **D-0: Pre-req — push promote_attempts schema to admin_dev**
  - Action: Run `db-migrate.yml@v4` via GitHub Actions (workflow_dispatch) with `environment: dev`, `firebase_project_id: triarch-dev-website`, `consumer_repo: MyAlterLego/triarch-dev`. Or manually: `DB_URL=$(firebase apphosting:secrets:access DATABASE_URL_DEV --project triarch-dev-website | tr -d '\n') && DATABASE_URL=$DB_URL npx drizzle-kit push`.
  - Acceptance: `promote_attempts` table exists in admin_dev; `SELECT COUNT(*) FROM promote_attempts;` returns 0 initially.
- [ ] **D-1: Scenario A — Clean Rebase + CI + Merge** (see [PHASE-4-UAT-CLOSURE.md Scenario A](./07.5-PHASE-4-UAT-CLOSURE.md#scenario-a--clean-rebase--ci--merge-sc-1))
  - Expected: `promote_attempts.result=merged`, `merge_sha` populated, OttoBot thread shows checkmark.
- [ ] **D-2: Scenario B — Rebase Conflict** (see [PHASE-4-UAT-CLOSURE.md Scenario B](./07.5-PHASE-4-UAT-CLOSURE.md#scenario-b--rebase-conflict-sc-2))
  - Expected: `promote_attempts.result=conflict`, `conflict_files` non-empty, OttoBot thread shows warning with file list.
- [ ] **D-3: Scenario C — CI Failure** (see [PHASE-4-UAT-CLOSURE.md Scenario C](./07.5-PHASE-4-UAT-CLOSURE.md#scenario-c--ci-failure-sc-3))
  - Expected: `promote_attempts.result=ci_failed`, `ci_run_url` populated, main unchanged.
- [ ] **D-4: Scenario D — Concurrency** (see [PHASE-4-UAT-CLOSURE.md Scenario D](./07.5-PHASE-4-UAT-CLOSURE.md#scenario-d--concurrency-sc-4))
  - Expected: Two `promote_attempts` rows both `result=merged`, distinct `merge_sha` values, final main contains both branches' commits.
- [ ] **D-5: Mark `04-HUMAN-UAT.md` resolved** (CONTEXT D-24)
  - Action: Edit `.planning/phases/04-promote-branch-workflow/04-HUMAN-UAT.md` frontmatter `status: partial` → `status: resolved`; mark each scenario `result: passed (UAT 7.5 D-N)`.
  - Acceptance: `04-HUMAN-UAT.md` frontmatter shows `status: resolved`.

## Closure (when all checkboxes above are checked)

- [ ] Flip THIS file's frontmatter `status: pending` → `status: resolved`
- [ ] Flip `07.5-PHASE-4-UAT-CLOSURE.md` frontmatter `status: pending` → `status: resolved` and fill in `completed: <date>`
- [ ] Update `.planning/ROADMAP.md` Phase 7.5 row to `Complete`
- [ ] Append closure entry to `.planning/STATE.md` Decisions block

## Summary

total: 17
passed: 0
issues: 0
pending: 17
skipped: 0
blocked: 0
