---
phase: 18-portal-auth-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/claude/triarch/development/portal/package.json"
  - "~/claude/triarch/development/portal/package-lock.json"
  - "~/claude/triarch/development/portal/.npmrc"
  - "~/claude/triarch/development/portal/.gitignore"
  - "~/claude/triarch/development/portal/next.config.ts"
  - "~/claude/triarch/development/portal/tsconfig.json"
  - "~/claude/triarch/development/portal/postcss.config.mjs"
  - "~/claude/triarch/development/portal/eslint.config.mjs"
  - "~/claude/triarch/development/portal/vitest.config.ts"
  - "~/claude/triarch/development/portal/vitest.setup.ts"
  - "~/claude/triarch/development/portal/apphosting.yaml"
  - "~/claude/triarch/development/portal/apphosting.dev.yaml"
  - "~/claude/triarch/development/portal/.github/workflows/ci-cd.yml"
  - "~/claude/triarch/development/portal/src/app/layout.tsx"
  - "~/claude/triarch/development/portal/src/app/page.tsx"
  - "~/claude/triarch/development/portal/src/app/globals.css"
  - "~/claude/triarch/development/portal/next-env.d.ts"
autonomous: true
requirements:
  - AUTH-01
user_setup: []

must_haves:
  truths:
    - "Portal repo has a deployable Next.js scaffold with all required configs"
    - "Portal apphosting.yaml binds PORTAL_NEXTAUTH_SECRET to NEXTAUTH_SECRET env var (host-only cookie baseline)"
    - "Portal next build exits 0"
    - "First push to main triggers a real Firebase App Hosting deploy"
  artifacts:
    - path: "~/claude/triarch/development/portal/package.json"
      provides: "Portal app dependencies + scripts (no db:push, no db:generate)"
      contains: "triarch-portal"
    - path: "~/claude/triarch/development/portal/next.config.ts"
      provides: "Next.js config with transpilePackages for shared packages"
      contains: "transpilePackages"
    - path: "~/claude/triarch/development/portal/apphosting.yaml"
      provides: "FAH env binding (NEXTAUTH_SECRET → PORTAL_NEXTAUTH_SECRET)"
      contains: "PORTAL_NEXTAUTH_SECRET"
    - path: "~/claude/triarch/development/portal/.github/workflows/ci-cd.yml"
      provides: "CI/CD pipeline calling shared-workflows@v4"
      contains: "MyAlterLego/shared-workflows"
    - path: "~/claude/triarch/development/portal/src/app/layout.tsx"
      provides: "Root layout (skeleton — auth callout added in 18-03)"
      min_lines: 15
    - path: "~/claude/triarch/development/portal/src/app/page.tsx"
      provides: "Login wall placeholder (real routing logic added in 18-04)"
      min_lines: 5
  key_links:
    - from: "~/claude/triarch/development/portal/apphosting.yaml"
      to: "Firebase secret PORTAL_NEXTAUTH_SECRET"
      via: "secret: PORTAL_NEXTAUTH_SECRET binding"
      pattern: "PORTAL_NEXTAUTH_SECRET"
    - from: "~/claude/triarch/development/portal/.github/workflows/ci-cd.yml"
      to: "Firebase App Hosting backend portal-prod"
      via: "deploy-firebase.yml@v4 with firebase_project_id: triarch-dev-website + app_hosting_backend: portal-prod"
      pattern: "portal-prod"
---

<objective>
Scaffold the portal Next.js application from scratch in `~/claude/triarch/development/portal/`. Drop in package.json, configs (next.config.ts, tsconfig.json, postcss, eslint, vitest), apphosting.yaml + apphosting.dev.yaml, .github/workflows/ci-cd.yml, .npmrc, .gitignore, and skeleton src/app/layout.tsx + page.tsx + globals.css. NO NextAuth wiring yet (that's 18-02). NO signIn callback (18-03). Just a deployable shell that, when pushed to main, triggers a real Firebase App Hosting build.

Purpose: Establishes the deployable surface. Phase 15 created the empty repo + FAH backends + DNS + secrets; this plan lands the actual app code so portal-prod and portal-dev have something to serve. AUTH-01 (cookie config baseline) starts here — apphosting.yaml binds PORTAL_NEXTAUTH_SECRET to the NEXTAUTH_SECRET env var. The cookies block in src/lib/auth.ts comes in 18-02, but the secret plumbing must be correct before that.

Output: ~17 files in the portal repo. First commit creates triarch-portal v0.1.0. Push triggers FAH deploy of a near-blank Next.js app at portal.triarch.dev.

WORKING DIRECTORY: `~/claude/triarch/development/portal/` — NOT admin. Verify with `pwd` showing `.../development/portal` before any file write.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-portal-auth-scaffolding/18-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md

# Reference files in admin (read-only — copy patterns, do NOT modify)
@/Users/mikegeehan/claude/triarch/development/admin/package.json
@/Users/mikegeehan/claude/triarch/development/admin/next.config.ts
@/Users/mikegeehan/claude/triarch/development/admin/tsconfig.json
@/Users/mikegeehan/claude/triarch/development/admin/.npmrc
@/Users/mikegeehan/claude/triarch/development/admin/apphosting.yaml
@/Users/mikegeehan/claude/triarch/development/admin/apphosting.dev.yaml
@/Users/mikegeehan/claude/triarch/development/admin/.github/workflows/ci-cd.yml
@/Users/mikegeehan/claude/triarch/development/admin/vitest.config.ts

<interfaces>
<!-- Key contracts the executor needs. Use these directly — no codebase exploration needed. -->

From @myalterlego/triarch-shared (already published 0.1.0 — Phase 16):
- Subpath exports: `/auth`, `/db`, `/schema`, `/sanitize-commit`, `/slack-status`
- Consumer pattern: `import { getCurrentUserContext } from '@myalterlego/triarch-shared/auth';`
- Portal does NOT use file: dep — uses real GitHub Packages npm install
- transpilePackages required because subpath exports ship as ESM dist

Apphosting binding pattern (admin's pattern, mirror exactly):
```yaml
- variable: NEXTAUTH_SECRET
  secret: PORTAL_NEXTAUTH_SECRET   # different secret from admin's
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write portal package.json + .npmrc + .gitignore + tsconfig.json</name>
  <files>
    - ~/claude/triarch/development/portal/package.json
    - ~/claude/triarch/development/portal/.npmrc
    - ~/claude/triarch/development/portal/.gitignore
    - ~/claude/triarch/development/portal/tsconfig.json
  </files>
  <read_first>
    1. `cd ~/claude/triarch/development/portal/` and confirm pwd. CRITICAL: every Bash command in this task must use `cd ~/claude/triarch/development/portal/ && ...` or absolute paths in /Users/mikegeehan/claude/triarch/development/portal/.
    2. Read /Users/mikegeehan/claude/triarch/development/admin/package.json — copy dependency versions EXACTLY (next 16.2.2, react 19.2.4, drizzle-orm ^0.45.2, jose ^5, lucide-react ^1.7.0, next-auth ^4.24.13, pg ^8.20.0, swr ^2.4.1, @myalterlego/secrets ^0.1.0, @myalterlego/shared-ui ^1.2.0).
    3. Read /Users/mikegeehan/claude/triarch/development/admin/.npmrc — mirror exactly.
    4. Read /Users/mikegeehan/claude/triarch/development/admin/tsconfig.json — mirror exactly.
  </read_first>
  <action>
    Write `/Users/mikegeehan/claude/triarch/development/portal/package.json` with EXACT content:

    ```json
    {
      "name": "triarch-portal",
      "version": "0.1.0",
      "private": true,
      "scripts": {
        "dev": "next dev -p 3002",
        "build": "next build",
        "start": "next start",
        "lint": "eslint",
        "test": "vitest run",
        "test:watch": "vitest"
      },
      "dependencies": {
        "@myalterlego/secrets": "^0.1.0",
        "@myalterlego/triarch-shared": "^0.1.0",
        "@myalterlego/shared-ui": "^1.2.0",
        "drizzle-orm": "^0.45.2",
        "jose": "^5",
        "lucide-react": "^1.7.0",
        "next": "16.2.2",
        "next-auth": "^4.24.13",
        "pg": "^8.20.0",
        "react": "19.2.4",
        "react-dom": "19.2.4",
        "swr": "^2.4.1"
      },
      "devDependencies": {
        "@tailwindcss/postcss": "^4",
        "@testing-library/jest-dom": "^6.9.1",
        "@testing-library/react": "^16.3.2",
        "@testing-library/user-event": "^14.6.1",
        "@types/node": "^20",
        "@types/pg": "^8.20.0",
        "@types/react": "^19",
        "@types/react-dom": "^19",
        "@vitest/ui": "^4.1.5",
        "eslint": "^9",
        "eslint-config-next": "16.2.2",
        "jsdom": "^25.0.1",
        "tailwindcss": "^4",
        "tsx": "^4.21.0",
        "typescript": "^5",
        "vitest": "^4.1.5"
      }
    }
    ```

    EXPLICITLY ABSENT (per Phase 19 / DB-03 defense-in-depth): `db:push`, `db:generate`, `db:seed`, `drizzle-kit` devDep. Portal is read-only schema consumer.

    Write `/Users/mikegeehan/claude/triarch/development/portal/.npmrc` with EXACT content (mirror admin):
    ```
    @myalterlego:registry=https://npm.pkg.github.com
    //npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
    ```

    Write `/Users/mikegeehan/claude/triarch/development/portal/.gitignore` with EXACT content (Next.js + Node defaults):
    ```
    # dependencies
    /node_modules
    /.pnp
    .pnp.js

    # testing
    /coverage

    # next.js
    /.next/
    /out/

    # production
    /build

    # misc
    .DS_Store
    *.pem

    # debug
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*

    # local env files
    .env*.local
    .env

    # vercel
    .vercel

    # typescript
    *.tsbuildinfo
    next-env.d.ts
    ```

    Write `/Users/mikegeehan/claude/triarch/development/portal/tsconfig.json` IDENTICAL to admin's tsconfig.json (already loaded in context — copy exact content).

    Run `cd ~/claude/triarch/development/portal/ && npm install` to generate package-lock.json. Requires NODE_AUTH_TOKEN in environment OR `~/.npmrc` with auth — if missing, instruct user to set NODE_AUTH_TOKEN before retrying. Lockfile MUST be generated under Node 22 / npm 10 (verify with `node -v` && `npm -v`).
  </read_first>
  <verify>
    <automated>cd ~/claude/triarch/development/portal/ &amp;&amp; node -e "const p = require('./package.json'); if (p.name !== 'triarch-portal') process.exit(1); if (p.version !== '0.1.0') process.exit(1); if (p.scripts['db:push']) process.exit(2); if (p.scripts['db:generate']) process.exit(2); if (!p.dependencies['@myalterlego/triarch-shared']) process.exit(3); if (p.dependencies['@myalterlego/triarch-shared'].startsWith('file:')) process.exit(4); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json exists at portal root with name "triarch-portal" version "0.1.0"
    - dependencies include @myalterlego/triarch-shared ^0.1.0 (NOT file:) plus all admin runtime deps at exact same versions
    - scripts contain dev, build, start, lint, test, test:watch
    - scripts DO NOT contain db:push, db:generate, db:seed
    - .npmrc points @myalterlego at GitHub Packages with NODE_AUTH_TOKEN
    - .gitignore present with Next.js standard patterns
    - tsconfig.json byte-for-byte mirrors admin's tsconfig.json
    - package-lock.json generated; `npm ls @myalterlego/triarch-shared` resolves to a real installed version
  </acceptance_criteria>
  <done>Portal package.json + .npmrc + .gitignore + tsconfig.json all match admin's pattern with portal-specific differences (name, port, no db:push)</done>
</task>

<task type="auto">
  <name>Task 2: Write portal Next.js + build configs (next.config.ts, postcss, eslint, globals.css, layout.tsx, page.tsx)</name>
  <files>
    - ~/claude/triarch/development/portal/next.config.ts
    - ~/claude/triarch/development/portal/postcss.config.mjs
    - ~/claude/triarch/development/portal/eslint.config.mjs
    - ~/claude/triarch/development/portal/src/app/globals.css
    - ~/claude/triarch/development/portal/src/app/layout.tsx
    - ~/claude/triarch/development/portal/src/app/page.tsx
    - ~/claude/triarch/development/portal/next-env.d.ts (next dev/build will create this; do not hand-write)
  </files>
  <read_first>
    1. Confirm pwd is `~/claude/triarch/development/portal/`.
    2. Read /Users/mikegeehan/claude/triarch/development/admin/next.config.ts. Note: admin's has `transpilePackages: ['@myalterlego/shared-ui', '@myalterlego/triarch-shared']` and `serverExternalPackages: ['@google-cloud/secret-manager', '@myalterlego/secrets']`. Portal needs the same.
    3. Read /Users/mikegeehan/claude/triarch/development/admin/postcss.config.mjs (use Bash `cat` if not already in context).
    4. Read /Users/mikegeehan/claude/triarch/development/admin/eslint.config.mjs.
    5. Read /Users/mikegeehan/claude/triarch/development/admin/src/app/layout.tsx and globals.css to understand structure.
  </read_first>
  <action>
    Write `/Users/mikegeehan/claude/triarch/development/portal/next.config.ts` EXACTLY:
    ```ts
    import type { NextConfig } from "next";

    const nextConfig: NextConfig = {
      images: { unoptimized: true },
      transpilePackages: ['@myalterlego/shared-ui', '@myalterlego/triarch-shared'],
      serverExternalPackages: ['@google-cloud/secret-manager', '@myalterlego/secrets'],
    };

    export default nextConfig;
    ```

    Write `/Users/mikegeehan/claude/triarch/development/portal/postcss.config.mjs` mirroring admin's (read admin's exact content — likely a single tailwindcss postcss plugin reference).

    Write `/Users/mikegeehan/claude/triarch/development/portal/eslint.config.mjs` mirroring admin's config (next + typescript flat config).

    Write `/Users/mikegeehan/claude/triarch/development/portal/src/app/globals.css` with Tailwind v4 @import:
    ```css
    @import "tailwindcss";

    :root {
      --background: #09090b;
      --foreground: #fafafa;
    }

    body {
      background: var(--background);
      color: var(--foreground);
      font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
    }
    ```

    Write `/Users/mikegeehan/claude/triarch/development/portal/src/app/layout.tsx` (skeleton — staff callout wired in 18-03):
    ```tsx
    import type { Metadata } from 'next';
    import './globals.css';

    export const metadata: Metadata = {
      title: 'Triarch Portal',
      description: 'Customer release portal',
    };

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="en">
          <body>
            {children}
          </body>
        </html>
      );
    }
    ```

    Write `/Users/mikegeehan/claude/triarch/development/portal/src/app/page.tsx` (placeholder — real routing in 18-04):
    ```tsx
    export default function HomePage() {
      return (
        <main className="min-h-screen flex items-center justify-center">
          <div className="text-center">
            <h1 className="text-4xl font-bold tracking-widest uppercase text-teal-400">TRIARCH</h1>
            <p className="mt-2 text-sm text-zinc-500 uppercase tracking-widest">Customer Portal</p>
            <p className="mt-8 text-zinc-400">Scaffold deployed. Auth wiring lands in 18-02.</p>
          </div>
        </main>
      );
    }
    ```

    Run `cd ~/claude/triarch/development/portal/ && npx next build` to verify. Build MUST exit 0. If `next-env.d.ts` is missing, `next build` creates it (committed; portal repo's .gitignore lists it but admin commits it — match admin's actual git state by removing `next-env.d.ts` from .gitignore if needed, OR keep the gitignore line and let the user know it's a Next.js convention).
  </action>
  <verify>
    <automated>cd ~/claude/triarch/development/portal/ &amp;&amp; grep -q "transpilePackages.*@myalterlego/triarch-shared" next.config.ts &amp;&amp; grep -q "transpilePackages.*@myalterlego/shared-ui" next.config.ts &amp;&amp; npx next build 2>&amp;1 | tail -5 &amp;&amp; test -d .next</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts contains transpilePackages with both '@myalterlego/triarch-shared' and '@myalterlego/shared-ui'
    - next.config.ts contains serverExternalPackages with '@myalterlego/secrets' and '@google-cloud/secret-manager'
    - postcss.config.mjs + eslint.config.mjs present and mirror admin's structure
    - src/app/layout.tsx renders {children} with html lang="en" + body
    - src/app/page.tsx exports a default React component (placeholder content acceptable)
    - `npx next build` exits 0 with `.next/` directory created
  </acceptance_criteria>
  <done>Next.js scaffold builds clean; layout + skeleton page render; transpilePackages includes both shared packages</done>
</task>

<task type="auto">
  <name>Task 3: Write portal vitest config + apphosting yamls + ci-cd.yml; commit and push</name>
  <files>
    - ~/claude/triarch/development/portal/vitest.config.ts
    - ~/claude/triarch/development/portal/vitest.setup.ts
    - ~/claude/triarch/development/portal/apphosting.yaml
    - ~/claude/triarch/development/portal/apphosting.dev.yaml
    - ~/claude/triarch/development/portal/.github/workflows/ci-cd.yml
  </files>
  <read_first>
    1. Confirm pwd is `~/claude/triarch/development/portal/`.
    2. Read /Users/mikegeehan/claude/triarch/development/admin/vitest.config.ts (already in context). Note: admin's has packageTestRedirectPlugin because admin has `file:` dep on packages/triarch-shared. Portal consumes published 0.1.0 from GitHub Packages — NO file: dep, NO local packages/ dir. Portal's vitest config does NOT need the redirect plugin.
    3. Read /Users/mikegeehan/claude/triarch/development/admin/vitest.setup.ts.
    4. Read /Users/mikegeehan/claude/triarch/development/admin/apphosting.yaml + apphosting.dev.yaml.
    5. Read /Users/mikegeehan/claude/triarch/development/admin/.github/workflows/ci-cd.yml.
  </read_first>
  <action>
    Write `/Users/mikegeehan/claude/triarch/development/portal/vitest.config.ts` (simpler than admin — no dist redirect needed since portal uses real npm install of @myalterlego/triarch-shared, not a file: dep):
    ```ts
    import { defineConfig } from 'vitest/config';
    import path from 'path';

    export default defineConfig({
      resolve: {
        alias: {
          '@': path.resolve(__dirname, './src'),
        },
      },
      test: {
        environment: 'jsdom',
        setupFiles: ['./vitest.setup.ts'],
      },
    });
    ```

    Write `/Users/mikegeehan/claude/triarch/development/portal/vitest.setup.ts` mirroring admin's (jest-dom matcher import):
    ```ts
    import '@testing-library/jest-dom/vitest';
    ```

    Write `/Users/mikegeehan/claude/triarch/development/portal/apphosting.yaml` EXACTLY (note: NEXTAUTH_SECRET maps to PORTAL_NEXTAUTH_SECRET, NEXTAUTH_URL is portal's host, NO Slack/GitHub-App secrets — those are admin-only or Phase 22):
    ```yaml
    runConfig:
      runtime: nodejs22
      concurrency: 10
      cpu: 1
      memoryMiB: 512
      minInstances: 0
      maxInstances: 2

    env:
      - variable: NEXTAUTH_URL
        value: https://portal.triarch.dev
        availability:
          - BUILD
          - RUNTIME

      # Secrets
      - variable: DATABASE_URL
        secret: DATABASE_URL

      - variable: NEXTAUTH_SECRET
        secret: PORTAL_NEXTAUTH_SECRET

      - variable: GOOGLE_CLIENT_ID
        secret: GOOGLE_CLIENT_ID

      - variable: GOOGLE_CLIENT_SECRET
        secret: GOOGLE_CLIENT_SECRET

      # Build-time GitHub Packages auth — .npmrc reads ${NODE_AUTH_TOKEN}
      - variable: NODE_AUTH_TOKEN
        secret: GITHUB_PACKAGES_TOKEN
        availability:
          - BUILD
    ```

    Write `/Users/mikegeehan/claude/triarch/development/portal/apphosting.dev.yaml` (overlay for portal-dev backend, picked up when FAH backend's "Environment Name" = "dev"):
    ```yaml
    # apphosting.dev.yaml — dev environment overlay for portal-dev FAH backend
    # Auto-loaded when backend's Environment Name = "dev" in Firebase Console.
    # Merged on top of apphosting.yaml — only overrides listed.

    env:
      - variable: NEXTAUTH_URL
        value: https://portal-dev--triarch-dev-website.us-central1.hosted.app
        availability:
          - BUILD
          - RUNTIME

      - variable: DATABASE_URL
        secret: DATABASE_URL_DEV
    ```

    Write `/Users/mikegeehan/claude/triarch/development/portal/.github/workflows/ci-cd.yml` (mirrors admin's structure but targets portal-prod backend; flush-changelog removed as portal has no changelog flush yet):
    ```yaml
    name: Triarch Portal CI/CD

    on:
      push:
        branches:
          - main
          - 'release/**'
          - 'hotfix/**'
      pull_request:
        branches:
          - main

    concurrency:
      group: ci-${{ github.ref }}
      cancel-in-progress: true

    permissions:
      contents: write
      packages: read

    jobs:
      quality-gate:
        uses: MyAlterLego/shared-workflows/.github/workflows/quality-gate.yml@v1
        with:
          run_qa_tests: false
          run_pentest: false
          needs_server: false
        secrets: inherit

      deploy:
        needs: quality-gate
        if: github.event_name == 'push'
        uses: MyAlterLego/shared-workflows/.github/workflows/deploy-firebase.yml@v4
        with:
          firebase_project_id: triarch-dev-website
          app_hosting_backend: portal-prod
          deploy_command: apphosting
        secrets: inherit

      notify:
        needs: [quality-gate, deploy]
        if: always() && github.event_name == 'push'
        uses: MyAlterLego/shared-workflows/.github/workflows/notify.yml@v1
        with:
          status: ${{ needs.deploy.result }}
          project_name: Triarch Portal
          version: ${{ needs.deploy.outputs.version }}
          wait_for_fah: true
          app_hosting_backend: portal-prod
          firebase_project_id: triarch-dev-website
        secrets: inherit
    ```

    After ALL files in this plan are written, commit and push the entire scaffold from `~/claude/triarch/development/portal/`:
    ```bash
    cd ~/claude/triarch/development/portal/
    git add package.json package-lock.json .npmrc .gitignore tsconfig.json next.config.ts postcss.config.mjs eslint.config.mjs vitest.config.ts vitest.setup.ts apphosting.yaml apphosting.dev.yaml .github/workflows/ci-cd.yml src/
    git status
    git commit -m "v0.1.0: portal Next.js scaffold (AUTH-01 baseline)" --no-verify
    git remote -v   # confirm origin = https://github.com/MyAlterLego/triarch-portal.git
    git push origin main
    ```

    Watch the GitHub Actions run start; do NOT block on the deploy completing. The CI/CD pipeline kicking off proves the workflow is valid; the actual deploy verification (visiting portal.triarch.dev returning 200) is HUMAN-UAT after the run finishes (~5 min).
  </action>
  <verify>
    <automated>cd ~/claude/triarch/development/portal/ &amp;&amp; grep -q "PORTAL_NEXTAUTH_SECRET" apphosting.yaml &amp;&amp; ! grep -q "domain.*triarch.dev" apphosting.yaml &amp;&amp; grep -q "portal-prod" .github/workflows/ci-cd.yml &amp;&amp; grep -q "shared-workflows/.github/workflows/deploy-firebase.yml@v4" .github/workflows/ci-cd.yml &amp;&amp; npx vitest run --reporter=verbose 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists with `@/` alias to ./src and jsdom env
    - vitest.setup.ts imports @testing-library/jest-dom/vitest
    - apphosting.yaml maps `variable: NEXTAUTH_SECRET, secret: PORTAL_NEXTAUTH_SECRET` (NOT NEXTAUTH_SECRET:NEXTAUTH_SECRET)
    - apphosting.yaml has NO `domain` attribute referencing triarch.dev (cookie config baseline — Pitfall 1 guard)
    - apphosting.dev.yaml overrides NEXTAUTH_URL to portal-dev FAH default + DATABASE_URL_DEV
    - ci-cd.yml deploy job targets `app_hosting_backend: portal-prod` and `firebase_project_id: triarch-dev-website`
    - Vitest runs with zero collected tests (no tests yet — will be added in 18-05) and exits 0
    - Commit pushed to MyAlterLego/triarch-portal main; GitHub Actions run started
  </acceptance_criteria>
  <done>Portal scaffold deployed via FAH; first push to main triggered the full CI/CD pipeline; portal-prod backend now serves real Next.js code at portal.triarch.dev (HUMAN-UAT to verify 200-OK)</done>
</task>

</tasks>

<verification>
- `cd ~/claude/triarch/development/portal/ && npx next build` exits 0
- `cd ~/claude/triarch/development/portal/ && npx vitest run` exits 0 (zero tests, no failures)
- `grep -q "PORTAL_NEXTAUTH_SECRET" ~/claude/triarch/development/portal/apphosting.yaml` succeeds
- `grep -q "transpilePackages.*@myalterlego/triarch-shared" ~/claude/triarch/development/portal/next.config.ts` succeeds
- `grep -E "domain.*triarch.dev" ~/claude/triarch/development/portal/apphosting.yaml ~/claude/triarch/development/portal/src/` returns empty (no Domain= leakage configured)
- GitHub Actions run for the first commit kicked off (HUMAN-UAT: confirm portal.triarch.dev serves 200-OK once FAH publishes)
</verification>

<success_criteria>
- Portal repo has a complete deployable Next.js scaffold
- Apphosting binding correctly maps NEXTAUTH_SECRET env to PORTAL_NEXTAUTH_SECRET secret (AUTH-01 / AUTH-02 prep)
- CI/CD pipeline targets portal-prod backend on first push to main
- Cookie-leakage baseline preserved (no Domain attribute anywhere in portal config)
- Foundation ready for 18-02 (NextAuth wiring)
</success_criteria>

<output>
After completion, create `.planning/phases/18-portal-auth-scaffolding/18-01-SUMMARY.md` (in admin repo planning dir, NOT portal repo) documenting:
- Files created (full list)
- Commit SHA pushed to portal repo main
- GitHub Actions run URL (if deploy completed before summary written)
- Whether portal.triarch.dev returned 200-OK (HUMAN-UAT result if available)
- Any deviations from the plan with reason
</output>
