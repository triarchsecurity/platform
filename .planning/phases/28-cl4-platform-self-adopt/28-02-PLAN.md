---
phase: 28-cl4-platform-self-adopt
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - .github/workflows/ci-cd.yml
  - package.json
autonomous: true
requirements: ["CL4-01"]

must_haves:
  truths:
    - "platform ci-cd.yml declares a `version` job that extracts package.json version for downstream gating"
    - "platform ci-cd.yml declares a `gate` job that uses triarchsecurity/shared-workflows/.github/workflows/gate-prod-version.yml@v8.2"
    - "The `gate` job runs ONLY for prod deploys (push to main); skipped for dev (push to dev)"
    - "The `gate` job passes project_key=`triarch-dev` (the platform's own key per src/db/seed-projects.ts:27)"
    - "The `deploy` job's needs: array includes `gate`"
    - "deploy-dev path is unaffected (no gate dependency added for dev pushes)"
    - "package.json version bumped from 2.13.14 to 2.13.15"
  artifacts:
    - path: ".github/workflows/ci-cd.yml"
      provides: "gate job + needs:gate wiring on deploy"
      contains: "gate-prod-version.yml@v8.2"
    - path: "package.json"
      provides: "v2.13.15"
      contains: "\"version\": \"2.13.15\""
  key_links:
    - from: ".github/workflows/ci-cd.yml gate job"
      to: "shared-workflows v8.2 gate-prod-version.yml"
      via: "uses: triarchsecurity/shared-workflows/.github/workflows/gate-prod-version.yml@v8.2"
      pattern: "gate-prod-version.yml@v8.2"
    - from: ".github/workflows/ci-cd.yml gate job"
      to: "GitHub Actions secret ADMIN_API_TOKEN"
      via: "secrets: ADMIN_API_TOKEN"
      pattern: "ADMIN_API_TOKEN"
    - from: "deploy job"
      to: "gate job"
      via: "needs: array"
      pattern: "needs:.*gate"
---

<objective>
Wire `triarchsecurity/shared-workflows/.github/workflows/gate-prod-version.yml@v8.2` (from Plan 28-01) into platform's `.github/workflows/ci-cd.yml` as a `needs:` prerequisite of the prod deploy job. Add a small `version` job that extracts package.json version so the gate can compare it against admin's recorded dev/prod versions. Bump platform to v2.13.15 to satisfy workspace CLAUDE.md per-push version-bump rule.

Purpose: Platform self-adopts the CL-4 gate it just asked the rest of the portfolio to adopt (CONTEXT.md: "self-eats the dog food + provides golden template for phase 32").

Output: A modified `ci-cd.yml` that gates prod deploys through the Phase 27/28 CL-6 enforcement loop, plus a v2.13.15 commit on a feature branch in this repo.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/28-cl4-platform-self-adopt/28-CONTEXT.md
@.planning/phases/28-cl4-platform-self-adopt/28-01-PLAN.md
@.github/workflows/ci-cd.yml
@package.json
@src/db/seed-projects.ts

<interfaces>
<!-- shared-workflows v8.2 gate-prod-version.yml — caller contract (unchanged from v8.1) -->

inputs:
  project_key:      required, string (e.g. 'triarch-dev')
  target_version:   required, string (semver X.Y.Z, with or without 'v' prefix)
  admin_callback_url: optional, default 'https://admin.triarch.dev'
  min_dev_age_seconds: optional, default 300

secrets:
  ADMIN_API_TOKEN: required (per-project Bearer = projects.apiKey from admin CRDB)

outputs:
  dev_version, prod_version, verdict
</interfaces>

<critical_facts>
**Platform's project key** (verified by reading `src/db/seed-projects.ts:27`): `triarch-dev`. NOT `triarchsecurity-platform` (the CONTEXT.md hypothesis). The `projects` table row keyed `triarch-dev` is THIS repo (`name: "Triarch Dev"`, `githubRepo: "MyAlterLego/triarch-dev"`). The gate's GET to `/api/platform/version-snapshot` will look up this row via the apiKey, and the gate's POST verdict will write a `deploy_gate_check` row tagged with this key.

**Current package.json version**: `2.13.14` (confirmed). Next: `2.13.15` (patch — CI/CD wiring only, no app behavior change).

**Existing ci-cd.yml job graph** (verified by reading the file):
- `quality-gate` (no deps)
- `flush-changelog` (needs: quality-gate)
- `validate-apphosting` (needs: quality-gate)
- `env-select` (needs: quality-gate, validate-apphosting)
- `verify-dev-deployed` (needs: env-select, if: prod)
- `gate-prod` (needs: env-select, verify-dev-deployed, if: prod, environment: prod)
- `deploy` (needs: quality-gate, validate-apphosting, env-select, verify-dev-deployed, gate-prod, complex if:)
- `notify` (needs: quality-gate, deploy)

The new `gate` job (CL-4 wire-up — different from the existing `gate-prod` job!) inserts ALONGSIDE `verify-dev-deployed` and `gate-prod`, all prod-only. To avoid name collision with the existing `gate-prod` job, the new job is named `cl4-gate`. The deploy job's `needs:` array gains `cl4-gate`.
</critical_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `version` job + `cl4-gate` job to ci-cd.yml; extend deploy.needs</name>
  <read_first>
    - .github/workflows/ci-cd.yml (CURRENT v8.1 state — entire file; understand the existing 8-job graph before adding the 9th and 10th)
    - .planning/phases/28-cl4-platform-self-adopt/28-CONTEXT.md (locked decisions about job placement, if: guards, secrets)
    - package.json (to confirm current version 2.13.14 before bump)
    - src/db/seed-projects.ts (lines 25-42 — confirm project key `triarch-dev` for THIS repo)
  </read_first>
  <files>.github/workflows/ci-cd.yml</files>
  <action>
**Operates in:** `/Users/mikegeehan/claude/triarch/shared/platform` (current working directory).

Step 1 — Create the platform feature branch (separate from shared-workflows; this is a different repo):
```bash
git checkout -b feat/cl4-self-adopt-gate
git status   # confirm clean tree on the new branch
```

Step 2 — Insert a new `version` job AFTER the existing `validate-apphosting` job (which ends around line 66) and BEFORE the existing `env-select` job (around line 71). The new job extracts the package.json version once, so both the gate and any downstream observers can read it. Insert this block (keeping the empty-line separator pattern between jobs):

```yaml
  # Extracts package.json version ONCE for downstream consumers (cl4-gate).
  # Mirrors the inline extraction inside shared-workflows deploy-firebase.yml so
  # the gate input and the deployed version stay byte-identical.
  version:
    needs: quality-gate
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.extract.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - id: extract
        run: |
          VER=$(node -p "require('./package.json').version")
          if [ -z "$VER" ]; then
            echo "::error::package.json version is empty"
            exit 1
          fi
          echo "version=$VER" >> $GITHUB_OUTPUT
          echo "Extracted version: $VER"

```

Step 3 — Insert the new `cl4-gate` job AFTER the existing `verify-dev-deployed` job (which ends around line 117) and BEFORE the existing `gate-prod` job (around line 121). This places the CL-4 verdict POST adjacent to the existing prod-only gating logic. Insert this exact block:

```yaml
  # CL-4 platform self-adopt (Phase 28). Calls shared-workflows v8.2 which:
  #   1. GETs /api/platform/version-snapshot (dev + prod recorded versions)
  #   2. Enforces 5 invariants (INV-1..INV-5) — fails the job if any violated
  #   3. POSTs the verdict to /api/platform/cicd/gate-verdict (CL-6 paired-verdict)
  # Prod-only; dev pushes skip this entirely. Project key matches admin's
  # `projects.key = triarch-dev` row (this repo's own registry entry).
  cl4-gate:
    needs: [env-select, version]
    if: needs.env-select.outputs.environment == 'prod'
    uses: triarchsecurity/shared-workflows/.github/workflows/gate-prod-version.yml@v8.2
    with:
      project_key: triarch-dev
      target_version: ${{ needs.version.outputs.version }}
    secrets:
      ADMIN_API_TOKEN: ${{ secrets.ADMIN_API_TOKEN }}

```

Step 4 — Modify the existing `deploy` job (currently starts at line 129). The `needs:` array on line 130 currently reads:
```yaml
    needs: [quality-gate, validate-apphosting, env-select, verify-dev-deployed, gate-prod]
```

Change it to ALSO include `cl4-gate` (preserving all existing entries):
```yaml
    needs: [quality-gate, validate-apphosting, version, env-select, verify-dev-deployed, gate-prod, cl4-gate]
```

(Note: `version` is also added so the deploy job remains in the same wave as cl4-gate, even though deploy doesn't directly read version.outputs — Actions requires every transitively-needed job to be in the same `needs:` array if any conditional checks `needs.<job>.result`.)

Step 5 — Extend the existing `deploy.if:` expression (lines 137-145 in v8.1). The current expression ends with:
```yaml
      (needs.verify-dev-deployed.result == 'success' || needs.verify-dev-deployed.result == 'skipped')
```

Append an AND clause for cl4-gate, preserving the `skipped` allowance for dev pushes (cl4-gate is prod-only). The resulting expression's final two clauses become:
```yaml
      (needs.verify-dev-deployed.result == 'success' || needs.verify-dev-deployed.result == 'skipped') &&
      (needs.cl4-gate.result == 'success' || needs.cl4-gate.result == 'skipped')
```

The full revised deploy.if: (insert in place of lines 137-145):
```yaml
    if: |
      always() &&
      github.event_name == 'push' &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') &&
      needs.quality-gate.result == 'success' &&
      needs.validate-apphosting.result == 'success' &&
      needs.env-select.result == 'success' &&
      (needs.gate-prod.result == 'success' || needs.gate-prod.result == 'skipped') &&
      (needs.verify-dev-deployed.result == 'success' || needs.verify-dev-deployed.result == 'skipped') &&
      (needs.cl4-gate.result == 'success' || needs.cl4-gate.result == 'skipped')
```

Step 6 — Do NOT modify:
- `quality-gate`, `flush-changelog`, `validate-apphosting`, `env-select`, `verify-dev-deployed`, `gate-prod`, `notify` job bodies
- The `deploy` job's `uses:`, `with:`, `secrets:` blocks (only `needs:` and `if:` change)
- Any other file

Step 7 — Read the resulting file back end-to-end to confirm:
- Total job count went from 8 to 10 (added: `version`, `cl4-gate`)
- The string `gate-prod-version.yml@v8.2` appears EXACTLY once
- The string `project_key: triarch-dev` appears EXACTLY once
- `ADMIN_API_TOKEN` appears EXACTLY once (only in cl4-gate's secrets:)
- YAML parses
  </action>
  <verify>
    <automated>grep -c "^  version:$" .github/workflows/ci-cd.yml | grep -q "^1$" && grep -c "^  cl4-gate:$" .github/workflows/ci-cd.yml | grep -q "^1$" && grep -c "gate-prod-version.yml@v8.2" .github/workflows/ci-cd.yml | grep -q "^1$" && grep -c "project_key: triarch-dev" .github/workflows/ci-cd.yml | grep -q "^1$" && grep -c "ADMIN_API_TOKEN" .github/workflows/ci-cd.yml | grep -q "^1$" && grep -E "needs: \[.*cl4-gate.*\]" .github/workflows/ci-cd.yml | head -1 | grep -q "cl4-gate" && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-cd.yml')); print('YAML OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^  version:$" .github/workflows/ci-cd.yml` returns `1`
    - `grep -c "^  cl4-gate:$" .github/workflows/ci-cd.yml` returns `1`
    - `grep -c "gate-prod-version.yml@v8.2" .github/workflows/ci-cd.yml` returns `1`
    - `grep -c "project_key: triarch-dev" .github/workflows/ci-cd.yml` returns `1`
    - `grep -c "ADMIN_API_TOKEN" .github/workflows/ci-cd.yml` returns `1`
    - `grep -E "needs: \[.*cl4-gate.*\]" .github/workflows/ci-cd.yml` returns ≥ 1 line (deploy job's needs includes cl4-gate)
    - `grep "needs.cl4-gate.result" .github/workflows/ci-cd.yml | wc -l` returns ≥ `1` (deploy.if: includes the cl4-gate skipped-or-success guard)
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-cd.yml'))"` exits 0
    - Existing `gate-prod` job is NOT renamed (`grep -c "^  gate-prod:$" .github/workflows/ci-cd.yml` returns `1`)
    - `quality-gate` line count unchanged (`grep -c "^  quality-gate:$"` returns `1`)
  </acceptance_criteria>
  <done>ci-cd.yml has 10 jobs, the new cl4-gate calls v8.2 with correct project_key=triarch-dev, deploy.needs and deploy.if are extended, existing jobs untouched, YAML still parses.</done>
</task>

<task type="auto">
  <name>Task 2: Bump package.json to v2.13.15 and commit on feature branch</name>
  <read_first>
    - package.json (confirm current version is 2.13.14)
    - .github/workflows/ci-cd.yml (just-modified file — confirm shape before commit)
    - ~/claude/CLAUDE.md (workspace commit format `vX.Y.Z: description`, PR vs dev not main)
  </read_first>
  <files>package.json</files>
  <action>
**Operates in:** `/Users/mikegeehan/claude/triarch/shared/platform` (current working directory).

Step 1 — Bump version from 2.13.14 to 2.13.15 (patch). Use `npm version --no-git-tag-version` to do an atomic edit that preserves package.json formatting:
```bash
npm version --no-git-tag-version 2.13.15
```

The `--no-git-tag-version` flag is mandatory — workspace CLAUDE.md says only push-time scripts create tags, and the commit hasn't happened yet.

Step 2 — Verify the bump landed correctly:
```bash
npm pkg get version   # must output "2.13.15"
git diff --stat package.json   # 1 file changed, 1 insertion, 1 deletion
```

If `package-lock.json` also changed (npm sometimes touches it), stage it too — both are part of the version bump.

Step 3 — Stage both files and commit. Per workspace CLAUDE.md commit format `vX.Y.Z: description`:
```bash
git add .github/workflows/ci-cd.yml package.json
# If package-lock.json was also modified by npm version, include it:
git status --short | grep package-lock.json && git add package-lock.json
git commit -m "v2.13.15: feat(ci-cd): wire gate-prod-version@v8.2 as needs: of prod deploy (CL-4 platform self-adopt)"
```

Per workspace CLAUDE.md "Never push to remote without explicit user approval" — DO NOT run `git push`. Plan 28-03 captures the push + PR-vs-dev step as HUMAN-UAT.

Step 4 — Verify the commit:
```bash
git log -1 --format="%H %s"   # shows v2.13.15 commit
git status                     # clean tree
git rev-parse --abbrev-ref HEAD   # echoes feat/cl4-self-adopt-gate
```
  </action>
  <verify>
    <automated>npm pkg get version | grep -q '"2.13.15"' && git log -1 --format="%s" | grep -q "^v2.13.15: feat(ci-cd): wire gate-prod-version@v8.2" && git status --porcelain | wc -l | grep -q "^[[:space:]]*0$" && git rev-parse --abbrev-ref HEAD | grep -q "^feat/cl4-self-adopt-gate$" && git log -1 --stat | grep -q "ci-cd.yml" && git log -1 --stat | grep -q "package.json"</automated>
  </verify>
  <acceptance_criteria>
    - `npm pkg get version` outputs `"2.13.15"`
    - `git log -1 --format="%s"` STARTS WITH `v2.13.15: feat(ci-cd): wire gate-prod-version@v8.2`
    - `git status --porcelain | wc -l` returns `0` (clean tree)
    - `git rev-parse --abbrev-ref HEAD` echoes `feat/cl4-self-adopt-gate`
    - `git log -1 --stat | grep -c "ci-cd.yml"` returns ≥ `1`
    - `git log -1 --stat | grep -c "package.json"` returns ≥ `1`
    - No `git push` has been executed
  </acceptance_criteria>
  <done>v2.13.15 committed on local feat/cl4-self-adopt-gate branch; commit contains both ci-cd.yml + package.json (+ package-lock.json if npm touched it); working tree clean; no remote push.</done>
</task>

</tasks>

<verification>
- ci-cd.yml parses as valid YAML and has exactly 10 jobs
- `cl4-gate` job exists and uses v8.2 of the shared workflow
- `deploy.needs` array includes `cl4-gate`
- `project_key: triarch-dev` is the platform's own key per src/db/seed-projects.ts:27
- package.json reports version 2.13.15
- Single feature-branch commit `v2.13.15: feat(ci-cd): ...` exists locally
- No remote push has happened (user-action item, captured in Plan 28-03's SUMMARY)
</verification>

<success_criteria>
- ci-cd.yml has cl4-gate job calling `triarchsecurity/shared-workflows/.github/workflows/gate-prod-version.yml@v8.2`
- cl4-gate runs only when `env-select.outputs.environment == 'prod'`
- deploy job's `needs:` array contains `cl4-gate` and `version`
- deploy job's `if:` allows cl4-gate to be `success` OR `skipped` (so dev pushes still deploy)
- package.json version = `2.13.15`
- Commit message starts with `v2.13.15: feat(ci-cd): wire gate-prod-version@v8.2`
- Branch name: `feat/cl4-self-adopt-gate`
</success_criteria>

<output>
After completion, the executor should NOT create a 28-02-SUMMARY.md as part of this plan (the SUMMARY work is handled by Plan 28-03 which writes the consolidated phase-close summary listing both 28-01 and 28-02 deliverables alongside HUMAN-UAT).
</output>
