---
phase: 28-cl4-platform-self-adopt
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - .planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md
  - .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md
autonomous: true
requirements: ["CL4-01"]

must_haves:
  truths:
    - "YAML validation on ci-cd.yml passes (parses without error)"
    - "All 6 wiring grep checks pass on the modified ci-cd.yml"
    - "package.json reports v2.13.15"
    - "A phase-level 28-SUMMARY.md exists consolidating Plan 28-01 + 28-02 deliverables"
    - "A 28-HUMAN-UAT.md exists enumerating every human-required step for Phase 28 operational completion"
    - "HUMAN-UAT covers: shared-workflows push/PR/merge/tag + platform push/PR-vs-dev + ADMIN_API_TOKEN secret + project_key verification + Phase 27 migration 0019 carryover + contrived dry-run + real prod-deploy verification + CL6_ENFORCEMENT_MODE flip plan"
  artifacts:
    - path: ".planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md"
      provides: "Phase-level summary referencing 28-01 + 28-02 SUMMARYs"
      contains: "Phase 28 CL-4 Platform Self-Adopt"
    - path: ".planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md"
      provides: "Operational completion checklist for Mike"
      contains: "HUMAN-UAT Items"
  key_links:
    - from: "28-SUMMARY.md"
      to: "28-HUMAN-UAT.md"
      via: "explicit Markdown link"
      pattern: "28-HUMAN-UAT.md"
    - from: "28-HUMAN-UAT.md ADMIN_API_TOKEN section"
      to: "admin CRDB projects.apiKey for triarch-dev project_key"
      via: "instruction text + SQL query"
      pattern: "apiKey.*triarch-dev"
---

<objective>
Verify Plan 28-02's edits are well-formed (YAML parses, all wiring grep checks pass, version bumped) and write the phase-close artifacts: a consolidated `28-SUMMARY.md` and a complete `28-HUMAN-UAT.md` enumerating every human step required for Phase 28 to be operationally live.

Purpose: Phase 28 is structurally complete when Wave 1+2 commits exist, but it's NOT operationally complete until the user pushes, merges, tags v8.2, sets the GitHub secret, applies the lingering Phase 27 migration, and runs the contrived + real verification deploys. This plan ensures none of those items are lost.

Output: Two markdown artifacts that close the phase and hand off cleanly to Mike's terminal.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-cl4-platform-self-adopt/28-CONTEXT.md
@.planning/phases/28-cl4-platform-self-adopt/28-01-PLAN.md
@.planning/phases/28-cl4-platform-self-adopt/28-02-PLAN.md
@.github/workflows/ci-cd.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated verification checks against Plan 28-02 output</name>
  <read_first>
    - .github/workflows/ci-cd.yml (the file under verification — confirm Plan 28-02's edits are intact)
    - package.json (confirm version 2.13.15)
    - .planning/phases/28-cl4-platform-self-adopt/28-02-PLAN.md (acceptance criteria reference)
  </read_first>
  <files>(none — verification only)</files>
  <action>
**Operates in:** `/Users/mikegeehan/claude/triarch/shared/platform` (current working directory).

Run each of the following checks in sequence. Each must pass. If ANY fails, STOP and report — do NOT write the SUMMARY or UAT artifacts on a broken base.

Check 1 — YAML validity:
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-cd.yml')); print('YAML OK')"
```
Expect: `YAML OK`, exit 0.

Check 2 — gate workflow ref pinned to v8.2:
```bash
grep -c "gate-prod-version.yml@v8.2" .github/workflows/ci-cd.yml
```
Expect: `1`.

Check 3 — cl4-gate job declared:
```bash
grep -c "^  cl4-gate:$" .github/workflows/ci-cd.yml
```
Expect: `1`.

Check 4 — version job declared (Plan 28-02 Task 1 Step 2):
```bash
grep -c "^  version:$" .github/workflows/ci-cd.yml
```
Expect: `1`.

Check 5 — project_key matches platform's own registry key:
```bash
grep -c "project_key: triarch-dev" .github/workflows/ci-cd.yml
```
Expect: `1`.

Check 6 — ADMIN_API_TOKEN secret referenced:
```bash
grep -c "ADMIN_API_TOKEN" .github/workflows/ci-cd.yml
```
Expect: `1`.

Check 7 — deploy job's needs: includes cl4-gate (single-line array form):
```bash
grep -E "needs: \[.*cl4-gate.*\]" .github/workflows/ci-cd.yml | wc -l
```
Expect: ≥ `1`.

Check 8 — deploy.if: allows cl4-gate skipped (so dev path still deploys):
```bash
grep -c "needs.cl4-gate.result" .github/workflows/ci-cd.yml
```
Expect: ≥ `1`.

Check 9 — package.json version:
```bash
npm pkg get version | grep -q '"2.13.15"' && echo "version OK"
```
Expect: `version OK`.

Check 10 — Both Plan 28-01 (shared-workflows) and Plan 28-02 (platform) commits exist locally on their respective feature branches:
```bash
git log -1 --format="%s" | grep -q "^v2.13.15: feat(ci-cd): wire gate-prod-version@v8.2" && echo "platform commit OK"
git -C /Users/mikegeehan/claude/triarch/shared/shared-workflows log -1 --format="%s" | grep -q "^v8.2: gate-prod-version posts verdict to /api/platform/cicd/gate-verdict" && echo "shared-workflows commit OK"
```
Expect: both echo `OK` lines.

If all 10 checks pass, proceed to Tasks 2 and 3. If any fail, output a structured failure report (which check failed, what was expected, what was observed) and exit without writing artifacts.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-cd.yml'))" && [ "$(grep -c 'gate-prod-version.yml@v8.2' .github/workflows/ci-cd.yml)" = "1" ] && [ "$(grep -c '^  cl4-gate:$' .github/workflows/ci-cd.yml)" = "1" ] && [ "$(grep -c '^  version:$' .github/workflows/ci-cd.yml)" = "1" ] && [ "$(grep -c 'project_key: triarch-dev' .github/workflows/ci-cd.yml)" = "1" ] && [ "$(grep -c 'ADMIN_API_TOKEN' .github/workflows/ci-cd.yml)" = "1" ] && npm pkg get version | grep -q '"2.13.15"'</automated>
  </verify>
  <acceptance_criteria>
    - All 10 checks listed in `<action>` execute and each passes per the documented expectation
    - YAML parses
    - All grep counts match exactly
    - npm pkg get version reports `2.13.15`
    - Both feature-branch commits exist (platform + shared-workflows)
  </acceptance_criteria>
  <done>All automated verification checks pass; the executor can proceed to write Phase artifacts. If any check fails, no artifacts are written and the failure is reported.</done>
</task>

<task type="auto">
  <name>Task 2: Write phase-level 28-SUMMARY.md</name>
  <read_first>
    - .planning/phases/28-cl4-platform-self-adopt/28-01-PLAN.md (for plan 01 must_haves + tasks)
    - .planning/phases/28-cl4-platform-self-adopt/28-01-SUMMARY.md (if created by Plan 28-01 — reference, not regenerate)
    - .planning/phases/28-cl4-platform-self-adopt/28-02-PLAN.md (for plan 02 must_haves + tasks)
    - .planning/phases/27-cl6-server-side-adoption/27-03-SUMMARY.md (template for Triarch SUMMARY frontmatter style)
  </read_first>
  <files>.planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md</files>
  <action>
**Operates in:** `/Users/mikegeehan/claude/triarch/shared/platform` (current working directory).

Use the Write tool to create `.planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md`. The file MUST start with a YAML frontmatter block (delimited by lines of three hyphens at column 0), followed by a markdown body.

**Critical formatting constraint:** Do NOT include any markdown horizontal-rule lines (three consecutive hyphens at column 0) anywhere in the body. Use H2 headers and blank lines as visual separators instead. The only three-hyphen lines in the entire file are the two frontmatter delimiters (file-opening and frontmatter-closing). The same applies inside ANY embedded code blocks — do not paste sample YAML frontmatter that contains its own `---` lines at column 0.

**Frontmatter fields (substitute literal three-hyphen delimiters around this block):**

```
phase: 28-cl4-platform-self-adopt
type: phase-summary
plans_shipped: ["28-01", "28-02"]
plans_followup: ["28-03"]
subsystems: [shared-workflows, ci-cd, platform]
tags: [cl4, cl6, gate, self-adopt, golden-template, cross-repo, v8.2]
dependency_graph:
  requires:
    - Phase 27 endpoint POST /api/platform/cicd/gate-verdict (admin v2.13.14 — code shipped)
    - Phase 27 schema deployGateCheck (migration 0019 — PENDING human apply to CRDB; carry-over from Phase 27)
    - shared-workflows v8.1 baseline (gate-prod-version.yml + GET version-snapshot path)
  provides:
    - shared-workflows v8.2 candidate (local feature branch commit, awaiting human push/PR/merge/tag)
    - Platform ci-cd.yml cl4-gate job pinned to @v8.2 (local feature branch commit, awaiting human push/PR-vs-dev)
    - Golden template for Phase 32 (consumer rollout) — copy the cl4-gate + version jobs into dev-portal, darksouls-rpg, tmi, truthtreason
  affects:
    - /Users/mikegeehan/claude/triarch/shared/shared-workflows/.github/workflows/gate-prod-version.yml (+ ~65 lines)
    - .github/workflows/ci-cd.yml (+ ~30 lines, 2 modified clauses)
    - package.json (2.13.14 -> 2.13.15)
tech_stack:
  added: []
  patterns:
    - Reusable shared-workflow gate pinned by tag (v8.2) — consumer pinning model preserved
    - Pre-deploy version extraction in a dedicated `version` job — deterministic input to the gate
    - Job name `cl4-gate` chosen to avoid collision with existing `gate-prod` GitHub Environment job
    - Skipped-OR-success allowance in deploy.if keeps dev path untouched
    - project_key=triarch-dev sourced from src/db/seed-projects.ts:27 (NOT CONTEXT.md hypothesis triarchsecurity-platform)
key_files:
  modified:
    - /Users/mikegeehan/claude/triarch/shared/shared-workflows/.github/workflows/gate-prod-version.yml
    - .github/workflows/ci-cd.yml
    - package.json
  created:
    - .planning/phases/28-cl4-platform-self-adopt/28-01-SUMMARY.md (Plan 01)
    - .planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md (this file)
    - .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md (Task 3 of Plan 03)
decisions:
  - "Platform project_key in admin projects table is `triarch-dev` (verified by reading src/db/seed-projects.ts:27). The CONTEXT.md hypothesis `triarchsecurity-platform` was wrong — no such row in the seed."
  - "New ci-cd.yml job named `cl4-gate` (not `gate`) to avoid collision with existing `gate-prod` GitHub Environment job."
  - "Added a dedicated `version` job that extracts package.json once, rather than relying on deploy-firebase.yml's late extraction."
  - "deploy.if extended with cl4-gate success-or-skipped so dev pushes still deploy."
  - "shared-workflows is tag-only versioning. Commit uses `v8.2:` prefix; tag created post-merge as HUMAN follow-up."
  - "Per workspace CLAUDE.md, no git push executed in either repo. All push/PR/merge/tag/secret operations enumerated in 28-HUMAN-UAT.md."
requirements: ["CL4-01"]
metrics:
  duration: "~10 minutes (Wave 1 + Wave 2 + Wave 3)"
  completed_date: "2026-05-16"
  plans: 3
  tasks_total: 8
  files_modified: 3
  files_created: 3
```

**Body sections (use H2 headers, no horizontal-rule separators):**

H1: `# Phase 28: CL-4 Platform Self-Adopt — Phase Summary`

Paragraph: `**One-liner:** Platform self-adopts the CL-4 prod-deploy gate it just built. Two repos modified: shared-workflows v8.1 to v8.2 (additive verdict POST step) and platform ci-cd.yml (cl4-gate job + version job + deploy.needs/deploy.if extensions). v2.13.15 ships the wiring on a local feature branch; remote operations and live verification are HUMAN-UAT.`

H2: `## What Shipped (Autonomous)`

Subheading + bullets for Plan 28-01 (Wave 1) shared-workflows v8.2:
- New step `Record verdict to admin (CL-6)` inserted between Compare + Audit steps
- Step POSTs `{target_version, verdict, dev_version, workflow_run_url}` to `${ADMIN_CALLBACK_URL}/api/platform/cicd/gate-verdict`
- `if: always() + continue-on-error: true` — fires on pass + fail, admin downtime never blocks deploys
- Header comment updated with v8.2 entry
- Local commit `v8.2: gate-prod-version posts verdict to /api/platform/cicd/gate-verdict (CL-6 enforcement)` on `feat/v8.2-cl6-verdict-post` branch in `/Users/mikegeehan/claude/triarch/shared/shared-workflows`
- See: `28-01-SUMMARY.md`

Subheading + bullets for Plan 28-02 (Wave 2) platform ci-cd.yml wire-up:
- New `version` job extracts package.json version (needs: quality-gate)
- New `cl4-gate` job uses `triarchsecurity/shared-workflows/.github/workflows/gate-prod-version.yml@v8.2`
  - inputs: `project_key: triarch-dev`, `target_version: ${{ needs.version.outputs.version }}`
  - secret: `ADMIN_API_TOKEN`
  - guard: `if: needs.env-select.outputs.environment == 'prod'` (prod-only)
- `deploy.needs: [...]` extended to include `version` and `cl4-gate`
- `deploy.if:` extended with `(needs.cl4-gate.result == 'success' || needs.cl4-gate.result == 'skipped')` — preserves dev deploy path
- package.json bumped 2.13.14 to 2.13.15
- Local commit `v2.13.15: feat(ci-cd): wire gate-prod-version@v8.2 as needs: of prod deploy (CL-4 platform self-adopt)` on `feat/cl4-self-adopt-gate` branch
- See: this SUMMARY

Subheading + bullets for Plan 28-03 (Wave 3) verification + phase close:
- All 10 automated verification checks passed (YAML parse, grep counts, version bump, both commits exist)
- This SUMMARY written
- `28-HUMAN-UAT.md` written with the complete operational-completion checklist

H2: `## Out of Scope (Captured in 28-HUMAN-UAT.md)`

Bullet list:
- Pushing either feature branch (no remote operation per workspace CLAUDE.md)
- Opening the two PRs (shared-workflows vs main; platform vs dev)
- Creating the `v8.2` git tag on shared-workflows
- Setting the `ADMIN_API_TOKEN` GitHub Actions secret on `MyAlterLego/triarch-dev`
- Applying Phase 27's migration 0019 to CRDB (carry-over from Phase 27 — gates non-empty deploy_gate_check table)
- Running the contrived dry-run (push a fake high version) to verify INV-2 blocks
- Running a real v2.13.15+ prod deploy and verifying the gate passes + deploy_gate_check row lands
- Flipping `CL6_ENFORCEMENT_MODE` from warn to enforce (separate operational decision after 7-day grace per Phase 27 D-Rollout)

See: `.planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md`

H2: `## Self-Eats-The-Dog-Food Architecture`

Paragraph: `The round-trip closure is now end-to-end discoverable. Platform CI on push to main runs the version job (extracts 2.13.15), then the cl4-gate job (uses shared-workflows v8.2: GETs admin /api/platform/version-snapshot, enforces INV-1..INV-5, POSTs admin /api/platform/cicd/gate-verdict to write the paired-verdict row), then the deploy job (needs cl4-gate success-or-skipped). deploy-firebase emits a release_logs ingest, and admin /api/platform/ingest/release-logs runs the CL-6 pre-check that reads the deploy_gate_check row (15-min lookback, same project_key + target_version + apiKey). If match: insert release_logs row (warn or enforce mode). If mismatch: 409 (enforce mode) or audit-and-pass (warn mode).`

Paragraph: `Phase 28 is the consumer side; Phase 27 was the producer side. The loop closes here.`

H2: `## Golden Template for Phase 32`

Paragraph: `Phase 32 (CL-4 roll to consumers: dev-portal, darksouls-rpg, tmi, truthtreason) copies the cl4-gate + version job blocks from this repo's ci-cd.yml verbatim, changing only project_key: and the GitHub Actions secret binding.`

Use the Write tool in one shot; the file should end with a final newline after the last paragraph.
  </action>
  <verify>
    <automated>test -f .planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md && grep -q "Phase 28: CL-4 Platform Self-Adopt" .planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md && grep -q 'plans_shipped: \["28-01", "28-02"\]' .planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md && grep -q "project_key=triarch-dev" .planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md && grep -q "28-HUMAN-UAT.md" .planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md && [ "$(grep -c '^---$' .planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md)" = "2" ] && python3 -c "import yaml; doc=open('.planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md').read().split('---')[1]; yaml.safe_load(doc); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md` exists
    - `grep -q "Phase 28: CL-4 Platform Self-Adopt"` succeeds
    - `grep -q 'plans_shipped: \["28-01", "28-02"\]'` succeeds
    - `grep -q "project_key=triarch-dev"` succeeds (correct platform key documented)
    - `grep -q "28-HUMAN-UAT.md"` succeeds (cross-link present)
    - Exactly 2 lines match `^---$` (no horizontal-rule pollution)
    - YAML frontmatter valid (yaml.safe_load on the first --- block exits 0)
    - File has `requirements: ["CL4-01"]` in frontmatter (`grep 'requirements:' 28-SUMMARY.md | grep -q "CL4-01"`)
  </acceptance_criteria>
  <done>Phase-level SUMMARY written with EXACTLY 2 three-hyphen lines (frontmatter only); frontmatter valid; requirements traceability present; cross-link to UAT file present.</done>
</task>

<task type="auto">
  <name>Task 3: Write 28-HUMAN-UAT.md with complete operational-completion checklist</name>
  <read_first>
    - .planning/phases/27-cl6-server-side-adoption/27-HUMAN-UAT.md (template for Triarch HUMAN-UAT format)
    - .planning/phases/28-cl4-platform-self-adopt/28-01-SUMMARY.md (lists H-1..H-4 shared-workflows steps; this UAT consolidates them)
    - .planning/phases/28-cl4-platform-self-adopt/28-CONTEXT.md (the "What This Phase Does NOT Do" section — every line in there becomes a HUMAN-UAT item)
  </read_first>
  <files>.planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md</files>
  <action>
**Operates in:** `/Users/mikegeehan/claude/triarch/shared/platform`.

Use the Write tool to create `.planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md`. The file MUST start with a YAML frontmatter block (delimited by three-hyphen lines at column 0), followed by a markdown body.

**Critical formatting constraint:** Do NOT include any markdown horizontal-rule lines (three consecutive hyphens at column 0) anywhere in the body. Use H2 headers and blank lines as visual separators instead. The only three-hyphen lines in the entire file are the two frontmatter delimiters.

**Frontmatter fields (substitute literal three-hyphen delimiters around this block):**

```
phase: 28-cl4-platform-self-adopt
type: human-uat
status: pending
generated: 2026-05-16
related_summary: ./28-SUMMARY.md
```

**Body sections:**

H1: `# Phase 28: CL-4 Platform Self-Adopt — Human UAT Checklist`

Paragraph: `Plan 28-01 + 28-02 ship the code; Plan 28-03 verifies the code. **None of those steps cross a network or touch a live system.** This file enumerates every action Mike must complete for Phase 28 to be operationally live.`

Paragraph: `Work top-to-bottom — most items have a strict ordering dependency.`

H2: `## Section A: Carry-Over From Phase 27 (Must Be Done First)`

Intro: `These items were captured in .planning/phases/27-cl6-server-side-adoption/27-HUMAN-UAT.md and remain pending. Plan 28's contrived dry-run depends on them.`

Checklist:
- [ ] **A-1: Apply migration 0019 to CRDB** — Creates the `deploy_gate_check` table that Phase 27's endpoint writes to and Phase 28's gate POSTs into.
  - Where: CockroachDB cluster `triarchdev-24092.j77.aws-us-east-2.cockroachlabs.cloud:26257`, database `triarch_dev`
  - How: With production DATABASE_URL exported, run `npx drizzle-kit push` from this repo's working directory
  - Verify: `psql "$DATABASE_URL" -c "\d deploy_gate_check"` lists the expected columns (id, projectKey, targetVersion, verdict, devVersion, apiKeyHash, reason, workflowRunUrl, createdAt)
- [ ] **A-2: Confirm CL6_ENFORCEMENT_MODE=warn is bound in production apphosting.yaml** (already shipped in admin v2.13.14, but confirm the FAH secret-snapshot has it)
  - Where: Firebase Console -> triarch-dev-website -> App Hosting -> triarch-dev backend -> Environment
  - Verify: `CL6_ENFORCEMENT_MODE` appears with value `warn`

H2: `## Section B: shared-workflows v8.2 Release`

Intro: `Plan 28-01 committed locally on branch feat/v8.2-cl6-verdict-post in /Users/mikegeehan/claude/triarch/shared/shared-workflows. These steps push, merge, and tag.`

Checklist:
- [ ] **B-1: Push the feature branch to remote**
  ```bash
  cd /Users/mikegeehan/claude/triarch/shared/shared-workflows
  git push -u origin feat/v8.2-cl6-verdict-post
  ```
- [ ] **B-2: Open PR vs main**
  ```bash
  gh pr create \
    --base main \
    --head feat/v8.2-cl6-verdict-post \
    --title "v8.2: gate-prod-version posts verdict to /api/platform/cicd/gate-verdict (CL-6)" \
    --body "Closes the CL-6 paired-verdict loop for Phase 28."
  ```
- [ ] **B-3: Merge PR to main** (squash or merge-commit; consumers pin to tag, not SHA)
- [ ] **B-4: Create annotated tag v8.2 on main**
  ```bash
  cd /Users/mikegeehan/claude/triarch/shared/shared-workflows
  git checkout main && git pull
  git tag -a v8.2 -m "v8.2: CL-6 paired-verdict POST step (additive)"
  git push origin v8.2
  ```
  - Verify: `git ls-remote --tags origin | grep v8.2` returns a hash

H2: `## Section C: Platform Repo Release (v2.13.15)`

Intro: `Plan 28-02 committed locally on branch feat/cl4-self-adopt-gate. These steps push and PR-vs-dev (per workspace CLAUDE.md — platform is a dev-promotion-path repo).`

Checklist:
- [ ] **C-1: Push the feature branch to remote**
  ```bash
  git push -u origin feat/cl4-self-adopt-gate
  ```
- [ ] **C-2: Open PR vs dev** (NOT main — workspace CLAUDE.md per-project promotion-branch rule)
  ```bash
  gh pr create \
    --base dev \
    --head feat/cl4-self-adopt-gate \
    --title "v2.13.15: feat(ci-cd): wire gate-prod-version@v8.2 as needs: of prod deploy (CL-4)" \
    --body "Platform self-adopts the CL-4 gate. Depends on shared-workflows tag v8.2 (Section B)."
  ```
- [ ] **C-3: CI runs on the PR**
  - Expected: quality-gate + validate-apphosting + version job run; cl4-gate is SKIPPED (it's prod-only via `if: needs.env-select.outputs.environment == 'prod'`)
  - If any of those fail: investigate before merging
- [ ] **C-4: Merge to dev**
  - dev deploy fires automatically; cl4-gate STILL skipped (dev push); existing deploy path unaffected
  - Verify the dev deploy actually completes successfully — this proves cl4-gate doesn't accidentally fire on dev
- [ ] **C-5: Open second PR dev -> main** (the promote step)
  ```bash
  gh pr create --base main --head dev --title "v2.13.15: promote CL-4 self-adopt to prod" --body "Promotes the verified dev state to prod."
  ```
  - DO NOT merge this PR yet — Section D's GitHub secret must be set first.

H2: `## Section D: GitHub Actions Secret Provisioning`

Intro: `The gate job references secrets.ADMIN_API_TOKEN. Without it, every prod deploy fails at gate-time with "ADMIN_API_TOKEN not set" (a deliberate fail-loud).`

Checklist:
- [ ] **D-1: Look up the platform's apiKey from admin CRDB**
  ```bash
  # With prod DATABASE_URL exported:
  psql "$DATABASE_URL" -c "SELECT key, name, apiKey FROM projects WHERE key = 'triarch-dev';"
  ```
  - Copy the apikey column value (32+ char string)
- [ ] **D-2: Add the secret to MyAlterLego/triarch-dev GitHub Actions**
  - URL: https://github.com/MyAlterLego/triarch-dev/settings/secrets/actions
  - Click "New repository secret"
  - Name: `ADMIN_API_TOKEN`
  - Value: paste the apiKey from D-1
  - Click "Add secret"
- [ ] **D-3: Verify the secret name is byte-exact**
  ```bash
  gh secret list --repo MyAlterLego/triarch-dev | grep ADMIN_API_TOKEN
  ```
  - Expected: one row showing `ADMIN_API_TOKEN` with an "Updated" timestamp

H2: `## Section E: Contrived Dry-Run (INV-2 Should Block)`

Intro: `Verify the gate ACTUALLY blocks when invariants are violated. This is success criteria #3 from the phase definition.`

Checklist:
- [ ] **E-1: Create a contrived branch with a fake high version**
  ```bash
  git checkout -b chore/contrived-gate-test main
  npm version --no-git-tag-version 9.9.9
  git add package.json package-lock.json 2>/dev/null
  git commit -m "vchore: contrived high version to prove INV-2 blocks"
  git push -u origin chore/contrived-gate-test
  ```
- [ ] **E-2: Open PR vs main and merge** (since main triggers prod deploy + gate)
  - WARNING: only do this on the contrived branch — merging this commit means a prod deploy ATTEMPT will fire, which will fail at gate-time. That's the test.
- [ ] **E-3: Verify the gate FAILS with INV-2**
  - GitHub Actions UI -> the merged commit's workflow run -> cl4-gate job -> expected `::error::INV-2: target v9.9.9 is HIGHER than current dev v...`
  - The deploy job should be skipped (gate failed)
  - Verify: `gh run view <run-id> --json conclusion | jq -r .conclusion` returns `failure`
- [ ] **E-4: Verify the deploy_gate_check row was written**
  ```bash
  psql "$DATABASE_URL" -c "SELECT projectKey, targetVersion, verdict, devVersion FROM deploy_gate_check WHERE projectKey='triarch-dev' ORDER BY createdAt DESC LIMIT 1;"
  ```
  - Expected: targetVersion=`v9.9.9`, verdict=`fail`
- [ ] **E-5: Roll back the contrived bump**
  - Revert the commit on main: `git revert <contrived-sha>` and merge to restore the true version. Reset package.json back to 2.13.15.

H2: `## Section F: Real Prod Deploy (v2.13.15+ Should Pass)`

Intro: `The actual "did we ship it" test.`

Checklist:
- [ ] **F-1: Ensure dev has seen v2.13.15** (Section C-4 already deployed it to dev; this confirms the version-snapshot endpoint reports it)
  ```bash
  curl -s -H "Authorization: Bearer $ADMIN_API_TOKEN" https://admin.triarch.dev/api/platform/version-snapshot | jq '.dev'
  ```
  - Expected: `{"version":"v2.13.15","deployed_at":"..."}`
- [ ] **F-2: Merge the dev -> main PR from Section C-5**
  - Prod deploy fires; cl4-gate runs the v8.2 workflow
  - Expected: gate PASSES (target=v2.13.15 matches dev=v2.13.15, > prod, dev age > 300s by now)
  - Expected: deploy job runs and ships
- [ ] **F-3: Verify the deploy_gate_check row for v2.13.15 was written with verdict=pass**
  ```bash
  psql "$DATABASE_URL" -c "SELECT targetVersion, verdict, devVersion, workflowRunUrl FROM deploy_gate_check WHERE projectKey='triarch-dev' AND targetVersion='v2.13.15' ORDER BY createdAt DESC LIMIT 1;"
  ```
  - Expected: targetVersion=`v2.13.15`, verdict=`pass`, devVersion=`v2.13.15`, workflowRunUrl=`https://github.com/MyAlterLego/triarch-dev/actions/runs/...`
- [ ] **F-4: Verify the release_logs row landed** (CL-6 pre-check should pass in warn mode regardless, but here we confirm the paired-verdict was found and the release was NOT 409'd)
  ```bash
  psql "$DATABASE_URL" -c "SELECT version, env, deployedAt FROM release_logs WHERE projectKey='triarch-dev' ORDER BY deployedAt DESC LIMIT 1;"
  ```
  - Expected: version=`v2.13.15`, env=`prod`

H2: `## Section G: CL6_ENFORCEMENT_MODE Flip (Operational — Not in Phase 28 Scope)`

Intro: `After 7 days of warn-mode telemetry showing zero false positives (per Phase 27 D-Rollout), flip the enforcement mode. Capture this here so it's not lost.`

Checklist:
- [ ] **G-1: After 7-day grace window**, flip the FAH binding:
  - Where: Firebase Console -> triarch-dev-website -> App Hosting -> triarch-dev backend -> Environment
  - Change `CL6_ENFORCEMENT_MODE` value from `warn` to `enforce`
  - Trigger a rollout (or wait for next deploy)
- [ ] **G-2: Sanity check** — re-run a normal prod deploy. Expected: still ships (gate + paired verdict + release ingest all happy)
- [ ] **G-3: Compliance matrix UI** (Phase 35) gains a CL-6 column once enforce mode is live

H2: `## HUMAN-UAT Items Completion Criteria`

Phase 28 is operationally complete when:
- [x] Sections A through F all checked
- [x] One green prod deploy of v2.13.15+ has happened via the gate
- [x] The corresponding `deploy_gate_check` row is verifiable in CRDB
- [x] The corresponding `release_logs` row is verifiable in CRDB
- [ ] Section G is a follow-up — does NOT block Phase 32 starting

After completion, mark CL4-01 = Complete in `.planning/REQUIREMENTS.md` and Phase 28 status = Complete in `.planning/STATE.md` + `.planning/ROADMAP.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "Section A: Carry-Over From Phase 27" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "Section B: shared-workflows v8.2 Release" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "Section C: Platform Repo Release" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "Section D: GitHub Actions Secret Provisioning" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "Section E: Contrived Dry-Run" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "Section F: Real Prod Deploy" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "Section G: CL6_ENFORCEMENT_MODE Flip" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "git push -u origin feat/v8.2-cl6-verdict-post" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "git push -u origin feat/cl4-self-adopt-gate" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "ADMIN_API_TOKEN" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "migration 0019" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && grep -q "9.9.9" .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md && [ "$(grep -c '^---$' .planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md)" = "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md` exists
    - Contains all 7 expected section headers (A through G)
    - Section A references "migration 0019" carry-over from Phase 27
    - Section B contains the exact `git push -u origin feat/v8.2-cl6-verdict-post` command
    - Section C contains the exact `git push -u origin feat/cl4-self-adopt-gate` command AND uses PR `--base dev` (per workspace CLAUDE.md)
    - Section D explains how to look up the apiKey from CRDB AND how to add it as ADMIN_API_TOKEN secret
    - Section E describes the contrived dry-run using version `9.9.9` to force INV-2
    - Section F describes the real prod deploy verification with CRDB confirmations
    - Section G describes the CL6_ENFORCEMENT_MODE warn-to-enforce flip with the 7-day grace
    - Exactly 2 lines match `^---$` (frontmatter delimiters only)
    - YAML frontmatter parses
  </acceptance_criteria>
  <done>HUMAN-UAT file written with EXACTLY 2 three-hyphen lines; all 7 sections (A-G) present; every human operational step from CONTEXT.md "What This Phase Does NOT Do" enumerated; cross-references to Phase 27 carry-over items included.</done>
</task>

</tasks>

<verification>
- All 10 automated checks against Plan 28-02 output pass
- 28-SUMMARY.md exists with valid frontmatter and references both 28-01 + 28-02
- 28-HUMAN-UAT.md exists with all 7 sections (A-G) and concrete, copy-pasteable commands
- Cross-links between SUMMARY and UAT are present
- Requirements traceability for CL4-01 is present in both files
- Both phase-artifact files contain EXACTLY 2 three-hyphen lines (frontmatter delimiters only)
</verification>

<success_criteria>
- Phase 28 has a clean phase-close artifact set: 28-01-PLAN, 28-01-SUMMARY (from Plan 01), 28-02-PLAN, 28-03-PLAN, 28-SUMMARY, 28-HUMAN-UAT
- Mike has a single document (28-HUMAN-UAT.md) to work through after the autonomous run completes
- The contract requirement CL4-01 has a complete code path (autonomous) AND a complete verification path (UAT)
- Phase 32 has a working golden template in this repo's ci-cd.yml
</success_criteria>

<output>
After completion, both files exist:
- `.planning/phases/28-cl4-platform-self-adopt/28-SUMMARY.md` (phase-close summary)
- `.planning/phases/28-cl4-platform-self-adopt/28-HUMAN-UAT.md` (operational checklist for Mike)
</output>
