---
status: passed
phase: 23.1-portal-ui-polish
generated: 2026-05-09T19:30:00Z
score: 35/35 must-haves verified
verified_by: gsd-verifier
plans_verified: [23.1-01, 23.1-02, 23.1-03, 23.1-04]
---

# Phase 23.1 Verification

**Phase Goal:** Surface what's deployed, what's pending review, and how to reach each customer surface — close the navigability + status-clarity gaps reported on portal v0.4.6 first sign-in.
**Status:** passed
**Score:** 35/35 must-haves verified across 4 plans
**Portal version observed:** v0.5.0 (matches phase-close target)
**Vitest result:** 39 test files, 371 passed / 1 skipped (delta from baseline 290 → 371 = +81, matches SUMMARY)

## Goal Achievement Summary

Phase 23.1 closes its goal cleanly. All four UI changes — sub-nav (UX-01), status column rewrite (UX-02), empty-state copy (UX-03), and staff preview-as-customer toggle (UX-04) — exist in the portal codebase, are wired end-to-end, and pass their automated tests. The most security-sensitive deliverable (the preview cookie) clears the hard requirement that it MUST NOT grant permissions to non-staff users — `isPreviewingAsCustomer` and `derivePortalUserRole` both gate on `ctx.isStaff` before reading the cookie, and the API route POST returns 403 for non-staff (defense-in-depth). The cookie module contains zero `domain:` lines (verified via regex grep), satisfying the `__Host-` prefix requirement.

## Must-Haves by Plan

### Plan 23.1-01: Sub-nav (UX-01)

| Check | Type | Status | Evidence |
|---|---|---|---|
| Tabs visible on every `/projects/[slug]/*` surface | Truth | VERIFIED | `layout.tsx:55-56` renders ProjectSubNav once; child page.tsx files don't import CustomerHeader (grep returns 0 outside layout) |
| Active tab visually distinct (underline + color shift) | Truth | VERIFIED | `ProjectSubNav.tsx:25-29` defines `ACTIVE_CLASS` = `text-teal-300 border-b-2 border-teal-400` and `INACTIVE_CLASS` = `text-zinc-400 ... border-b-2 border-transparent` |
| Tabs clickable + navigate within slug | Truth | VERIFIED | `ProjectSubNav.tsx:43-48` renders `<Link href={base}/{tab.segment}>` for each |
| ≤320px viewport horizontal-scrolls | Truth | VERIFIED | `ProjectSubNav.tsx:40-41` outer div has `overflow-x-auto`; ul has `whitespace-nowrap`; no `flex-wrap` present |
| Sub-nav below CustomerHeader, above content; no double-render | Truth | VERIFIED | `layout.tsx:53-58` renders CustomerHeader → ProjectSubNav → children; `grep -rn "import CustomerHeader" portal/src/app/projects/[slug]/` returns exactly 1 (the layout) |
| `ProjectSubNav.tsx` artifact | Artifact | VERIFIED | exists, `'use client'`, contains "Releases", "Bugs", "Features" |
| `[slug]/layout.tsx` artifact | Artifact | VERIFIED | exists, server component, performs PORTAL-03 404 guard at `:50` |
| `ProjectSubNav.test.tsx` artifact | Artifact | VERIFIED | exists alongside component |
| layout → ProjectSubNav key link | Wiring | VERIFIED | `layout.tsx:25` `import ProjectSubNav from './ProjectSubNav'` |
| page.tsx no longer imports CustomerHeader | Wiring | VERIFIED | grep returns no matches outside layout |
| ProjectSubNav uses `usePathname` | Wiring | VERIFIED | `ProjectSubNav.tsx:17, 32` |

### Plan 23.1-02: Status column (UX-02)

| Check | Type | Status | Evidence |
|---|---|---|---|
| Status pill reflects `releaseLogs.status` (not env) | Truth | VERIFIED | `BranchSection.tsx:296` renders `<ReleaseStatusPill status={release.status}>`; status column distinct from env column |
| Separate ENV column | Truth | VERIFIED | `BranchSection.tsx:282-290` renders env in its own `<td>` using `ENV_BADGE_COLORS` |
| Saturated 6-status palette per UX-02 D-03 | Truth | VERIFIED | `StatusPill.tsx:58-65` `RELEASE_STATUS_COLORS` map: amber/teal/red/emerald/zinc-grey/zinc-blue. `promoted` → emerald-500 (drift fix from prior amber-400) |
| Pending-approval rows highlighted (amber/5 bg + amber-500 left border) | Truth | VERIFIED | `BranchSection.tsx:253-256, 261` conditional `bg-amber-500/5 border-l-2 border-l-amber-500` on `release.status === 'pending_approval'` |
| Branch-section header count badge "{N} pending review" | Truth | VERIFIED | `BranchSection.tsx:117-159` derives `pendingReviewCount`, renders amber pill with text "pending review" |
| "Pending review only" filter chip toggleable, off by default | Truth | VERIFIED | `ReleasesClient.tsx:186-188, 575-588` URL state `?pending=1`, default off |
| `ReleaseStatusPill` export in StatusPill.tsx | Artifact | VERIFIED | `StatusPill.tsx:85-97` exported |
| ReleasesClient.tsx wires ReleaseStatusPill | Artifact | VERIFIED | `ReleasesClient.tsx:33` import; consumed in `:785` ExpandedPanel and via BranchSection in row render |
| BranchSection.tsx wires ReleaseStatusPill + count badge | Artifact | VERIFIED | exists; ReleaseStatusPill rendered in row; pendingReviewCount badge in header |
| ReleaseStatusPill consumes `row.status` (not row.env) | Wiring | VERIFIED | `BranchSection.tsx:296` uses `release.status` |
| ENV_BADGE_COLORS still present in ReleasesClient | Wiring | VERIFIED | `ReleasesClient.tsx:51` retained per acceptance criteria |
| Pending-only filter URL state `?pending=1` | Wiring | VERIFIED | `ReleasesClient.tsx` uses `searchParams.get('pending')` and router.replace pattern |

### Plan 23.1-03: Empty-state copy (UX-03)

| Check | Type | Status | Evidence |
|---|---|---|---|
| `PROD: Not yet released` exact copy when no prod row | Truth | VERIFIED | `projects/page.tsx:104-107` literal "Not yet released" copy in fallback branch |
| `DEV: Not yet released` exact copy when no dev row | Truth | VERIFIED | `projects/page.tsx:121-124` symmetric branch |
| Timestamp HIDDEN (not rendered) when no row | Truth | VERIFIED | `projects/page.tsx:97-101, 114-118` `formatRelativeTime` only rendered when `prodDeployedAt` / `devDeployedAt` truthy; empty branch contains no timestamp span |
| Both rows empty → standard card style (no greyed/inactive) | Truth | VERIFIED | `projects/page.tsx:81` Link className unchanged across populated/empty states; no opacity-50 / grayscale conditional |
| Populated tile rendering unchanged | Truth | VERIFIED | `projects/page.tsx:93-102, 110-119` populated branches preserve prior layout (3-span justify-between) |
| `projects/page.tsx` artifact | Artifact | VERIFIED | modified; contains 3 occurrences of "Not yet released" (2 user-facing + 1 comment) |
| `projects/page.test.tsx` artifact | Artifact | VERIFIED | exists (created new); 7 cases reported in suite |
| `?? '--'` fallback removed | Wiring | VERIFIED | `grep -F "?? '--'" projects/page.tsx` returns 0 matches |

### Plan 23.1-04: Staff preview-as-customer toggle (UX-04)

| Check | Type | Status | Evidence |
|---|---|---|---|
| Cookie set with 1-hour TTL, `__Host-` in prod, HttpOnly, SameSite=Lax, Secure-in-prod | Truth | VERIFIED | `preview-as-customer.ts:24-29` env-aware name; `:55-69` buildSetCookieAttributes returns httpOnly/sameSite='lax'/path='/'/maxAge=3600/secure-in-prod; PREVIEW_COOKIE_TTL_MS = 60*60*1000 |
| StaffCallout suppressed + PreviewModeBanner shown when previewing | Truth | VERIFIED | `StaffCallout.tsx:26-29` returns null when `previewing===true`; `app/layout.tsx:30-32` renders `<PreviewModeBanner expiresAt={expiry}>` conditionally |
| Customer action UI renders for staff as if isStaff=false | Truth | VERIFIED | `derivePortalUserRole` (`preview-as-customer.ts:123-132`) computes `isStaffEffective = ctx.isStaff && !previewing`; releases page `:57-58` consumes via `derivePortalUserRole(ctx, membership, previewing)` |
| Exit preview clears cookie + reloads | Truth | VERIFIED | `PreviewModeBanner.tsx:35-44` fetch DELETE → router.refresh; `route.ts:49-55` DELETE handler sets clear-cookie attributes |
| **SECURITY: cookie NEVER grants permissions to non-staff** | Truth | VERIFIED | Triple-gated: (a) `isPreviewingAsCustomer:92-97` returns false unless `session.user.isStaff`; (b) POST `route.ts:30-34` 403 for non-staff; (c) `derivePortalUserRole` only modifies the `ctx.isStaff` branch — `membership.role==='admin'` is unaffected |
| Cookie auto-expires after 1 hour | Truth | VERIFIED | `maxAge: Math.floor(3_600_000 / 1000)` = 3600 seconds |
| `preview-as-customer.ts` artifact w/ all exports | Artifact | VERIFIED | exports: isPreviewingAsCustomer, getPreviewExpiry, buildSetCookieAttributes, buildClearCookieAttributes, derivePortalUserRole, PREVIEW_COOKIE_NAME, PREVIEW_COOKIE_TTL_MS |
| `api/preview-as-customer/route.ts` artifact w/ POST + DELETE | Artifact | VERIFIED | both named exports present |
| `PreviewModeBanner.tsx` artifact | Artifact | VERIFIED | `'use client'` (line 1), fixed top-0 z-50, eye emoji 👁 (line 52), "Exit preview" button (line 61) |
| `StaffCallout.tsx` modified — null when previewing + Preview-as-customer link | Artifact | VERIFIED | line 26-29 (suppression); line 39-46 (form action POST `/api/preview-as-customer`) |
| StaffCallout link → POST API route | Wiring | VERIFIED | `<form action="/api/preview-as-customer" method="POST">` |
| Banner Exit → DELETE API route | Wiring | VERIFIED | `fetch('/api/preview-as-customer', { method: 'DELETE' })` |
| All page.tsx use cookie-aware userRole via helper | Wiring | VERIFIED | grep finds isPreviewingAsCustomer + derivePortalUserRole in 4 page.tsx files (releases, bugs, bugs/[id], features, features/[id]); legacy ternary `isStaff || membership?.role === 'admin' ? 'admin' : 'viewer'` returns 0 matches |
| Cookie attrs include `__Host-` prefix in prod, no Domain attr | Wiring | VERIFIED | `grep -E "^\s*domain:" preview-as-customer.ts` returns 0 lines (regex matches property syntax only) |
| POST route guards on isStaff | Wiring | VERIFIED | `route.ts:30-34` returns 403 if `!session.user.isStaff` |

## Requirements Trace

| Req | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| UX-01 | 23.1-01 | Customer sub-nav linking Releases/Bugs/Features w/ active styling, mobile-responsive | SATISFIED | ProjectSubNav.tsx + layout.tsx (see Plan 23.1-01 truths above) |
| UX-02 | 23.1-02 | Release log status column rendering `releaseLogs.status` w/ color-coded badges, ENV separated, "needs review" emphasis | SATISFIED | ReleaseStatusPill + BranchSection row + section badge + filter chip |
| UX-03 | 23.1-03 | Project home cards replace `PROD --` w/ "Not yet released" | SATISFIED | projects/page.tsx conditional empty-state branches |
| UX-04 | 23.1-04 | Staff "preview as customer" toggle, off-by-default, obvious exit, cookie NEVER grants permissions | SATISFIED | preview-as-customer.ts + route.ts + PreviewModeBanner + StaffCallout mod + cookie-aware role threaded across 5 pages |

**Note on REQUIREMENTS.md traceability:** REQUIREMENTS.md (dated 2026-05-08) predates Phase 23.1 and does not contain UX-01..04 entries in its requirements list or its traceability table. The phase was inserted as a decimal phase between 23 and 24 after first customer sign-in surfaced the gaps; ROADMAP.md tracks it as Phase 23.1 (4/4 complete, line 395). This is a documentation gap (REQUIREMENTS.md not updated when the decimal phase was added), not a code gap. Recommend a follow-up doc commit to add UX-01..04 + their traceability rows to REQUIREMENTS.md so v2.2 coverage stays accurate.

## Cross-Cutting Checks

| # | Check | Result | Evidence |
|---|---|---|---|
| 1 | Sub-nav renders on all 3 surfaces | PASS | `[slug]/layout.tsx` mounts ProjectSubNav once; pages don't import CustomerHeader (grep = 0 outside layout) |
| 2 | Status column shows `releaseLogs.status`, NOT env; ENV separate column; pending row highlight; section count badge; "Pending review only" chip | PASS | BranchSection.tsx:282-306 (separate cells); :253-261 (highlight); :117-159 (count badge); ReleasesClient.tsx:575-588 (chip) |
| 3 | Promoted color is emerald-500 (not amber-400 drift) | PASS | StatusPill.tsx:62 `promoted: 'bg-emerald-500/20 text-emerald-400 border-emerald-500/30'` AND BranchSection.tsx:31 mirrors |
| 4 | Empty-state exact copy "Not yet released" (no period, no ellipsis); no `--`; timestamp hidden | PASS | projects/page.tsx:106, 123 literal "Not yet released"; `?? '--'` grep = 0; conditional formatRelativeTime |
| 5 | Cookie name `portal-preview-as-customer` (or `__Host-` in prod); HttpOnly + SameSite=Lax + Secure-in-prod + 1h TTL + NO Domain attribute; cookie NEVER grants permissions; explicit non-staff+cookie test | PASS | preview-as-customer.ts:24-29 env-aware naming; :55-69 attrs; :92-97 + :103-112 staff-gated reads; `^\s*domain:` grep = 0; `derivePortalUserRole` only suppresses staff-bypass branch; SUMMARY documents tests T1.9 (helper non-staff+cookie), T2.2 (route 403), T4.4 (page-level non-staff unchanged) |
| 6 | API route POST 403 for non-staff, DELETE clears | PASS | route.ts:30-34 (403); :49-55 (DELETE always 200, clears via buildClearCookieAttributes) |
| 7 | PreviewModeBanner appears when cookie present, has Exit affordance | PASS | app/layout.tsx:30-32 conditional mount; PreviewModeBanner.tsx:46-63 fixed top-0 z-50 with "Exit preview" button |
| 8 | StaffCallout has "Preview as customer →" link in addition to admin switch | PASS | StaffCallout.tsx:39-46 form-action POST + :47-52 admin link |
| 9 | No regression in earlier phases (21/22/23) | PASS | Vitest 371 passed / 1 skipped (39 test files all GREEN); previous baseline 290 + delta 81 = 371 reconciles cleanly; WRITE-05 (W5-1..5) tests still pass per SUMMARY 23.1-02 |

## Anti-Patterns Found

None blocking. Two non-blocking observations from SUMMARYs (already documented):

| File | Note | Severity | Impact |
|---|---|---|---|
| Multiple test files (7) | Pre-existing TypeScript test errors from commit `9dae716` (v0.4.3 import migration) — logged in `deferred-items.md` by Plan 23.1-01 | Info | Out of phase scope; `next build` clean (build excludes test files); vitest GREEN |
| `ReleasesClient.tsx:51-58` | `ENV_BADGE_COLORS` retained as unused-locals const + `void ENV_BADGE_COLORS` suppress, only consumed in BranchSection.tsx | Info | Documentation anchor + acceptance-criteria grep target; documented in plan key-decisions |
| `bugs/page.tsx`, `features/page.tsx` | `derivePortalUserRole` threaded but `userRole` not yet rendered (no admin-only customer actions exist on these surfaces yet) | Info | Future-proofing per plan key-decision; same pattern as ENV_BADGE_COLORS |

## Human Verification Required

These items pass automated verification but warrant a quick human eye on a real browser before declaring the phase complete in customer-facing reality:

### 1. Sub-nav active styling actually visible

**Test:** Browse to `/projects/<slug>/releases`, then click "Bugs", then click "Features".
**Expected:** Active tab visually distinct via teal-300 text + teal-400 underline; inactive tabs are zinc-400. `usePathname` startsWith correctly highlights parent tab when on `/bugs/[id]` or `/features/new` sub-routes.
**Why human:** Color perception + the underline-vs-pill distinction is hard to verify programmatically beyond classname presence.

### 2. PreviewModeBanner overflow at 320px

**Test:** Set browser viewport to 320px, sign in as staff, click "Preview as customer →".
**Expected:** Banner remains readable; "Exit preview" button reachable. Plan 23.1-04 SUMMARY notes this is a P3 polish item — banner is functional but visually crowded at narrow viewports. Verify it's tolerable.
**Why human:** Visual layout + readability at constrained viewport.

### 3. Form-action POST 303-redirect round trip

**Test:** Sign in as staff (mike@triarchsecurity.com), click "Preview as customer →" in StaffCallout.
**Expected:** Browser POSTs, route handler returns 303 to Referer, page reloads with cookie active, PreviewModeBanner appears, StaffCallout disappears, customer-action UI (approve/reject buttons on releases page) becomes visible.
**Why human:** Full HTTP round-trip + browser cookie behavior + Referer header presence. Tests mock these layers.

### 4. Customer sees no change after cookie injected (security boundary)

**Test:** As a non-staff customer (e.g. simulate via dev cookie injection or temporarily flip session.user.isStaff to false in dev), set the `portal-preview-as-customer` cookie manually via DevTools. Reload.
**Expected:** Zero UI change. No banner appears. userRole computed identically with/without the cookie. (T1.9, T2.2, T4.4 cover this in tests; sanity-check end-to-end.)
**Why human:** Defense-in-depth check on the most security-sensitive deliverable in the phase. Worth a real-browser confirmation.

### 5. Customer-reported v0.4.6 issues actually resolved on a real customer account

**Test:** Sign in as the actual customer who reported the gaps (or as staff in preview mode), navigate `/projects/<their-slug>/releases`, observe project tiles + releases table.
**Expected:** Sub-nav visible, status column shows real lifecycle status (not env), pending-approval rows visually distinct, brand-new project tiles read "Prod: Not yet released" / "Dev: Not yet released" without a `--` in sight.
**Why human:** This phase exists because of a customer report — closing the loop with a customer-perspective walkthrough is the only way to confirm the gaps are actually closed in their experience.

## Verdict

**passed** — All 35 must-haves across 4 plans verified. Phase 23.1 goal achieved: portal v0.5.0 ships with the navigability + status-clarity gaps from v0.4.6 closed, automated tests pass (371 GREEN), and the security-sensitive preview cookie clears its hard requirement (no permission elevation, triple-gated). Five human-verify items recommended for confidence but none block the phase.

**Documentation gap (non-blocking, post-phase):** REQUIREMENTS.md does not list UX-01..04 — recommend a docs commit to add the four requirement IDs and their traceability rows to keep v2.2 coverage table accurate.

---

_Verified: 2026-05-09T19:30:00Z_
_Verifier: Claude (gsd-verifier)_
