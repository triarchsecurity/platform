---
phase: 08-admin-home-pipeline-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/db/migrations/
autonomous: true
requirements:
  - PIPE-01
  - PIPE-03
must_haves:
  truths:
    - "Composite index (project, env, deployed_at DESC) exists on release_logs in the dev cluster"
    - "Drizzle schema.ts declares the index so future db:push diffs don't drop it"
    - "Migration ships in the same deploy as the dashboard query (Pitfall 8 guard)"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "releaseLogs table second-arg with index() declaration"
      contains: "release_logs_project_env_deployed_idx"
    - path: "src/db/migrations/0013_release_logs_pipeline_idx.sql"
      provides: "Generated migration creating composite index"
      contains: "CREATE INDEX"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/db/migrations/0013_release_logs_pipeline_idx.sql"
      via: "drizzle-kit generate"
      pattern: "release_logs_project_env_deployed_idx"
---

<objective>
Add the composite index `(project, env, deployed_at DESC)` on `release_logs` to make the DISTINCT ON dashboard query (Plan 02) fast at scale and to satisfy the standing decision in STATE.md (Pending Todos): "Phase 8 planning: include composite index `(project, env, deployed_at DESC)` on `release_logs` in the same deploy as the dashboard query (Pitfall 8 guard)."

Purpose: Without this index, the per-project `DISTINCT ON (project, env) ORDER BY ... deployed_at DESC` query in Plan 02 will full-scan `release_logs` on every admin home render. The index must ship in the same deploy as the query — this is an explicit standing constraint from PITFALLS.md Pitfall 8 and STATE.md Pending Todos.

Output: Drizzle schema declaration + auto-generated migration SQL file. No application code touched.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-admin-home-pipeline-visibility/08-CONTEXT.md
@.planning/research/PITFALLS.md
@src/db/schema.ts
@drizzle.config.ts

<interfaces>
<!-- Existing Drizzle index pattern in schema.ts (lines 162-170) — replicate this style -->

```typescript
// Pattern: pgTable second argument is a function returning an array of constraints
export const projectMembers = pgTable('project_members', {
  // ... columns
}, (table) => [
  uniqueIndex('project_members_unique_idx').on(table.projectKey, sql`lower(${table.email})`),
]);
```

<!-- Existing index() (non-unique) pattern is auto-generated in migrations only — see 0012_promote_attempts.sql -->
<!-- For non-unique composite indexes with DESC ordering, declare via the index() helper from drizzle-orm/pg-core -->

```typescript
// drizzle-orm/pg-core exports both `index` and `uniqueIndex`.
// For non-unique with DESC: index('name').on(table.col1, table.col2, table.col3.desc())
// .desc() and .asc() are column-level methods on Drizzle column objects.
```

<!-- releaseLogs current shape (schema.ts lines 137-158): -->
- Currently: `pgTable('release_logs', { ... })` with NO second-arg constraints array
- After this plan: `pgTable('release_logs', { ... }, (table) => [...])` with one index declaration
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare composite index in Drizzle schema and generate migration</name>
  <files>src/db/schema.ts, src/db/migrations/0013_release_logs_pipeline_idx.sql</files>
  <read_first>
    - src/db/schema.ts (full file — must see current releaseLogs table definition at lines 137-158, the existing `index` and `uniqueIndex` imports at line 10, and the projectMembers second-arg pattern at lines 162-170)
    - drizzle.config.ts (confirms migrations output path: `./src/db/migrations`)
    - src/db/migrations/0012_promote_attempts.sql (confirms auto-generated migration format, including `CREATE INDEX ... USING btree ... DESC NULLS LAST` syntax for descending indexes)
    - .planning/research/PITFALLS.md (Pitfall 8 — confirms the EXACT index spec: `(project, env, deployed_at DESC)`)
  </read_first>
  <action>
    Step 1 — Verify imports in src/db/schema.ts line 10. Existing import already includes `uniqueIndex` from drizzle-orm/pg-core. Confirm `index` is also imported. If not, add `index` to the same import statement.

    Step 2 — Modify `releaseLogs` table declaration (currently lines 137-158, ending with `});`). Convert from single-arg `pgTable('release_logs', { ...columns })` to two-arg form `pgTable('release_logs', { ...columns }, (table) => [...])`. Add ONE non-unique composite index inside the array:

    ```typescript
    index('release_logs_project_env_deployed_idx').on(
      table.project,
      table.env,
      table.deployedAt.desc(),
    ),
    ```

    DO NOT change any column definitions, types, or comments. Only add the second-arg index array.

    Step 3 — Run `npx drizzle-kit generate` to produce the migration SQL file. The expected output is `src/db/migrations/0013_*.sql` (next sequential number after 0012_promote_attempts.sql). The generated file MUST contain a statement matching:

    ```sql
    CREATE INDEX "release_logs_project_env_deployed_idx" ON "release_logs" USING btree ("project","env","deployed_at" DESC NULLS LAST);
    ```

    Step 4 — If drizzle-kit named the migration file something other than `0013_release_logs_pipeline_idx.sql` (it auto-generates a snake_case slug like `0013_naive_kraven.sql`), rename the generated file to `0013_release_logs_pipeline_idx.sql`. Also update `src/db/migrations/meta/_journal.json` to reflect the renamed `tag` field for the new entry. The existing journal entries follow the pattern `"tag": "0012_promote_attempts"` — keep that style.

    Step 5 — DO NOT run `db:push` in this plan. Schema rollout to the dev cluster happens at deploy time via the standard CI flow when this commit hits main. This plan only updates source-of-truth files (schema.ts + migration SQL).

    DECISION: This index uses the `index()` helper (non-unique). Per Pitfall 8 the index is purely for query performance. There is no uniqueness constraint to enforce — `release_logs` legitimately has multiple rows per `(project, env)` over time.

    DECISION: `.desc()` is applied only to `deployedAt` (the third column). The first two columns use default ascending order — DISTINCT ON groups by `(project, env)` so leading ASC columns are equally fine for grouping; only the tiebreaker `deployedAt` needs DESC for the "latest first" pick.

    DECISION: NULLS LAST is the Postgres default for DESC. Plan 02's query uses `COALESCE(deployed_at, released_at) DESC NULLS LAST` — the index covers the `deployed_at` portion; the COALESCE fallback path is acceptable to be slightly slower for the legacy null-deployed_at rows.
  </action>
  <verify>
    <automated>grep -q "release_logs_project_env_deployed_idx" src/db/schema.ts && grep -q "release_logs_project_env_deployed_idx" src/db/migrations/0013_release_logs_pipeline_idx.sql && grep -q "deployed_at\" DESC" src/db/migrations/0013_release_logs_pipeline_idx.sql && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "release_logs_project_env_deployed_idx" src/db/schema.ts` returns `1` (exact index name appears once in schema)
    - `grep -E "index\('release_logs_project_env_deployed_idx'\)" src/db/schema.ts` matches (uses non-unique `index()` helper, not `uniqueIndex`)
    - `grep -E "table\.deployedAt\.desc\(\)" src/db/schema.ts` matches (DESC ordering on the third column)
    - File `src/db/migrations/0013_release_logs_pipeline_idx.sql` exists
    - `grep -E 'CREATE INDEX "release_logs_project_env_deployed_idx" ON "release_logs"' src/db/migrations/0013_release_logs_pipeline_idx.sql` matches
    - `grep -E '"project","env","deployed_at" DESC' src/db/migrations/0013_release_logs_pipeline_idx.sql` matches (column order exactly: project, env, deployed_at DESC)
    - `grep '"tag": "0013_release_logs_pipeline_idx"' src/db/migrations/meta/_journal.json` matches (journal entry renamed to match file)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors from the schema change)
    - No other files in `src/db/migrations/` were created or modified beyond the new 0013 file and the journal
    - `git diff src/db/schema.ts` shows ONLY the addition of `index` import (if needed) and the new second-arg array on `releaseLogs` — no column changes, no comment changes, no other table modifications
  </acceptance_criteria>
  <done>
    Drizzle schema declares the composite index; migration SQL file 0013 exists with the exact `CREATE INDEX ... (project, env, deployed_at DESC)` statement; journal updated; TypeScript compiles. Ready to ship in same deploy as Plan 02 query.
  </done>
</task>

</tasks>

<verification>
- Run `git diff src/db/schema.ts` — only releaseLogs second-arg added; no other table touched
- Run `cat src/db/migrations/0013_release_logs_pipeline_idx.sql` — confirm CREATE INDEX statement matches Pitfall 8 spec exactly
- Run `npx drizzle-kit check` — should report no schema/migration drift
- Run `npx tsc --noEmit` — no type errors
</verification>

<success_criteria>
- `release_logs_project_env_deployed_idx` declared in src/db/schema.ts
- Migration file 0013 generated with correct DESC ordering on deployed_at
- Drizzle journal updated
- TypeScript compiles
- No application code touched (schema-only change)
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-home-pipeline-visibility/08-01-SUMMARY.md` documenting the index name, the migration file path, and confirmation that Plan 02 can rely on the index being present in the same deploy.
</output>
