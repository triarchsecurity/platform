---
phase: 29-cl2-envbadge-component
plan: 06
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - /Users/mikegeehan/claude/triarch/shared/truthtreason/src/app/layout.tsx
  - /Users/mikegeehan/claude/triarch/shared/truthtreason/package.json
  - /Users/mikegeehan/claude/triarch/shared/truthtreason/next.config.ts
  - /Users/mikegeehan/claude/triarch/shared/truthtreason/apphosting.dev.yaml
autonomous: true
requirements:
  - CL2-03
  - CL2-04
gap_closure: false

must_haves:
  truths:
    - "EnvBadge mounts in truthtreason root layout (operates in a SEPARATE git repo)"
    - "truthtreason package.json gains @triarchsecurity/shared-ui dep (currently not installed)"
    - "truthtreason next.config.ts gains transpilePackages with @triarchsecurity/shared-ui (currently has no transpilePackages config)"
    - "truthtreason apphosting.dev.yaml sets NEXT_PUBLIC_ENV=dev"
    - "truthtreason apphosting.yaml does NOT set NEXT_PUBLIC_ENV"
  artifacts:
    - path: "/Users/mikegeehan/claude/triarch/shared/truthtreason/src/app/layout.tsx"
      provides: "Mount point"
      contains: "EnvBadge"
    - path: "/Users/mikegeehan/claude/triarch/shared/truthtreason/next.config.ts"
      provides: "transpilePackages config (added by this plan)"
      contains: "@triarchsecurity/shared-ui"
    - path: "/Users/mikegeehan/claude/triarch/shared/truthtreason/apphosting.dev.yaml"
      provides: "Dev env var binding"
      contains: "NEXT_PUBLIC_ENV"
  key_links:
    - from: "src/app/layout.tsx"
      to: "@triarchsecurity/shared-ui"
      via: "named import"
      pattern: "import \\{ EnvBadge \\} from '@triarchsecurity/shared-ui'"
---

<objective>
Mount EnvBadge in truthtreason's root layout. truthtreason is a SEPARATE git repository at `/Users/mikegeehan/claude/triarch/shared/truthtreason/`. Current branch is `dev` (CLEAN — no stale fix branch). Branch off `dev` per CONTEXT.md.

Special case: truthtreason currently has NO `@triarchsecurity/shared-ui` dep and NO `transpilePackages` config. This plan adds BOTH (in addition to the standard mount + apphosting work). It also has an additional `apphosting.prod.yaml` file (alongside apphosting.yaml) — leave both alone; only edit `apphosting.dev.yaml`.

Output: Local feature branch `feat/cl2-envbadge-mount` in truthtreason repo with one atomic v1.1.19 commit.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-CONTEXT.md
@/Users/mikegeehan/claude/triarch/shared/truthtreason/src/app/layout.tsx
@/Users/mikegeehan/claude/triarch/shared/truthtreason/apphosting.dev.yaml
@/Users/mikegeehan/claude/triarch/shared/truthtreason/apphosting.yaml
@/Users/mikegeehan/claude/triarch/shared/truthtreason/package.json
@/Users/mikegeehan/claude/triarch/shared/truthtreason/next.config.ts

<interfaces>
EnvBadge contract (from 29-01):

export interface EnvBadgeProps { env?: string; }
export function EnvBadge(props: EnvBadgeProps): JSX.Element | null;

truthtreason current state:
- Repo: /Users/mikegeehan/claude/triarch/shared/truthtreason/
- Current branch: dev (clean — no stale fix branch)
- Current version: 1.1.18
- Current shared-ui dep: NONE (needs to be ADDED)
- Current next.config.ts: no transpilePackages field (needs to be ADDED)
- Has BOTH apphosting.yaml AND apphosting.prod.yaml — only edit apphosting.dev.yaml
- Layout: synchronous with head block for Google Fonts + simple body element wrapping children
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cd to truthtreason repo, branch off dev, ADD shared-ui dep + transpilePackages</name>
  <files>
    /Users/mikegeehan/claude/triarch/shared/truthtreason/package.json
    /Users/mikegeehan/claude/triarch/shared/truthtreason/next.config.ts
  </files>
  <read_first>
    1. cd /Users/mikegeehan/claude/triarch/shared/truthtreason and git status — confirm clean tree.
    2. git branch --show-current — expected `dev`.
    3. Read full next.config.ts — confirm structure to know where to insert transpilePackages.
    4. Read full package.json — confirm no existing shared-ui dep.
  </read_first>
  <action>
    Step 1 — Branch off dev:

    cd /Users/mikegeehan/claude/triarch/shared/truthtreason
    git stash push -m "phase-29-stash" 2>/dev/null || echo "clean"
    git checkout dev
    git pull --ff-only origin dev 2>/dev/null || echo "no pull"
    git checkout -b feat/cl2-envbadge-mount

    Step 2 — ADD `@triarchsecurity/shared-ui` as a new dep in package.json. Insert into the `dependencies` block alphabetically (likely between `pg` and `react`):

    "@triarchsecurity/shared-ui": "^1.5.0",

    Step 3 — Bump truthtreason version `1.1.18` to `1.1.19` (patch).

    Step 4 — ADD `transpilePackages` to next.config.ts. Insert into the config object near the top:

    transpilePackages: ['@triarchsecurity/shared-ui'],

    Common shape after edit:

    const nextConfig = {
      transpilePackages: ['@triarchsecurity/shared-ui'],
      // ...existing config
    };
    export default nextConfig;

    Step 5 — Attempt npm install (expected 404 on v1.5.0 until shared-ui publish):

    cd /Users/mikegeehan/claude/triarch/shared/truthtreason
    npm install
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/shared/truthtreason && grep -q '"@triarchsecurity/shared-ui": "\^1.5.0"' package.json && grep -q '"version": "1.1.19"' package.json && grep -q "transpilePackages" next.config.ts && grep -q "@triarchsecurity/shared-ui" next.config.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - On `feat/cl2-envbadge-mount` in truthtreason repo
    - package.json has new `"@triarchsecurity/shared-ui": "^1.5.0"` dep
    - package.json version is 1.1.19
    - next.config.ts has `transpilePackages` array containing `@triarchsecurity/shared-ui`
  </acceptance_criteria>
  <done>Branch + new dep + new transpilePackages added.</done>
</task>

<task type="auto">
  <name>Task 2: Mount EnvBadge in layout.tsx, add NEXT_PUBLIC_ENV to apphosting.dev.yaml, atomic commit</name>
  <files>
    /Users/mikegeehan/claude/triarch/shared/truthtreason/src/app/layout.tsx
    /Users/mikegeehan/claude/triarch/shared/truthtreason/apphosting.dev.yaml
  </files>
  <read_first>
    1. Re-read full src/app/layout.tsx — note the head block with Google Fonts preconnect/stylesheet lines.
    2. Read full apphosting.dev.yaml — existing env entry is DATABASE_URL with `secret:` (no other entries; minimal file).
    3. Grep apphosting.yaml AND apphosting.prod.yaml for NEXT_PUBLIC_ENV — both should return zero matches.
  </read_first>
  <action>
    Step 1 — Edit src/app/layout.tsx. Add import after existing imports:

    import { EnvBadge } from '@triarchsecurity/shared-ui';

    Final body block:

    <body>
      {children}
      <EnvBadge env={process.env.NEXT_PUBLIC_ENV} />
    </body>

    Step 2 — Append to apphosting.dev.yaml at END of env list:

      # CL-2 dev chrome badge (Phase 29). NEXT_PUBLIC_* vars are baked at build.
      - variable: NEXT_PUBLIC_ENV
        value: dev
        availability:
          - BUILD
          - RUNTIME

    Step 3 — Confirm BOTH prod yamls lack NEXT_PUBLIC_ENV:

    cd /Users/mikegeehan/claude/triarch/shared/truthtreason
    grep -n "NEXT_PUBLIC_ENV" apphosting.yaml || echo "absent from apphosting.yaml"
    grep -n "NEXT_PUBLIC_ENV" apphosting.prod.yaml || echo "absent from apphosting.prod.yaml"

    Step 4 — Atomic commit:

    cd /Users/mikegeehan/claude/triarch/shared/truthtreason
    git add src/app/layout.tsx apphosting.dev.yaml package.json next.config.ts
    git status --short
    git commit -m "v1.1.19: feat(cl-2): mount <EnvBadge/> in root layout + set NEXT_PUBLIC_ENV=dev"

    DO NOT push, DO NOT PR.
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/shared/truthtreason && grep -q "EnvBadge" src/app/layout.tsx && grep -q "NEXT_PUBLIC_ENV" apphosting.dev.yaml && git log -1 --oneline | grep -q "v1.1.19" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - truthtreason layout imports + mounts EnvBadge
    - truthtreason apphosting.dev.yaml has NEXT_PUBLIC_ENV=dev, BUILD+RUNTIME
    - truthtreason apphosting.yaml has no NEXT_PUBLIC_ENV
    - truthtreason apphosting.prod.yaml has no NEXT_PUBLIC_ENV
    - Single commit on `feat/cl2-envbadge-mount` in truthtreason repo: `v1.1.19: feat(cl-2)...`
    - No push
  </acceptance_criteria>
  <done>truthtreason mount complete; v1.1.19 atomic commit on feat branch.</done>
</task>

</tasks>

<verification>
1. Inside /Users/mikegeehan/claude/triarch/shared/truthtreason: branch `feat/cl2-envbadge-mount` exists, 1 commit ahead of dev.
2. grep -c "EnvBadge" src/app/layout.tsx returns 2.
3. grep -c "NEXT_PUBLIC_ENV" apphosting.dev.yaml returns 1.
4. grep -c "NEXT_PUBLIC_ENV" apphosting.yaml returns 0.
5. grep -c "NEXT_PUBLIC_ENV" apphosting.prod.yaml returns 0.
6. Working tree clean.
</verification>

<success_criteria>
truthtreason atomic mount commit on feat branch locally; ready for human push. Both shared-ui dep AND transpilePackages config NEW in this repo (special case).
</success_criteria>

<output>
Create `.planning/phases/29-cl2-envbadge-component/29-06-SUMMARY.md` in the PLATFORM repo's planning dir.
</output>
