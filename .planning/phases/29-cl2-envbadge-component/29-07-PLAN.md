---
phase: 29-cl2-envbadge-component
plan: 07
type: execute
wave: 3
depends_on:
  - "29-01"
  - "29-02"
  - "29-03"
  - "29-04"
  - "29-05"
  - "29-06"
files_modified:
  - /Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-SUMMARY.md
  - /Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-HUMAN-UAT.md
autonomous: true
requirements:
  - CL2-01
  - CL2-02
  - CL2-03
  - CL2-04
gap_closure: false

must_haves:
  truths:
    - "All 6 prior plans verified via cross-repo grep checks"
    - "shared-ui vitest passes (6/6 EnvBadge tests + no regression)"
    - "29-SUMMARY.md consolidates work across the 6 plans + 2 repos beyond platform"
    - "29-HUMAN-UAT.md gives a copy-paste runbook for human push + PR + merge + tag + publish + redeploy for all 6 repos in correct sequence"
  artifacts:
    - path: ".planning/phases/29-cl2-envbadge-component/29-SUMMARY.md"
      provides: "Phase-level consolidation document"
      min_lines: 40
    - path: ".planning/phases/29-cl2-envbadge-component/29-HUMAN-UAT.md"
      provides: "Human runbook for push/PR/merge/tag/publish/deploy across 6 repos"
      min_lines: 80
  key_links:
    - from: "29-SUMMARY.md"
      to: "Each of 6 plan summaries"
      via: "links + bullets"
      pattern: "29-0[1-6]-SUMMARY"
---

<objective>
Cross-repo verification and human-handoff packaging. Confirms all 6 prior plans landed correctly across 6 separate git repositories, runs the shared-ui vitest suite as the only automated end-to-end gate available pre-publish, then writes 29-SUMMARY.md (phase consolidation) and 29-HUMAN-UAT.md (the sequential push/PR/merge/tag/publish/deploy runbook the human will execute).

Purpose: Phase 29 ships across 6 repos with intentional inter-repo coupling (consumer PRs require shared-ui v1.5.0 to be published before their CI builds will pass). The HUMAN-UAT document is the linchpin — it's the ordering recipe that prevents 5 broken PRs.

Output: Two .md files in the platform planning dir. One git commit on platform's `feat/cl2-envbadge-mount` branch covering these planning docs (alongside the earlier mount commit from 29-02).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-CONTEXT.md
@/Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-01-PLAN.md
@/Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-02-PLAN.md
@/Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-03-PLAN.md
@/Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-04-PLAN.md
@/Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-05-PLAN.md
@/Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-06-PLAN.md
@/Users/mikegeehan/claude/triarch/shared/platform/CLAUDE.md
@/Users/mikegeehan/claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-repo verification grep + shared-ui vitest</name>
  <files>
    (no file modifications — verification only)
  </files>
  <read_first>
    1. Re-read all six 29-NN-PLAN.md files (already in context above) to know which branch + commit message each plan created.
  </read_first>
  <action>
    Step 1 — Confirm shared-ui v1.5.0 commit and tests:

    cd /Users/mikegeehan/claude/triarch/shared/shared-ui
    git branch --show-current
    git log -1 --oneline
    grep '"version"' package.json
    npx vitest run 2>&1 | tail -10

    Expected output:
    - branch `feat/v1.5.0-envbadge`
    - commit message starts `v1.5.0: feat: EnvBadge`
    - package.json version `1.5.0`
    - vitest exit 0, summary line shows all suites GREEN (existing + 6 new EnvBadge)

    Step 2 — Verify each of the 5 consumer mounts via grep. Loop:

    for repo_path in \
        /Users/mikegeehan/claude/triarch/shared/platform \
        /Users/mikegeehan/claude/triarch/shared/dev-portal \
        /Users/mikegeehan/claude/triarch/shared/darksouls \
        /Users/mikegeehan/claude/triarch/shared/tmi \
        /Users/mikegeehan/claude/triarch/shared/truthtreason; do
      echo "=== $repo_path ==="
      cd "$repo_path"
      echo "branch: $(git branch --show-current)"
      echo "last commit: $(git log -1 --oneline)"
      echo "EnvBadge import: $(grep -c 'EnvBadge' src/app/layout.tsx) (expected 2)"
      echo "NEXT_PUBLIC_ENV in dev yaml: $(grep -c NEXT_PUBLIC_ENV apphosting.dev.yaml) (expected 1)"
      echo "NEXT_PUBLIC_ENV in prod yaml: $(grep -c NEXT_PUBLIC_ENV apphosting.yaml) (expected 0)"
      echo "shared-ui dep: $(grep '@triarchsecurity/shared-ui' package.json | head -1)"
    done

    Expected: each repo on `feat/cl2-envbadge-mount`, last commit starts `v...: feat(cl-2): mount <EnvBadge/>...`, grep counts match. For truthtreason additionally:

    grep -c NEXT_PUBLIC_ENV /Users/mikegeehan/claude/triarch/shared/truthtreason/apphosting.prod.yaml

    Expected: 0.

    Step 3 — Write the verification log to a temp file for use in Task 2:

    OUTPUT=$(mktemp)
    {
      echo "## Cross-repo verification ($(date -u +%FT%TZ))"
      # repeat the loop above and capture into OUTPUT
    } > "$OUTPUT"
    cat "$OUTPUT"

    If ANY check fails (wrong branch, missing import, missing env var, missing dep), STOP and surface which repo + which check.
  </action>
  <verify>
    <automated>cd /Users/mikegeehan/claude/triarch/shared/shared-ui && npx vitest run 2>&1 | tail -3 | grep -E "(passed|0 failed|Test Files)" && for p in /Users/mikegeehan/claude/triarch/shared/{platform,dev-portal,darksouls,tmi,truthtreason}; do grep -q EnvBadge "$p/src/app/layout.tsx" || { echo "MISSING in $p"; exit 1; }; done && echo "ALL OK"</automated>
  </verify>
  <acceptance_criteria>
    - shared-ui vitest GREEN (all existing + 6 new EnvBadge tests)
    - All 5 consumer repos have `EnvBadge` import in src/app/layout.tsx (grep count = 2)
    - All 5 consumer repos have NEXT_PUBLIC_ENV in apphosting.dev.yaml (grep count = 1)
    - All 5 consumer repos do NOT have NEXT_PUBLIC_ENV in apphosting.yaml (grep count = 0)
    - All 6 repos on `feat/cl2-envbadge-mount` (or feat/v1.5.0-envbadge for shared-ui) with their atomic commit at HEAD
  </acceptance_criteria>
  <done>Cross-repo verification complete; verification output captured for use in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write 29-SUMMARY.md and 29-HUMAN-UAT.md to platform .planning/, commit</name>
  <files>
    /Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-SUMMARY.md
    /Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-HUMAN-UAT.md
  </files>
  <read_first>
    1. Read `/Users/mikegeehan/.claude/get-shit-done/templates/summary.md` if it exists, for shape conventions.
    2. Re-read each per-plan SUMMARY in `.planning/phases/29-cl2-envbadge-component/` (29-01-SUMMARY.md through 29-06-SUMMARY.md should exist by now; if not, skip — Task 1's verification still anchors the final summary).
  </read_first>
  <action>
    Step 1 — Write `.planning/phases/29-cl2-envbadge-component/29-SUMMARY.md` containing:

    1. Phase header: phase 29, milestone v2.3, requirements CL2-01..04 (5 of 7 projects per phase scope; security-admin + security-portal deferred to 33/34)
    2. What shipped (per-repo bullet list with the commit hash + branch name from Task 1's verification)
    3. Test results: shared-ui vitest summary (X tests / 0 failed)
    4. Acceptance criteria status: each of CL2-01..04 marked PASS with evidence (which file/grep proves it)
    5. Known limitations:
       - CI builds in 5 consumer PRs cannot succeed until shared-ui v1.5.0 publishes (intentional — sequencing handled in 29-HUMAN-UAT)
       - Stale `fix/deploy-skip-bug` branches in dev-portal/darksouls/tmi still exist (incidental cleanup flagged in HUMAN-UAT)
       - security-admin and security-portal mounts deferred to phases 33/34 (those repos need dev paths first)
    6. Links: each of `29-01-SUMMARY.md` through `29-06-SUMMARY.md` if they exist
    7. Next step: human runs through 29-HUMAN-UAT.md

    Step 2 — Write `.planning/phases/29-cl2-envbadge-component/29-HUMAN-UAT.md` with the following sections:

    Section A — Pre-flight check:
    Commands the human runs to confirm the 6 local branches are in the expected state (mirror Task 1's grep loop).

    Section B — Push + PR + merge sequence (STRICT ORDER):

    Step B1 — shared-ui FIRST:
    ```
    cd /Users/mikegeehan/claude/triarch/shared/shared-ui
    git push -u origin feat/v1.5.0-envbadge
    gh pr create --base main --head feat/v1.5.0-envbadge \
      --title "v1.5.0: EnvBadge component (CL-2)" \
      --body "Phase 29 — adds <EnvBadge env={...}/> for dev chrome marker. Renders only when env in ('dev','staging'); emits data-env attribute for compliance scan."
    # After CI green + merge to main:
    cd /Users/mikegeehan/claude/triarch/shared/shared-ui
    git checkout main && git pull
    git tag v1.5.0
    git push origin v1.5.0
    # CI publish workflow fires on tag push -> publishes @triarchsecurity/shared-ui@1.5.0 to GitHub Packages npm registry
    # Verify package is available:
    npm view @triarchsecurity/shared-ui@1.5.0 version
    # Expected: 1.5.0
    ```

    Step B2 — For each consumer repo (platform, dev-portal, darksouls, tmi, truthtreason) IN PARALLEL after shared-ui publish:
    ```
    cd /Users/mikegeehan/claude/triarch/shared/<repo>
    npm install   # Now resolves @triarchsecurity/shared-ui@^1.5.0 cleanly
    npx next build  # Confirm build passes locally before pushing
    git add package-lock.json  # if it changed
    # If lockfile changed, amend the existing commit:
    git commit --amend --no-edit
    git push -u origin feat/cl2-envbadge-mount
    # Open PR against repo's deploy branch (per workspace CLAUDE.md):
    # - platform / dev-portal / darksouls / tmi -> base dev
    # - truthtreason -> base dev (clean dev branch)
    gh pr create --base dev --head feat/cl2-envbadge-mount \
      --title "v<X.Y.Z>: feat(cl-2): mount <EnvBadge/>" \
      --body "Phase 29 — mounts CL-2 env badge in root layout; sets NEXT_PUBLIC_ENV=dev. Visual on dev URL after FAH deploy."
    ```

    Step B3 — Merge each consumer PR to dev. FAH auto-deploys to that repo's dev backend.

    Step B4 — Visual verify each dev URL (open in browser, confirm yellow "DEV" pill bottom-right):
    - admin-dev.triarch.dev (after platform deploy)
    - portal-dev URL (after dev-portal deploy — note portal-dev hostname not yet in DNS; use Firebase auto-domain `portal-dev--triarch-dev-website.us-central1.hosted.app`)
    - darksouls-dev.triarch.dev (after darksouls deploy)
    - tmi-dev.tmiengine.com (after tmi deploy)
    - truthtreason-dev.triarch.dev (after truthtreason deploy — note hostname not yet in DNS; use FAH auto-domain)

    Step B5 — Compliance scan check (also visual): curl each dev URL HTML and grep for `data-env="dev"`:
    ```
    curl -s https://admin-dev.triarch.dev/ | grep -o 'data-env="dev"'
    ```
    Expected: 1 match per repo. This is what Phase 35's compliance scanner will assert.

    Section C — Prod promotion (per repo, when ready):
    Open second PR `dev -> main` for each repo. Merging triggers prod deploy via the CL-4 gate (only platform has the gate today; others gain it in Phase 32). Confirm prod chrome does NOT show the badge (NEXT_PUBLIC_ENV absent from prod yaml).

    Section D — Incidental cleanup:
    Once Phase 29 PRs merge, delete the stale fix branches:
    ```
    cd /Users/mikegeehan/claude/triarch/shared/dev-portal && git branch -D fix/deploy-skip-bug
    cd /Users/mikegeehan/claude/triarch/shared/darksouls && git branch -D fix/deploy-skip-bug
    cd /Users/mikegeehan/claude/triarch/shared/tmi && git branch -D fix/deploy-skip-bug
    ```

    Section E — Phase 33/34 dependency note:
    security-admin + security-portal mounts are NOT in this phase. They depend on Phase 33/34 restructuring those repos to have dev paths. Once shared-ui v1.5.0 is published, those phases will consume the same EnvBadge.

    Step 3 — Commit the planning docs to platform repo:

    cd /Users/mikegeehan/claude/triarch/shared/platform
    # Already on feat/cl2-envbadge-mount from 29-02; verify:
    git branch --show-current
    git add .planning/phases/29-cl2-envbadge-component/29-SUMMARY.md \
            .planning/phases/29-cl2-envbadge-component/29-HUMAN-UAT.md \
            .planning/phases/29-cl2-envbadge-component/29-*-SUMMARY.md \
            .planning/phases/29-cl2-envbadge-component/29-*-PLAN.md \
            .planning/STATE.md \
            .planning/ROADMAP.md
    git status --short
    git commit -m "docs(phase-29): add 29-SUMMARY + 29-HUMAN-UAT + per-plan summaries"

    DO NOT push.

    Note: this commit lives on `feat/cl2-envbadge-mount` alongside the v2.13.16 mount commit. When the human pushes that branch and merges, both commits land together (the mount + the planning docs). That's fine — planning docs lagging the code in the same PR is normal Phase-close shape.
  </action>
  <verify>
    <automated>ls /Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-SUMMARY.md /Users/mikegeehan/claude/triarch/shared/platform/.planning/phases/29-cl2-envbadge-component/29-HUMAN-UAT.md && cd /Users/mikegeehan/claude/triarch/shared/platform && git log -1 --oneline | grep -q "docs(phase-29)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `29-SUMMARY.md` exists with phase consolidation, per-repo commit hashes, test results, acceptance status, known limitations
    - `29-HUMAN-UAT.md` exists with Section A (preflight), B (push/PR/merge/publish/deploy sequence — shared-ui FIRST), C (prod promotion), D (incidental cleanup), E (Phase 33/34 dep note)
    - Both docs committed to platform `feat/cl2-envbadge-mount` with message starting `docs(phase-29):`
    - No push
  </acceptance_criteria>
  <done>Phase 29 fully packaged for human handoff: code on 6 local branches, planning docs committed to platform branch.</done>
</task>

</tasks>

<verification>
1. shared-ui vitest: all GREEN.
2. 5 consumer repos: branch + commit + grep checks all pass.
3. 29-SUMMARY.md and 29-HUMAN-UAT.md exist in platform .planning/phases/29-cl2-envbadge-component/.
4. Platform feat branch has the docs commit at HEAD.
</verification>

<success_criteria>
Phase 29 ready for human push. The HUMAN-UAT runbook is the critical artifact — it sequences the 6 repos so the human doesn't merge consumer PRs before shared-ui v1.5.0 publishes (which would break their CI). All requirements CL2-01..04 marked complete via evidence.
</success_criteria>

<output>
Update STATE.md and ROADMAP.md to mark Phase 29 as in-progress / ready-for-UAT (use the orchestrator's update step if it runs post-execute; otherwise this plan's commit covers it).

After Phase 29 ships via the HUMAN-UAT runbook, the next phase candidate is Phase 30 (DNS sweep — independent infra work). Phase 33/34 unlock once Phase 30 lands new dev hostnames.
</output>
