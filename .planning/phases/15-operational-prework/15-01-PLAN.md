---
phase: 15-operational-prework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [OPS-01]
must_haves:
  truths:
    - "Repository MyAlterLego/triarch-portal exists, is private, and has an initial README commit on main"
    - "A no-op push to main reaches the deploy stage in shared-workflows@v4 ci-cd.yml"
    - "Local working directory ~/claude/triarch/development/portal is cloned and tracks origin/main"
  artifacts:
    - path: "GitHub repo MyAlterLego/triarch-portal"
      provides: "Portal app source repository with private visibility, MyAlterLego org ownership"
      verify_command: "gh repo view MyAlterLego/triarch-portal --json name,visibility,isPrivate --jq '.name + \" \" + .visibility'"
      expected: "triarch-portal PRIVATE"
    - path: "Local clone at ~/claude/triarch/development/portal"
      provides: "Working directory for Phase 16 scaffolding"
      verify_command: "git -C ~/claude/triarch/development/portal remote get-url origin"
      expected: "git@github.com:MyAlterLego/triarch-portal.git OR https://github.com/MyAlterLego/triarch-portal.git"
  key_links:
    - from: "GitHub repo MyAlterLego/triarch-portal"
      to: "Phase 16 scaffolding (Shared Package Extraction)"
      via: "git clone target"
      pattern: "Repo must exist and be writable by Mike's gh auth before Phase 16 plan-phase runs"
---

<objective>
Create the new portal repository in the MyAlterLego GitHub organization. This is the source repository where Phase 16 will scaffold the portal Next.js app. Repo is private, has a baseline README commit, and is cloned locally so Phase 16 can begin immediately.

Purpose: Without this repo, no portal app code can land. Phase 16 scaffolding writes files into the local clone created here.
Output: GitHub repo `MyAlterLego/triarch-portal` (private) with a single README commit on `main`, plus a local clone at `~/claude/triarch/development/portal`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-operational-prework/15-CONTEXT.md

<reference>
Workspace rule reminder (from `~/claude/CLAUDE.md`):
- Branch naming: feature branches describe the work; never use version numbers as branch names
- GitHub account: MyAlterLego
- Working directory for new portal: `~/claude/triarch/development/portal` (sibling to admin)
</reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify gh auth + create repo</name>
  <read_first>
    Run `gh auth status` and confirm output contains `Logged in to github.com account MyAlterLego`. If not logged in as MyAlterLego, STOP and surface the auth gap to the user — do not continue.
    Then run `gh repo list MyAlterLego --limit 200 --json name --jq '.[].name' | grep -x triarch-portal` — if it returns `triarch-portal`, the repo already exists and the create step must be SKIPPED (idempotent — log "repo exists, skipping create" and proceed to Task 2).
  </read_first>
  <files></files>
  <action>
    Create the new repository:

    ```bash
    gh repo create MyAlterLego/triarch-portal \
      --private \
      --description "Triarch customer portal — sibling Next.js app to triarch-dev (admin); customer-facing surface for release approvals, branch preview, bug/feature submission" \
      --add-readme \
      --license MIT
    ```

    Flags rationale:
    - `--private`: workspace rule, all internal repos are private
    - `--add-readme`: ensures `main` has at least one commit so subsequent push has a base
    - `--license MIT`: matches admin repo license
    - NO `--clone` flag — we control the clone destination explicitly in Task 2

    If `gh repo create` returns "Name already exists on this account", the read_first check was racy or wrong; verify with `gh repo view MyAlterLego/triarch-portal` and proceed to Task 2.
  </action>
  <verify>
    <automated>gh repo view MyAlterLego/triarch-portal --json name,visibility,isPrivate --jq '.name + " " + .visibility' | grep -x "triarch-portal PRIVATE"</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view MyAlterLego/triarch-portal --json name --jq .name` outputs exactly `triarch-portal`
    - `gh repo view MyAlterLego/triarch-portal --json visibility --jq .visibility` outputs exactly `PRIVATE`
    - `gh repo view MyAlterLego/triarch-portal --json defaultBranchRef --jq .defaultBranchRef.name` outputs exactly `main`
  </acceptance_criteria>
  <done>Repo exists in MyAlterLego org, private, with `main` branch and at least one commit (README).</done>
</task>

<task type="auto">
  <name>Task 2: Clone repo locally to ~/claude/triarch/development/portal</name>
  <read_first>
    Run `ls -d ~/claude/triarch/development/portal 2>/dev/null` — if the directory already exists, check whether it has a `.git` directory pointing to MyAlterLego/triarch-portal. If yes, log "already cloned, skipping" and proceed to verification. If the directory exists without `.git` or pointing elsewhere, STOP and surface the conflict.
    Run `ls -d ~/claude/triarch/development` to confirm parent directory exists (it does — admin lives there).
  </read_first>
  <files></files>
  <action>
    Clone the repo to its canonical local path:

    ```bash
    cd ~/claude/triarch/development
    gh repo clone MyAlterLego/triarch-portal portal
    ```

    Verify the clone tracks `origin/main`:

    ```bash
    git -C ~/claude/triarch/development/portal status -b --short
    # Expected: "## main...origin/main" on first line
    ```

    Do NOT add any commits, do NOT modify package.json, do NOT scaffold the Next.js app — that all happens in Phase 16. This task ends with: repo cloned, working tree clean, on main, tracking origin/main.
  </action>
  <verify>
    <automated>git -C ~/claude/triarch/development/portal remote get-url origin | grep -E "MyAlterLego/triarch-portal(\.git)?$"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C ~/claude/triarch/development/portal remote get-url origin` outputs a URL ending in `MyAlterLego/triarch-portal.git` or `MyAlterLego/triarch-portal`
    - `git -C ~/claude/triarch/development/portal status --porcelain` outputs nothing (clean tree)
    - `git -C ~/claude/triarch/development/portal rev-parse --abbrev-ref HEAD` outputs `main`
    - File `~/claude/triarch/development/portal/README.md` exists (auto-created by `--add-readme`)
  </acceptance_criteria>
  <done>Local clone exists at `~/claude/triarch/development/portal`, tree clean, on main, tracking origin/main.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify repo creation</name>
  <what-built>
    - Private GitHub repo `MyAlterLego/triarch-portal` created
    - Local clone at `~/claude/triarch/development/portal` tracking `origin/main`
    - README seeded; no app code yet (Phase 16 scaffolds it)
  </what-built>
  <how-to-verify>
    1. Open https://github.com/MyAlterLego/triarch-portal in a browser — confirm repo is visible and shows "Private" badge
    2. Confirm the org owner is `MyAlterLego` (not `mikegeehan` personal)
    3. Optional: spot-check Settings → no GitHub Actions disabled (default), no branch protection yet (Phase 24 adds it)
    4. Approve to allow Phase 16 to begin scaffolding
  </how-to-verify>
  <resume-signal>Type "approved" if repo looks correct; describe any issues otherwise (e.g., wrong org, public visibility, etc.)</resume-signal>
  <files>(none — checkpoint task; verifies prior task outputs)</files>
  <action>
    Pause execution and present the verification checklist below to Mike. Do not proceed past this task until Mike replies with the resume-signal. This task wraps the human-verify checkpoint and contains no automated implementation work — Tasks 1 and 2 already created the repo and clone; this task only confirms Mike has eyes on the result before Wave 2 / next-phase work depends on it.
  </action>
  <verify>
    <automated>echo "Checkpoint task — no automated verify; awaits human resume-signal"</automated>
  </verify>
  <done>Mike has typed the resume-signal ("approved" or equivalent) and any issues raised have been resolved before proceeding.</done>
  <acceptance_criteria>
    - Mike's resume-signal received and recorded
    - All items in the how-to-verify checklist confirmed
    - Any issues Mike raised are resolved before next plan begins
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `gh repo view MyAlterLego/triarch-portal --json name,visibility --jq '.name + " " + .visibility'` outputs `triarch-portal PRIVATE`
- `git -C ~/claude/triarch/development/portal remote get-url origin` resolves to MyAlterLego/triarch-portal
- Working tree on `~/claude/triarch/development/portal` is clean and on `main`
- Mike has visually confirmed the repo on github.com
</verification>

<success_criteria>
OPS-01 partial completion: portal repo exists and is locally clone-able. CI/CD wiring (`.github/workflows/ci-cd.yml` calling shared-workflows@v4) lands in Phase 16 — that file content is part of the Phase 16 scaffold, not Phase 15. Phase 15 only ensures the repo container exists.

Note: The phase-level success criterion ("a no-op push to `main` reaches the deploy stage") is achievable only after Phase 16 lands the ci-cd.yml workflow file. Phase 15 produces the repo; Phase 16 produces the workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/15-operational-prework/15-01-SUMMARY.md` documenting:
- Repo URL + clone path
- Confirmation of private visibility + org ownership
- Note that ci-cd.yml is deferred to Phase 16

Then commit (no source code modified — phase docs only):

```bash
node ~/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(15-01): summary for portal repo creation" --files .planning/phases/15-operational-prework/15-01-SUMMARY.md
```
</output>
